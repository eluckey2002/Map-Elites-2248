{
  "schemaVersion": 1,
  "purpose": "The candidate stores the receipt gate must cover. Membership only: this file records WHAT is in scope, never whether it passes. A stale receipt is reported by the gate as a failure and cannot be excused here.",
  "removalRule": "Removing a store from solver/ fails the suite until this list changes in the same commit. Archiving stays available; it just requires a signature instead of a silent git mv.",
  "stores": [
    "candidate-levels-52.json",
    "candidate-levels.json"
  ]
}
