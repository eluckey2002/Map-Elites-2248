---
id: INV-001
run: 2026-09-23-u1-baseline-reconciliation
status: complete
executor: orch-investigate
independence: gate
depends_on: []
write_scope:
  - .orch/tickets/2026-09-23-u1-baseline-reconciliation/INV-001.md
excluded_actions:
  - modify source, tests, documentation, generated artifacts, branches, commits, remotes, or Git refs
  - weaken, skip, delete, or reclassify a failing test
  - reserve or rewrite any RESULT identity
  - begin U2 through U7, run a reportable experiment, or redesign Progressive Rigor
  - contact the user
bound: 50 minutes or 45 tool calls, whichever binds first
claimed_by: baseline_inv_gpt_5_6_sol_high
claimed_at: 2026-09-23T17:33:29.7155690Z
profile: orch-worker
---

## Objective

Produce one evidence-complete failure partition and minimal correction scope for the 25 full-suite failures at U1 result `5048f88245907f1178706f4b65c8895ca08313c8`, distinguishing U1-caused generated-state invalidation from pre-existing Windows baseline incompatibility so execution can resume without weakening criterion 7.

## Fixed inputs

- Read-only workspace: `C:\OOO\Map-Elites-2248-u1`, clean branch `codex/level-map-elites-authoring` at `5048f88245907f1178706f4b65c8895ca08313c8`.
- Canonical base: `a98d7f42c10d4b832c98f0f81fbf31bc14421277`.
- Suspended implementation ticket and accepted evidence: `C:\OOO\Map-Elites-2248\.orch\tickets\2026-09-23-u1-oracle-adoption-execution\IMP-001.md`.
- Full-suite result at the fixed identity: 444 tests; 418 pass, 25 fail, 1 skip. Groups recorded by the executor: 2 authoring-server; 6 pilot/receipt identity; 4 candidate-receipt; 1 linked-worktree durable-state; 12 Universe Map/control-view.
- U1-required ledger change: exactly one append-only `DECISION-0007`; it must not be removed, exempted, or rewritten to make checks pass.
- Accepted U1 evidence at the fixed identity: criteria 1-6 PASS, focused oracle 22/22, frozen replay 20/20, experiment tests 32/32, experiment gate PASS.
- Progressive Rigor rule: remain at L1 unless evidence shows a working control needs adversarial Challenge → Repair. A missing co-scoped generated artifact or target-host compatibility repair is an L1 correction.
- Source policy: repository files and tests at the fixed result and canonical base, targeted reruns of only implicated test files, and commit-qualified Git comparisons. Do not rerun the entire full suite unless a targeted file cannot identify the failure.

## Completion test

1. Every one of the 25 failures is mapped to its exact test file/test name, failed expectation, and consuming source/generated artifact. Oracle: targeted `node --test` runs for implicated files plus cited test/source lines at `5048f88245907f1178706f4b65c8895ca08313c8`. Oracle class: deterministic. Provenance: pre-existing.
2. Each failure group is classified as U1-caused, pre-existing-at-base-on-Windows, or unresolved, with evidence that tests the classification rather than inferring from proximity. Oracle: commit-qualified diff/blob comparison to `a98d7f42c10d4b832c98f0f81fbf31bc14421277`, identity-input tracing, and when safe a targeted base-equivalent execution that does not alter Git state. Oracle class: evidence. Provenance: pre-existing.
3. The packet proposes the smallest path-scoped correction tickets, with disjoint scopes where independent, that preserve all accepted U1 evidence and repair criterion 7 without weakening or changing the test oracle. Oracle: every proposed path is traced to a failing consumer/producer and every failure is covered exactly once. Oracle class: evidence. Provenance: pre-existing.
4. The packet returns exactly one rigor decision: `L1 CORRECTION SUFFICIENT`, `ESCALATE TO L2 — EVIDENCE PROVIDED`, or `STOP — UNRESOLVED`, with the material evidence required by the Progressive Rigor rule. Oracle: fixed Progressive Rigor boundary checked against the classified failures. Oracle class: evidence. Provenance: pre-existing.

## Return fields

- status
- result_identity
- verification
- rigor_decision
- failure_partition
- proposed_correction_tickets
- escalation_evidence
- failures_found
- cost_added
- framework_observations
- contradictions
- dead_ends
- gaps
- changed_artifacts

## Result

### Contracted return

- **status:** `complete` for this bounded investigation; ticket frontmatter remains `claimed` for the join.
- **result_identity:** this ticket at `C:\OOO\Map-Elites-2248\.orch\tickets\2026-09-23-u1-baseline-reconciliation\INV-001.md`, based on clean `codex/level-map-elites-authoring@5048f88245907f1178706f4b65c8895ca08313c8` and canonical base `a98d7f42c10d4b832c98f0f81fbf31bc14421277`.
- **verification:** PASS on investigation criteria 1-4. The fixed full-suite observation reproduced exactly: 444 tests, 418 pass, 25 fail, 1 skip. This is not a claim that U1 criterion 7 itself is green.
- **rigor_decision:** `L1 CORRECTION SUFFICIENT`. The excess failures are checkout/materialization, path portability, stale surface expectation, and external durable-state issues. No working control failed in a way that requires adversarial Challenge → Repair.
- **escalation_evidence:** none for L2. There is one acceptance contradiction for the join to preserve: repository instructions require four deliberate failures to remain visible, so “criterion 7 passes” cannot mean exit code zero without violating the pre-existing oracle.
- **failures_found:** 25 reproduced; 21 are excess over the protected four-failure baseline; zero failing test identities are newly caused by U1. U1 does add a real, co-resident `universe/resolved.json` ledger-hash delta.
- **cost_added:** one source-policy-authorized diagnostic full-suite rerun after targeted discovery stopped at 24/25; targeted current and base-equivalent runs; no implementation, receipt rewrite, branch/ref change, reportable experiment, or RESULT identity.
- **framework_observations:** generated consumers must be co-scoped with an identity-bearing ledger append even when the corresponding gate was already red. Target-host checkout policy is also an input: raw-byte identities require an explicit EOL contract or they are not portable identities.
- **changed_artifacts:** only this ticket's `## Result`, `## Verification`, `## Feedback`, and `## Risks`; friction telemetry was logged as required. No repository source, test, documentation, generated view, receipt, or Git state was changed.

### Failure partition

All cited test/producer blobs below are identical at base and result unless explicitly noted. Confidence is high: each failure was reproduced, its consuming line inspected, and its input identity traced.

1. `solver/tests/authoringServer.test.js:76`, **serves only the game assets and exposes the bound candidate** — expected `/2248 Challenge/`; `src/index.html` contains `Keeper — The Apothecary Bench`, served by `solver/authoring-server.js:11-12`. **Pre-existing-at-base-on-Windows (also platform-independent).**
2. `solver/tests/bankAndBreakValidation.test.js:33`, **launcher keeps validation roles separate in ports and recording paths** — expected two `/`-separated tail segments; Windows `path.join` in `prototypes/bank-and-break/serve.js:16-25` produced one backslash-delimited absolute segment. **Pre-existing-at-base-on-Windows.**
3. `solver/tests/humanPilot.test.js:43`, **pilot artifacts bind the exact candidate, seed, batch, base, authority, and non-claims** — `buildPilotArtifacts()` threw `source batch identity mismatch` before assertions.
4. `solver/tests/humanPilot.test.js:76`, **focused inspection reads the durable pilot artifacts and refuses candidate drift** — fixture preparation threw the same mismatch.
5. `solver/tests/humanPilot.test.js:86`, **focused inspection refuses a controlled changed runtime file supplied beside the tree** — fixture preparation threw the same mismatch.
6. `solver/tests/humanPilot.test.js:96`, **challenge receipt is a fresh real recording PASS and same-predicate broken-twin FAIL** — fixture preparation threw the same mismatch.
7. `solver/tests/humanPilot.test.js:121`, **execution verifier invalidates candidate, recording, checker, and challenge identities** — fixture preparation threw the same mismatch. Failures 3-7 all consume `tools/human-pilot.js:37-38,84-88` and `solver/generated-batch-04.json`: working raw SHA-256 `31649856…`, LF/Git and pinned identity `5b0dd3bf…`. **Pre-existing-at-base-on-Windows.**
8. `solver/tests/humanPilot0002.test.js:40`, **committed challenge and execution receipt reproduce and verify PASS** — recomputed serialized challenge differed; `pilots/HUMAN-PILOT-0002/qualify.js:32,69-71,88` hashes working bytes. The checker was recomputed as raw `1a198594…` rather than committed/LF `c7284bb2…`, cascading into challenge/runtime identities. **Pre-existing-at-base-on-Windows.**
9. `solver/tests/receiptGate.test.js:165`, **candidate-levels-52.json has a receipt that verifies against the current bot** — expected current receipt; got `calibration stamp mismatch`. **Protected pre-existing baseline failure** (known owner decision; `receiptGate.test.js:147-180`).
10. `solver/tests/receiptGate.test.js:165`, **candidate-levels-54.json has a receipt that verifies against the current bot** — expected current receipt; got `calibration stamp mismatch`. **Protected pre-existing baseline failure** (non-exempt stale receipt; `receiptGate.test.js:554-569`).
11. `solver/tests/receiptGate.test.js:165`, **candidate-levels.json has a receipt that verifies against the current bot** — expected current receipt; got `calibration stamp mismatch` because `solver/calibration.js:6-10` raw-hashes `engine.js + calib-1.js`: raw `ddf2a077…` versus receipt/LF `c3cefaf3…`. **Pre-existing-at-base-on-Windows.**
12. `solver/tests/receiptGate.test.js:373`, **a receipt whose code has drifted is refused, not verified** — expected `/code\/input identity mismatch/`; the supposedly good receipt failed earlier on the same calibration mismatch, so the injected drift could not reach its intended guard. Consumes the same calibration identity plus `solver/level-author.js:37-46`. **Pre-existing-at-base-on-Windows.**
13. `solver/tests/repoBaseline.test.js:139`, **LIVE: no linked worktree is holding uncommitted .orch state** — expected no offenders; `tools/verify-repo-baseline.js:96-110` found eleven uncommitted `.orch/tickets/**` directories in `C:/OOO/Map-Elites-2248`, including both U1 runs and older 2026-09-08/22 runs. **Pre-existing checkout-state failure at the base; not U1 source-caused.**
14. `solver/tests/universeMap.test.js:68`, **one resolved model keeps admitted evidence separate from the later verified artifact** — expected a resolved model; got archive SHA mismatch.
15. `solver/tests/universeMap.test.js:103`, **the stale-navigation warning fires when CURRENT.md ages past the contract limit** — fixture construction failed on the same archive SHA mismatch.
16. `solver/tests/universeMap.test.js:138`, **the builder is byte-stable and the committed generated views are current** — observed archive SHA mismatch; after LF normalization it still failed because `verifyUniverse` reported the already-known stale date plus all three stale generated views. **Protected pre-existing baseline failure with a co-resident U1 delta.**
17. `solver/tests/universeMap.test.js:173`, **CURRENT links the generated control panel without erasing its historical milestone** — expected the LF-only regex `## Active milestone\n\nAuthor new levels.`; working `CURRENT.md` has CRLF.
18. `solver/tests/universeMap.test.js:181`, **verification fails closed for a missing card** — fixture construction failed on archive SHA before the mutation.
19. `solver/tests/universeMap.test.js:191`, **verification fails closed for an unresolved ledger record** — same early archive SHA failure.
20. `solver/tests/universeMap.test.js:201`, **verification fails closed when selected current evidence is not accepted** — same early archive SHA failure.
21. `solver/tests/universeMap.test.js:214`, **verification fails closed for receipt hash mismatch** — same early archive SHA failure.
22. `solver/tests/universeMap.test.js:225`, **verification fails closed when the artifact verification evidence drifts** — same early archive SHA failure.
23. `solver/tests/universeMap.test.js:236`, **verification observations derive from rebound evidence instead of copied metrics in code** — observed archive SHA mismatch; after LF normalization its final expected `[]` was the documented date-drift failure (`contract asOf 2026-08-28`, today `2026-09-23`). **Protected pre-existing baseline failure.**
24. `solver/tests/universeMap.test.js:267`, **verification fails closed for protected champion identity mismatch even with a rebound hash** — fixture construction failed on archive SHA before the mutation.
25. `solver/tests/universeMap.test.js:285`, **verification fails closed when a generated view drifts** — fixture construction failed on archive SHA before the mutation.

For failures 14-16, 18-25, `tools/universe-map-core.js:133-137` consumed `.orch/runs/2026-08-28-map-elites-independent-round/evidence/archive.json`; raw working SHA-256 was `6b497a34…`, while `universe/contract.json` and LF/Git bytes agree on `ab8ed417…`. Failure 17 consumes `CURRENT.md` directly. After base-equivalent LF normalization, only failures 16 and 23 remained in this file. Therefore failures 14, 15, 17-22, 24, and 25 are Windows-checkout incompatibilities; 16 and 23 are the protected base controls described above.

### U1 versus base

- `git diff --name-only a98d7f42…...5048f882…` names the 24 authorized U1 paths. Among every failure consumer/producer/receipt/control, only `EVIDENCE_LEDGER.md` differs; all other implicated blobs are identical.
- A normalized base-equivalent targeted run produced 62 tests: 55 pass, six fail, one skip. The six were failures 1, 2, 9, 10, 16, and 23. Replacing only its ledger with `5048f882…:EVIDENCE_LEDGER.md` left the same two Universe test identities failing.
- U1 appends exactly `DECISION-0007`, changing normalized ledger SHA-256 from `6e995f34…` to `af9aa407…`. `tools/universe-map-core.js:246-252` places that full hash in `universe/resolved.json`. Normalized base/current expected builds showed `UNIVERSE.md` and `universe/map.html` byte-identical, with only `universe/resolved.json` different. All three committed views were already stale at base, so U1 caused an additional resolved-model delta but no new failing test identity.

### Proposed correction tickets

Every observed failure is assigned exactly once below; scopes are disjoint.

1. **WIN-EOL-PATH (failures 2-8, 11-12, 14-15, 17-22, 24-25; 19 failures).** Scope: `.gitattributes` and `prototypes/bank-and-break/serve.js`. Add an explicit LF materialization contract for repository text (`*.js`, `*.json`, `*.md`, `*.html` at minimum) so fresh Windows worktrees preserve the existing Git/LF identities without rewriting owner-bound receipts; make `createEntries().recordingsDir` separator-stable while retaining a usable filesystem path. Oracle: a fresh Windows checkout at the correction identity runs the six implicated files; pilot receipts reproduce, `candidate-levels.json` and the drift control pass, Universe reduces to only failures 16/23, and the bank-and-break path test passes. Do not edit hashed runtime files or receipts for an EOL-only correction.
2. **AUTHORING-ASSET-CONTRACT (failure 1).** Scope: `src/index.html`. Restore the unchanged test's required `2248 Challenge` phrase while retaining the Keeper branding. Oracle: `authoringServer.test.js` passes 6/6. `src/index.html` is not in either pilot runtime bundle, so no receipt rewrite is required.
3. **DURABLE-STATE-CLOSEOUT (failure 13).** Scope: the eleven exact dirty `.orch/tickets/**` directories reported by the live failure in `C:/OOO/Map-Elites-2248`; no source/test scope. Commit or otherwise durably land their records before removing any worktree, then rerun only the named live repo-baseline test. This ticket must run after the current ticket and suspended U1 ticket stop changing.
4. **PROTECTED-BASELINE-VERIFY (failures 9, 10, 16, 23; read-only verification, no corrective write).** Confirm the post-correction full suite has exactly the four repository-declared failures and no others. Preserve the Level 52 owner decision and do not re-author, archive, exempt, skip, or weaken it. Record the new total (U1 added tests, so the inherited `421/416` count is stale) through the owning documentation process only after the result identity is fixed. If the project separately authorizes a real Universe freshness review, scope `universe/contract.json`, `UNIVERSE.md`, `universe/map.html`, and `universe/resolved.json` together and consume the current U1 ledger; that is not required to restore the protected baseline.

Dependencies: tickets 1 and 2 are independent; ticket 3 waits for durable writers; ticket 4 verifies their integrated identity. The latent U1 `universe/resolved.json` delta is assigned to protected failure 16 and is not double-counted.

### Contradictions, dead ends, and gaps

- **contradictions:** `AGENTS.md` at both base and result says the suite has 421 tests with four deliberate failures; U1 now has 444 tests, so the count is stale, but its named four-failure set is confirmed by the normalized probe. IMP-001's handoff attributed the 12 Universe failures to the U1 ledger append; primary evidence shows 10 are Windows-only and the other two already failed at base. A zero-exit reading of criterion 7 conflicts with the explicit protected Level 52 failure and cannot be satisfied without changing/weaking the oracle.
- **dead_ends:** `recordingReplay.test.js` passed 22/22; import search found no second authoring-server consumer; no saved U1 full-suite transcript existed; a first archive extraction inherited Windows EOL conversion and answered nothing until the commit-qualified text was normalized. These probes were not used as positive evidence.
- **gaps:** no correction was implemented, so fresh-checkout acceptance for the proposed `.gitattributes` policy remains for WIN-EOL-PATH. The live durable-state failure depends on other agents' ticket directories and must be rechecked after their writers finish. This investigation did not adjudicate whether the project wants to clear the four deliberately red controls; doing so is outside the U1 restoration objective.


## Verification

- Criterion 1: **PASS** — all 25 failures are mapped above to exact test file/name, failed expectation, and consumer. Targeted reruns plus the one permitted locator suite reproduced the fixed counts.
- Criterion 2: **PASS** — commit-qualified blobs show only the ledger changed among failure inputs; raw/LF hashes and the LF-normalized base/current executions test the classification. All 25 failing test identities are pre-existing-at-base-on-Windows/check-out state; U1's sole generated delta is separately identified.
- Criterion 3: **PASS** — the four proposed packets have disjoint write scopes and assign failures 1-25 exactly once (19 + 1 + 1 + 4). They preserve U1 criteria 1-6 and the existing receipt/test oracles.
- Criterion 4: **PASS** — `L1 CORRECTION SUFFICIENT`; no evidence crosses the Progressive Rigor boundary into an adversarial control repair.
- Read-only workspace postcondition: clean `codex/level-map-elites-authoring@5048f88245907f1178706f4b65c8895ca08313c8`; no source or Git mutation.

## Feedback

- The U1 execution cut correctly protected the ledger append, but its criterion-7 oracle omitted the repository's deliberate-red baseline semantics and the Windows checkout identity policy. Future cuts should freeze the exact expected failure set/count and declare EOL as an identity input.

## Risks

- Criterion 7 remains failed at the fixed U1 identity until the correction packets are executed and independently verified.
- Applying a source-code normalization fix instead of a checkout/EOL contract would change the pilot verifier identities and cascade into owner-bound attestations; the proposed `.gitattributes` route avoids that evidence rewrite.
- Any correction commit changes the result identity; criteria 1-6 must be reverified at the integrated correction identity, while the four protected baseline failures must remain visible unless separately authorized.
