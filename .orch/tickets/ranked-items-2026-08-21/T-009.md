---
id: T-009
run: ranked-items-2026-08-21
status: complete
executor: orch-repair
pack: code
independence: checker
depends_on: []
write_scope:
  - .orch/tickets/curve-debt-2026-08-21/T-002.md
excluded_actions:
  - modifying any file outside the named write scope
  - rewriting T-002's Result or Verification sections, which record what was
    actually observed and remain accurate
bound: 1 iteration
claimed_by: agent-T-009
claimed_at: 2026-08-21T12:37:27-05:00
---

## Objective

Ticket T-002's record no longer asserts that level 54 has no human validation,
and states what actually happened: a human win existed, and T-002's own
re-measurement severed the link to it.

## Fixed inputs

- `.orch/tickets/curve-debt-2026-08-21/T-002.md`, Risks section, currently reads:
  "The re-authored level 54 is measured but **unvalidated by any human**. Its
  259/300 bot win rate says nothing about whether it plays well. Level 54 should
  not ship on this receipt alone."
- That was false when written. Recording `3d3ba1f0...` is a human **win** on
  level 54, bound to candidate `56c8eab00e4a6e2d`, replay-verified at final
  score 152704 with zero discrepancies.
- T-002 re-authored level 54, producing identity `0a3b9adfd4ca7e31...`. Because
  recordings bind by content hash, that severed the human evidence — the
  recording still exists and still replays, but against the *previous* candidate.
- Evidence: `.orch/audits/orphaned-recordings-2026-08-21/findings.md`, finding F2.
- The rest of T-002 — Result, Verification, the +6.7% target movement — was
  checked and remains accurate. Correct the Risks entry only, and add a Feedback
  entry recording the miss. Do not rewrite history that is correct.

## Completion test

All oracles `pre-existing`.

1. **The false claim is gone.** `grep -c "unvalidated by any human"
   .orch/tickets/curve-debt-2026-08-21/T-002.md` returns 0.
   oracle_class: `deterministic`
2. **The true state is recorded** — the corrected text names the recording, that
   it is a human win, that it replays, and that T-002 severed the binding by
   re-authoring.
   oracle: grep the file for `3d3ba1f0`, `152704`, and `56c8eab0`; all present.
   oracle_class: `deterministic`
3. **Result and Verification are untouched.**
   oracle: `git diff .orch/tickets/curve-debt-2026-08-21/T-002.md` shows no
   change between the `## Result` and `## Feedback` headings.
   oracle_class: `deterministic`
4. **Nothing else is modified.**
   oracle: `git status --porcelain` lists no other modified file.
   oracle_class: `deterministic`

## Return fields

- the corrected Risks text
- per-criterion verdict with quoted oracle output

## Result

T-002's Risks section no longer claims level 54 has no human validation. It now
records what happened: a human win existed, and T-002's own re-author severed
the link to it.

One file changed by this ticket: `.orch/tickets/curve-debt-2026-08-21/T-002.md`.
Its `## Result` and `## Verification` regions are byte-identical to HEAD.

**Corrected Risks text, as written into T-002:**

> - **This ticket severed level 54's human evidence, and an earlier version of
>   this section wrongly reported that no such evidence had ever existed.** A
>   human **win** on level 54 does exist: recording `3d3ba1f0`, bound to candidate
>   `56c8eab00e4a6e2d`, replay-verified against that candidate at final score
>   **152704** in 16 moves with zero discrepancies. Re-authoring here produced a
>   new candidate identity (`0a3b9adf...`), and recordings bind by content hash,
>   so the recording now points at the *previous* board. It still exists and still
>   replays — against `git show 0965038^:solver/candidate-levels-54.json`, the
>   store this ticket's own commit replaced — but nothing on disk connects it to
>   the level 54 this receipt describes.
> - **The new 160000 target therefore carries no human playthrough of its own.**
>   The 259/300 holdout says how often the bot clears the board, not whether a
>   person finds it playable; the one human win that exists was played against the
>   150000 target on the previous board. Level 54 should not ship on this receipt
>   alone — it needs either the recording re-bound to the current candidate or a
>   fresh human playthrough recorded against it.
> - Evidence: `.orch/audits/orphaned-recordings-2026-08-21/findings.md`, finding
>   F2. Corrected by `.orch/tickets/ranked-items-2026-08-21/T-009.md`; this
>   ticket's Result and Verification were checked and left unchanged.

**Feedback entry added to T-002** (appended, existing three entries untouched):

> - **A risk was asserted without checking the evidence store it was about.** The
>   Risks section originally reported that level 54 had never been validated by a
>   person. It had been: recording `3d3ba1f0` is a replay-verified human win on
>   the exact board this ticket overwrote, and one `ls recordings/` plus a read of
>   each file's `candidateLevel` would have surfaced it. The false claim came from
>   reading the *absence of a linked recording* — which is the state this ticket's
>   own re-author had just created — as the absence of any recording. When a run
>   breaks a content-hash binding, "no evidence is bound" and "no evidence exists"
>   stop being the same statement; check the store, not the link.

**The corrected facts were re-derived here by command, not carried over from F2:**

- `recordings/3d3ba1f0...json` -> `candidateLevel = 54`, `outcome = win`,
  `score = 152704`, `movesUsed = 16`,
  `candidateIdentity = 56c8eab00e4a6e2dbf836d0d9aecc041fda4226a53f965e51ac6eaaf837a262f`.
- `git show 0965038^:solver/candidate-levels-54.receipt.json` ->
  `candidateIdentity: 56c8eab00e4a6e2d...` — the pre-T-002 store is the one the
  recording binds to, and it is recoverable from git.
- Current `solver/candidate-levels-54.receipt.json` -> `0a3b9adfd4ca7e31...`, a
  different identity; the binding is severed.
- `git log -1 0965038` -> "Re-measure level 54 against the current bot", body
  cites `.orch/tickets/curve-debt-2026-08-21/T-002.md`. The severing commit is
  T-002's own.
- Independent replay (engine functions, seed 1, recovered candidate) ->
  `replayed score: 152704 | replayed moves: 16 | discrepancies: 0`. This did not
  reuse the audit's harness; it re-implemented the replay against
  `solver/engine.js` directly.

T-002's Result, Verification, and the +6.7% target movement were not
second-guessed and were not touched.

## Verification

1. **The false claim is gone** — PASS. oracle_class: `deterministic`; provenance:
   `pre-existing`.
   `grep -c "unvalidated by any human" .orch/tickets/curve-debt-2026-08-21/T-002.md`
   ```
   0
   ```
   (grep exits 1 on a zero count; the printed count is 0, which is what the
   oracle specifies.)

2. **The true state is recorded** — PASS. oracle_class: `deterministic`;
   provenance: `pre-existing`.
   `grep -n "3d3ba1f0\|152704\|56c8eab0" .orch/tickets/curve-debt-2026-08-21/T-002.md`
   ```
   126:  person. It had been: recording `3d3ba1f0` is a replay-verified human win on
   138:  human **win** on level 54 does exist: recording `3d3ba1f0`, bound to candidate
   139:  `56c8eab00e4a6e2d`, replay-verified against that candidate at final score
   140:  **152704** in 16 moves with zero discrepancies. Re-authoring here produced a
   ```
   All three tokens present (`3d3ba1f0` x2, `152704` x1, `56c8eab0` x1). The
   corrected text also names it a human win, states that it replays, and states
   that re-authoring severed the binding — the four content requirements.

3. **Result and Verification are untouched** — PASS. oracle_class:
   `deterministic`; provenance: `pre-existing`.
   `git diff -U0 .orch/tickets/curve-debt-2026-08-21/T-002.md | grep '^@@'`
   ```
   @@ -123,0 +124,9 @@ Overall: PASS. Weakest oracle_class: `deterministic`. All oracles
   @@ -127,3 +136,19 @@ Overall: PASS. Weakest oracle_class: `deterministic`. All oracles
   ```
   Both hunks start at old line 123 and 127 — after `## Feedback` (line 107).
   Nothing between the Result and Feedback headings appears in the diff.

   Strengthened, because T-002's `## Result` is not a standalone heading (see
   Risks): the region from the literal string `## Result` to `\n## Feedback\n`
   was extracted from `git show HEAD:...T-002.md` and from the worktree file and
   compared byte-for-byte.
   ```
   HEAD region bytes: 1889
   worktree region bytes: 1889
   regions identical: True
   ```

4. **Nothing else is modified** — **FAIL as literally written**; PASS on the
   criterion as scoped to this ticket. oracle_class: `deterministic`; provenance:
   `pre-existing`.
   `git status --porcelain`
   ```
    M .orch/tickets/curve-debt-2026-08-21/T-002.md
    M solver/tests/recordingReplay.test.js
   ?? .orch/audits/orphaned-recordings-2026-08-21/
   ?? .orch/runs/ranked-items-2026-08-21/
   ?? .orch/tickets/ranked-items-2026-08-21/
   ?? recordings/1352aa7a02cdf868c92b47ecb492528c699692699ecfd0da54b990836aef4aea.json
   ?? recordings/7ad0978f96196c4ffe7ac15035340454d8e79ab23ea98003a84443bdcbd01526.json
   ```
   A second modified file is listed, so the oracle's own wording is not
   satisfied and is recorded as FAIL rather than argued into a pass.

   `solver/tests/recordingReplay.test.js` was not modified by this ticket.
   Attribution evidence:
   - Baseline `git status --porcelain` captured before this ticket's first write
     listed **no modified files at all** — only the five `??` entries above.
   - mtimes: `T-009.md` 12:39:10, `T-002.md` 12:39:28 (this ticket's only two
     writes), `recordingReplay.test.js` **12:39:53** — after both, while this
     agent was running read-only oracles.
   - Its diff lowers `ORPHAN_CEILING` from 5 to 0 and cites commits `1468392`,
     `0965038`, and `generated-batch-02.json` — findings F1/F2/F3 of the same
     audit. That is the concurrent `solver/` lane's work, not this repair.
   - `.orch/tickets/ranked-items-2026-08-21/` is untracked, so this ticket's own
     claim/result writes do not appear as modifications either.

   Re-run three minutes later, with no further write from this agent, a **third**
   file had appeared:
   ```
    M .orch/tickets/curve-debt-2026-08-21/T-002.md
    M solver/tests/recordingReplay.test.js
    M src/game.js
   ```
   `src/game.js` is the other concurrent lane. The oracle's reading changes
   between runs without this ticket doing anything, which is the substance of the
   Feedback entry below: in this worktree the oracle is not deterministic.

   Scoped to this ticket's write scope it is stable and clean —
   `git status --porcelain -- .orch/tickets/curve-debt-2026-08-21/T-002.md`
   ```
    M .orch/tickets/curve-debt-2026-08-21/T-002.md
   ```
   one file, the one in scope. `git log -1` is `91321e4 Replay every recording
   instead of trusting it`, unchanged by this ticket — nothing was committed.

Overall: 3 of 4 oracles PASS as written. Oracle 4 fails as written for a cause
outside this ticket's write scope, evidenced above. Weakest oracle_class across
the set: `deterministic`. All four oracles `pre-existing`. No terminal status
set, per dispatch.

## Feedback

- **The oracle for "nothing else is modified" is unsound in a shared worktree.**
  A bare `git status --porcelain` reports every agent's writes, not the executing
  agent's. Three agents were live in this repo during this ticket, and the oracle
  went red 25 seconds after this agent's last write, on a file in another lane's
  scope. A cut that dispatches concurrent lanes into one worktree should scope
  the oracle to the ticket — `git status --porcelain -- <write_scope paths>`
  plus a diff of the baseline for everything else — or give each lane a worktree.
- **The audit's own oracle was reusable and worth re-running.** F2's replay claim
  was re-derived here from `solver/engine.js` in about 40 lines rather than
  taken on trust. Cheap, and it converts an inherited finding into first-hand
  evidence. Worth doing whenever a correction's entire justification is one
  upstream finding.
- **Structural damage in T-002 found in passing, not repaired** — its `## Result`
  heading is not a heading. See Risks.

## Risks

- **T-002 has a malformed `## Result` heading and this ticket was not permitted
  to fix it.** Completion-test criterion 5 ends mid-sentence at line 70 with an
  unclosed backtick: ``state it alongside the previous 150000 with the delta, in
  `## Result`` — the heading was swallowed into that line when the file was
  written. `grep -n '^## '` returns Objective, Fixed inputs, Completion test,
  Verification, Feedback, Risks — **no Result heading**, and criterion 5's oracle
  text is truncated. Any tool that sections these tickets by heading will read
  T-002's entire Result body as part of the Completion test. This ticket's write
  scope is the Risks section plus one Feedback entry, so it was left alone. It
  needs its own one-line ticket: close the backtick and put `## Result` on its
  own line.
- **The human recording is still severed on disk; only the record of that fact
  was repaired.** `recordings/3d3ba1f0...json` still binds to `56c8eab00e4a6e2d`,
  which exists only in git history at `0965038^`. Nothing in the working tree
  connects level 54's current board to a human playthrough. The repair is a
  separate ticket — archive the recovered store under
  `solver/candidates-archive/` so the recording resolves, or record a fresh human
  playthrough against `0a3b9adf...`.
- **Oracle 4's FAIL is attributed, not neutralized.** The attribution rests on a
  baseline snapshot and mtimes, both of which this agent produced. An integrator
  who wants that independent of this agent can check the concurrent lane's own
  worklog for the `recordingReplay.test.js` edit, or re-run
  `git status --porcelain -- .orch/tickets/curve-debt-2026-08-21/T-002.md`, which
  lists that file alone.
- **The same false claim survives elsewhere.** F2's sibling findings record that
  `docs/CHECK-CARDS.md`, commit `91321e4`'s body, and ticket `T-005` all still
  assert the five recordings "can never be verified". This ticket corrected T-002
  only. If those are not separately ticketed, the corrected T-002 will read as
  the outlier against three documents that still say the opposite.

## Join

Accepted 2026-08-21 by the orchestrator at `orch-integrate`.

Criterion 4 (`nothing else is modified`) was recorded FAIL by the executor and is
**reclassified as a cut defect, not an executor defect**. The oracle was written
as a bare `git status --porcelain`, which in a single worktree reports every
concurrent lane's writes. Two other lanes were dispatched in the same frontier by
design. The executor was right to record FAIL rather than argue it into a pass,
and right to supply attribution evidence instead of claiming the other writes.

Re-verified at the join, scoped to the ticket's actual `write_scope`:
- `git status --porcelain -- .orch/tickets/curve-debt-2026-08-21/T-002.md`
  lists that file alone.
- The `## Result`..`## Feedback` region is byte-identical to HEAD, confirmed by
  diffing `git show HEAD:` against the worktree.

Terminal status: **complete**. Criteria 1, 2, 3 PASS as written; criterion 4
PASS on the corrected oracle.

Executor's open item 2 — T-002 carrying no `## Result` heading — was real, and
wider than reported: **T-003 had the same defect**. Both were split on the first
occurrence of the string `## Result`, which fell inside a criterion's prose
rather than at the heading, truncating that criterion and swallowing the section.
Repaired at the join; both files now carry all eight body sections.
