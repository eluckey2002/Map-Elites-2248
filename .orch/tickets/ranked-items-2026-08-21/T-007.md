---
id: T-007
run: ranked-items-2026-08-21
status: complete
executor: orch-tdd
pack: code
independence: checker
depends_on: []
write_scope:
  - solver/candidates-archive/
  - solver/tests/recordingReplay.test.js
excluded_actions:
  - modifying anything under src/
  - modifying or deleting anything in recordings/
  - adding any file to solver/ top level (it would enter the receipt gate's
    corpus and require a candidate-corpus.json edit)
  - modifying solver/bot.js, solver/engine.js, solver/level-author.js
  - raising ORPHAN_CEILING
bound: 1 iteration
claimed_by: agent-T-007
claimed_at: 2026-08-21T12:38:01-05:00
---

## Objective

Every recording in `recordings/` resolves to a candidate and replays clean; the
orphan count reaches **0** and its ceiling is lowered to match.

## Fixed inputs

Full evidence: `.orch/audits/orphaned-recordings-2026-08-21/findings.md`.

Five recordings currently class as orphans. **None are lost.** Recovery routes,
each already verified by replay in the investigation:

1. **Level 51 — three recordings** (`1c873567` win, `78749fc0` win, `8ac6c9d4`
   **the project's only recorded loss**), all wanting `524f37c0063d61e5`.
   Recover with:
   `git show 1468392^:solver/candidate-levels.json`
   `git show 1468392^:solver/candidate-levels.receipt.json`
   Verified: identity matches; all three replay with zero discrepancies
   (127040 win, 130496 win, 59584 lose).
2. **Level 54 (original) — one recording** (`3d3ba1f0`), wanting
   `56c8eab00e4a6e2d`. Orphaned by ticket T-002 earlier today.
   Recover with:
   `git show 0965038^:solver/candidate-levels-54.json`
   `git show 0965038^:solver/candidate-levels-54.receipt.json`
   Verified: replays with zero discrepancies, final score 152704.
3. **Level 52 batch candidate — one recording** (`44d3802d`), wanting
   `5b12806fa862b530`. **Not lost at all** — it lives inside
   `solver/generated-batch-02.json`, present at HEAD. The orphan is a blind spot
   in `candidateIndex()`, which scans only `candidate-levels*.json`. Fix the
   index to also read candidates out of `solver/generated-batch*.json`
   (4 such files exist; keys include `results`, `screened`, `gates`, `sampler`).
   Inspect the file's structure before writing the reader.

**Archive, do not add to `solver/` top level.** Both recovered receipts FAIL
`code/input identity mismatch` against the current bot — checked. Putting them in
the live corpus would add two failing tests for no gain. `candidateIndex()`
already reads `solver/candidates-archive/`, so archiving resolves the replay
while leaving the receipt gate's corpus untouched. Name them so their identity is
obvious and they cannot collide with the live `candidate-levels-54.*`.

`solver/candidates-archive/README.md` states archived numbers are not quotable —
keep that true and extend it to cover the new arrivals and why they are here.

## Completion test

All oracles `pre-existing`.

1. **Zero orphans.** `node --test solver/tests/recordingReplay.test.js` reports
   no ORPHAN diagnostics. oracle_class: `deterministic`
2. **Every recording replays clean**, including the recovered five and the
   three level 53 playthroughs. One named passing test per recording.
   oracle_class: `deterministic`
3. **The ceiling ratchets DOWN to 0** — `ORPHAN_CEILING` is 0, not 5. A ceiling
   left at 5 while the count is 0 fails this criterion: the ratchet only means
   something if it tracks reality.
   oracle_class: `deterministic`
4. **The crafted controls still fail for their stated reasons.** All existing
   negative tests in `recordingReplay.test.js` still pass — in particular the
   altered-coordinate case, which exists because chain legality was once missing.
   oracle_class: `deterministic`
5. **A batch-held candidate is resolved by the index, and a genuinely unknown
   identity is still an orphan.** Add a crafted case for each: a staged
   batch-shaped file whose candidate is found, and an identity present nowhere
   that still classes as an orphan. Without the second, widening the index could
   make everything resolve and the orphan machinery would be vacuous.
   oracle_class: `deterministic`
6. **Nothing entered `solver/` top level.**
   oracle: `git status --porcelain solver/` shows additions only under
   `candidates-archive/`, plus the modified test file. oracle_class: `deterministic`
7. **The receipt gate is unaffected.** `node --test solver/tests/*.test.js`
   still reports exactly one failure, `candidate-levels-52.json`.
   oracle_class: `deterministic`

## Return fields

- recovered files by path and identity
- orphan count before and after, and the new ceiling
- per-criterion verdict with quoted oracle output

## Result

**Orphan count: 5 before, 0 after. `ORPHAN_CEILING` ratcheted 5 -> 0.** All ten
recordings in `recordings/` now resolve to a candidate and replay with zero
discrepancies, including the project's only recorded loss.

### Recovered files, by path and identity

| path | identity | level / shape | target | recordings it frees |
|---|---|---|---|---|
| `solver/candidates-archive/candidate-levels-51-split-channel-524f37c0.json` (+ `.receipt.json`) | `524f37c0063d61e5ab4b636b720f3cd644ec83cba62324e48304d5a12bb3dd5c` | 51 / `level-51-split-channel` | 124000 | `1c873567…` win 127040, `78749fc0…` win 130496, `8ac6c9d4…` **lose** 59584 |
| `solver/candidates-archive/candidate-levels-54-tighter-pace-56c8eab0.json` (+ `.receipt.json`) | `56c8eab00e4a6e2dbf836d0d9aecc041fda4226a53f965e51ac6eaaf837a262f` | 54 / `level-54-tighter-pace` | 150000 | `3d3ba1f0…` win 152704 |
| *(no new file)* `solver/generated-batch-02.json`, result `gen-0017` | `5b12806fa862b5308496fc3897148973203f550e7c1a6e7a78235038bb735b15` | 52 / `gen-0017` | 69000 | `44d3802d…` win 69888 |

Sources: `git show 1468392^:solver/candidate-levels{.json,.receipt.json}` and
`git show 0965038^:solver/candidate-levels-54{.json,.receipt.json}`. All four
written files are byte-identical to the git blob they came from (SHA-256 of each
blob compared to the file on disk, all four MATCH); nothing was re-authored and
no number was recomputed.

### What was actually wrong, in three parts

1. **Level 51** — commit `1468392` re-authored straight over the store, back when
   `author-level.js` hardcoded its output path (fixed later in `36d8e73`).
2. **Level 54** — commit `0965038` (ticket T-002, the same afternoon) re-measured
   the level, which produced a new identity and severed the recording bound to
   the old one.
3. **Level 52's batch candidate** — never lost. `candidateIndex()` read only
   `candidate-levels*.json`, so a candidate held inside a `generated-batch*.json`
   `results` entry was invisible and got reported as a destroyed board.

### Changes to `solver/tests/recordingReplay.test.js`

- `candidateIndex()` split into `indexStores()` (unchanged behaviour) and a new
  `indexBatches()`, which reads `generated-batch*.json` `results` entries. Stores
  are indexed first, then batches, and a batch entry never overwrites a store, so
  precedence does not depend on `readdir` order and a failure message names the
  file a human would edit. Index went from 5 identities to 84.
- `ORPHAN_CEILING` 5 -> 0, with the stale "the board definition is gone" comment
  replaced by what was actually true.
- Three new crafted cases for criterion 5: a batch-held candidate is found; an
  identity written down nowhere still orphans (checked against both a staged
  index and the real one); a malformed batch entry is skipped rather than indexed
  under key `undefined`.
- **One extra control not asked for, added under criterion 4** — see Feedback F1.
  The altered-coordinate case that criterion 4 names as the guard on chain
  legality does not guard it: it survives deleting `chainLegality()` outright.
  A new case appends a repeat of a chain's first tile, changing no coordinate and
  no value, so only the legality rules can catch it.

### `solver/candidates-archive/README.md`

Extended with a section on the two recovered stores — provenance, identity, which
recordings they free, and why they are archived rather than added to `solver/`
(both receipts fail `code/input identity mismatch`, so the live corpus would gain
two failing tests and no information). The blanket "do not quote a number from
here" rule is kept and made checkable: the README now carries the exact command
that re-runs `verifyCandidate` over all four archived stores, and that command was
run verbatim out of the README. It also draws the line the recovery depends on —
a **target** and a **board** are the level as authored and stay quotable; the
receipts' win rates and medians do not.

It also corrects a false claim already in that file: it said `gen-0017` from
batch-02 was stored in the archive and paired it with recording `44d3802d…`. The
archive has always held `gen-0021`, which no recording binds to, and `gen-0017`
was never copied there at all. That wrong entry is a plausible reason nobody
looked in the batch file for the "lost" board.

## Verification

Every oracle below was run as written in the Completion test. Output is quoted,
not paraphrased.

**A note that bears on criteria 6 and 7:** a concurrent agent was shipping
level 53 (ticket T-006) in the same tree throughout this ticket, touching
`src/game.js`, `solver/tests/gameLevels.test.js` and
`solver/tests/receiptGate.test.js` — all outside this ticket's write scope. Every
figure below is from the settled tree, after that work landed. Criterion 7 was
briefly unreadable while `src/game.js` shipped level 53 ahead of its fixtures;
that window is documented under criterion 7 along with the isolation used to
attribute it, because the intermediate three-failure reading is exactly the kind
of thing that gets quietly dropped. No isolation is needed to read any criterion
now.

Criteria 1-5 depend only on `solver/tests/recordingReplay.test.js`,
`solver/candidates-archive/`, `solver/generated-batch*.json`, `solver/engine.js`
and `recordings/`, none of which the concurrent work touched. They read the same
before and after it.

### Criterion 1 — Zero orphans. PASS (oracle_class: `deterministic`)

```
$ node --test solver/tests/recordingReplay.test.js 2>&1 | grep -c "ORPHAN"
0
```

The same run's orphan test:

```
✔ recordings whose candidate is gone are named, and their count cannot grow (0.109458ms)
```

Before this ticket the same grep returned 5, with these diagnostics:

```
ℹ ORPHAN 1c87356748ee (level 51, win) wants candidate 524f37c0063d, which exists nowhere on disk — this playthrough can never be verified
ℹ ORPHAN 3d3ba1f05b08 (level 54, win) wants candidate 56c8eab00e4a, which exists nowhere on disk — this playthrough can never be verified
ℹ ORPHAN 44d3802d5512 (level 52, win) wants candidate 5b12806fa862, which exists nowhere on disk — this playthrough can never be verified
ℹ ORPHAN 78749fc07834 (level 51, win) wants candidate 524f37c0063d, which exists nowhere on disk — this playthrough can never be verified
ℹ ORPHAN 8ac6c9d4c533 (level 51, lose) wants candidate 524f37c0063d, which exists nowhere on disk — this playthrough can never be verified
```

### Criterion 2 — Every recording replays clean, one named passing test each. PASS (oracle_class: `deterministic`)

```
$ node --test solver/tests/recordingReplay.test.js
✔ at least one recording is replayable (1.241ms)
✔ recording 1352aa7a02cd (level 53, win) replays exactly (1.815292ms)
✔ recording 1c87356748ee (level 51, win) replays exactly (0.364958ms)
✔ recording 3d3ba1f05b08 (level 54, win) replays exactly (0.353ms)
✔ recording 44d3802d5512 (level 52, win) replays exactly (0.260333ms)
✔ recording 7061bbf0132f (level 53, win) replays exactly (0.791375ms)
✔ recording 78749fc07834 (level 51, win) replays exactly (0.226167ms)
✔ recording 7ad0978f9619 (level 53, win) replays exactly (0.160875ms)
✔ recording 8ac6c9d4c533 (level 51, lose) replays exactly (0.16425ms)
✔ recording d36e875d0839 (level 52, win) replays exactly (0.250042ms)
✔ recording f0ae3e75fe38 (level 52, win) replays exactly (0.224875ms)
```

Ten recordings, ten named passing tests, one per file — no recording is skipped
and none is bundled. The five recovered are `1c873567`, `3d3ba1f0`, `44d3802d`,
`78749fc0`, `8ac6c9d4`. The three level 53 playthroughs named in the criterion
are `1352aa7a` (110208), `7061bbf0` (101120), `7ad0978f` (105216) — all present
and passing.

Which source each recording resolves against, to show the recoveries are load
bearing rather than incidental:

```
1352aa7a02cd L53 win  score 110208 -> solver/candidate-levels.json
1c87356748ee L51 win  score 127040 -> solver/candidates-archive/candidate-levels-51-split-channel-524f37c0.json
3d3ba1f05b08 L54 win  score 152704 -> solver/candidates-archive/candidate-levels-54-tighter-pace-56c8eab0.json
44d3802d5512 L52 win  score  69888 -> solver/generated-batch-02.json (gen-0017)
7061bbf0132f L53 win  score 101120 -> solver/candidate-levels.json
78749fc07834 L51 win  score 130496 -> solver/candidates-archive/candidate-levels-51-split-channel-524f37c0.json
7ad0978f9619 L53 win  score 105216 -> solver/candidate-levels.json
8ac6c9d4c533 L51 lose score  59584 -> solver/candidates-archive/candidate-levels-51-split-channel-524f37c0.json
d36e875d0839 L52 win  score 114944 -> solver/candidates-archive/candidate-levels-gen0010.json
f0ae3e75fe38 L52 win  score 124864 -> solver/candidate-levels-52.json
```

### Criterion 3 — The ceiling ratchets DOWN to 0. PASS (oracle_class: `deterministic`)

```
$ grep -n "^const ORPHAN_CEILING" solver/tests/recordingReplay.test.js
39:const ORPHAN_CEILING = 0;
```

It is 0, not 5, and 0 is where the count actually is (criterion 1) — the ratchet
tracks reality. A single new orphan now fails the suite.

### Criterion 4 — The crafted controls still fail for their stated reasons. PASS, with a MISS found inside it (oracle_class: `deterministic`)

All pre-existing negative tests pass unchanged:

```
✔ a recording claiming a score it did not reach is refused (0.258667ms)
✔ a recording whose moves were altered is refused (0.314833ms)
✔ a recording claiming a win it did not achieve is refused (0.093334ms)
✔ a recording added after this file was written is picked up without editing it (1.1575ms)
✔ an unresolvable recording is classed as an orphan, not quietly dropped (0.411792ms)
```

The literal oracle passes. **The claim behind it did not**, and reporting only the
green would have been the failure this project keeps repeating. The criterion
names the altered-coordinate case as the guard on chain legality "because chain
legality was once missing". It is not that guard. Deleting the `chainLegality()`
call from `replay()` outright, in an isolated copy:

```
### MUTANT: chainLegality deleted (before this ticket's new control) ###
ℹ tests 20
ℹ pass 20
ℹ fail 0
```

Twenty of twenty still green with the legality rules removed. The altered
coordinate lands on a tile holding a different value, so the value check catches
it and the legality branch is never needed. No test in the file bound it.

A control that does bind it was added — appending a repeat of a chain's first
tile, which changes no coordinate and no value, so only the legality rules can
object. Same mutation, with it present:

```
### MUTANT: chainLegality deleted (with the new control) ###
✖ a chain that revisits a tile is refused as illegal, not merely as mis-scored (0.982833ms)
ℹ tests 21
ℹ pass 20
ℹ fail 1

  AssertionError [ERR_ASSERTION]: expected move 1 to be refused for revisiting a tile, got: ["move 1: chain scored 10560, recording claims 10240","move 2: tile (4,1) holds 256, recording claims 64", ...]
```

It is the only test that dies, and it dies naming the missing rule. Unmutated it
passes:

```
✔ a chain that revisits a tile is refused as illegal, not merely as mis-scored (0.348125ms)
```

### Criterion 5 — A batch-held candidate resolves, and an unknown identity still orphans. PASS (oracle_class: `deterministic`)

```
✔ a candidate held only inside a generated batch file is found by the index (0.844792ms)
✔ widening the index to batch files still leaves an unknown identity an orphan (0.415791ms)
✔ a batch entry without a candidate identity is skipped, not indexed as undefined (0.507708ms)
```

Both required cases are present and both were watched failing first, for their
stated reasons, before `indexBatches()` existed:

```
✖ a candidate held only inside a generated batch file is found by the index (3.102791ms)
  AssertionError [ERR_ASSERTION]: the batch-held candidate was not indexed; index holds
✖ widening the index to batch files still leaves an unknown identity an orphan (1.668083ms)
  AssertionError [ERR_ASSERTION]: precondition: this index really did read the batch file
```

The second case is the anti-vacuity one and it checks the unknown identity twice
— once against a staged index proven to have read a batch file, once against the
real `INDEX` over the whole `solver/` tree, so it cannot pass on a directory that
happens to hold little. The third case was mutation-checked too: deleting the
`typeof identity !== 'string' || !entry.candidate` guard makes it, and only it,
go red.

```
### MUTANT: malformed-entry guard removed ###
✖ a batch entry without a candidate identity is skipped, not indexed as undefined (1.584959ms)
```

### Criterion 6 — Nothing entered `solver/` top level. PASS (oracle_class: `deterministic`)

```
$ git status --porcelain solver/
 M solver/candidates-archive/README.md
 M solver/tests/gameLevels.test.js
 M solver/tests/receiptGate.test.js
 M solver/tests/recordingReplay.test.js
?? solver/candidates-archive/candidate-levels-51-split-channel-524f37c0.json
?? solver/candidates-archive/candidate-levels-51-split-channel-524f37c0.receipt.json
?? solver/candidates-archive/candidate-levels-54-tighter-pace-56c8eab0.json
?? solver/candidates-archive/candidate-levels-54-tighter-pace-56c8eab0.receipt.json
```

**All four additions are under `candidates-archive/`.** Nothing was added to
`solver/` top level, `solver/candidate-corpus.json` is untouched, and the receipt
gate's corpus is unchanged at three stores.

Of the four modifications, two are mine and two are not:

- `solver/tests/recordingReplay.test.js` — mine, the file the criterion allows.
- `solver/candidates-archive/README.md` — mine, inside `candidates-archive/` and
  explicitly required by the Fixed inputs section.
- `solver/tests/gameLevels.test.js`, `solver/tests/receiptGate.test.js` — **not
  mine.** They appeared in the tree during this ticket, from the concurrent agent
  shipping level 53 (ticket T-006). Their diffs are `LEVELS.length` 52 -> 53 and
  the level-53 exemption assertions; neither touches anything this ticket wrote.
  An earlier run of this same oracle, before those two arrived, listed only my
  two modifications.

### Criterion 7 — The receipt gate is unaffected; exactly one failure. PASS (oracle_class: `deterministic`)

In the live working tree, once the concurrent level-53 work settled:

```
$ node --test solver/tests/*.test.js
ℹ tests 196
ℹ suites 0
ℹ pass 195
ℹ fail 1
ℹ skipped 0
ℹ todo 0

✖ failing tests:
test at solver/tests/receiptGate.test.js:161:3
✖ candidate-levels-52.json has a receipt that verifies against the current bot (38.267125ms)
  AssertionError [ERR_ASSERTION]: candidate-levels-52.json: code/input identity mismatch
```

Exactly one failure, and it is the expected one. 196 tests against a 187-test
baseline: +5 named replay tests for the recordings that were orphans, and +4 new
crafted controls.

**This criterion was briefly unmeasurable and the intermediate reading is
recorded rather than dropped.** While the concurrent agent had shipped level 53
in `src/game.js` but had not yet updated its two fixtures, the same command
reported three failures — `gameLevels.test.js:5` ("53 !== 52") and
`receiptGate.test.js:510` ("level 53 does not ship either") alongside the
expected one. Both were isolated to that one file at the time, by running the
full suite against a copy of the tree whose only difference was `src/game.js`
restored to `HEAD`:

```
$ (isolated copy: this ticket's changes in full, src/game.js at HEAD)
$ node --test solver/tests/*.test.js
ℹ tests 196
ℹ pass 195
ℹ fail 1

✖ failing tests:
test at solver/tests/receiptGate.test.js:161:3
✖ candidate-levels-52.json has a receipt that verifies against the current bot (6.208083ms)
  AssertionError [ERR_ASSERTION]: candidate-levels-52.json: code/input identity mismatch
```

The isolated result and the settled live result agree: one failure, the known
one. The concurrent agent has since updated both fixtures, so no isolation is
needed to read this criterion now.

### Supporting check — the archived receipts really are stale

Run verbatim out of the README's own code block, from the repo root:

```
FAILS    candidate-levels-51-split-channel-524f37c0.json -> code/input identity mismatch
FAILS    candidate-levels-54-tighter-pace-56c8eab0.json -> code/input identity mismatch
FAILS    candidate-levels-gen0010.json -> code/input identity mismatch
FAILS    candidate-levels-gen0021.json -> code/input identity mismatch
```

Confirms the ticket's premise directly: adding either recovered store to
`solver/` top level would add a failing receipt-gate test for no gain.

## Feedback

- **F1 — Criterion 4 named a control that does not do the job it is named for.**
  The altered-coordinate case is cited as the guard on chain legality; it passes
  with `chainLegality()` deleted. A new control now binds it (Verification,
  criterion 4). Flagging rather than burying it, because the criterion was
  written on the belief the property was already guarded, and it was not.
- **F2 — `docs/CHECK-CARDS.md` now contradicts the code, in three places.** Out of
  this ticket's write scope, so untouched. The card
  `recordings-replay-to-their-claims` still says "Orphans — 5 of 8 recordings
  cannot be verified at all… the board is gone and no replay is possible" (line
  ~26 area), line ~38 counts the archive at 4 files (it is 8), and line ~384 says
  "At ship: 3 replayable, 5 orphans". Reality is 10 replayable, 0 orphans, and a
  blind spot in the checker rather than lost data. The same false claim is in
  commit `91321e4`'s body and in ticket `T-005`'s Result and Risks.
- **F3 — The archive README carried a wrong entry that plausibly caused the
  whole orphan story.** It claimed `gen-0017` from batch-02 was stored in the
  archive and bound to recording `44d3802d…`. The archive holds `gen-0021`, which
  no recording binds to; `gen-0017` was only ever in `generated-batch-02.json`.
  Anyone checking "is 44d3802d covered?" against that README got yes. Corrected in
  place.
- **F4 — Both archived `gen-*` stores are now redundant with the batch files.**
  `gen-0010` also lives in `generated-batch-01.json` and `gen-0021` in
  `generated-batch-02.json`, identical identities, so the widened index would
  resolve `d36e875d…` even if the archive copies vanished. Not acted on: this
  project's known failure mode is making checks pass by deleting their inputs,
  and deleting a store because "something else has a copy" is that move wearing a
  different hat.
- **F5 — A ticket that owns two files but is graded on the whole suite has no
  stated way to handle a concurrent edit elsewhere.** Criterion 7 was
  unmeasurable in the working tree until a tree copy was reconstructed by hand.
  Logged to friction as `contract-gap`.

## Risks

- **R1 — This ticket's evidence was gathered while another agent was editing the
  same test directory, and the tree moved twice under it.** `src/game.js` gained
  level 53 mid-ticket, then `solver/tests/gameLevels.test.js` and
  `solver/tests/receiptGate.test.js` gained matching fixtures after criterion 6
  had already been run once. Both criteria were re-run on the settled tree and
  both pass there, so nothing here rests on a stale reading — but the join should
  re-run `node --test solver/tests/*.test.js` itself rather than trusting the
  quoted tally, because a third concurrent edit would invalidate it the same way.
  The one thing that is stable regardless: `node --test
  solver/tests/recordingReplay.test.js` is 21/21 and depends only on files this
  ticket owns plus `recordings/`.
- **R2 — The index went from 5 identities to 84, and most of that is not
  human-vetted.** Every candidate any generation run ever produced is now
  resolvable, including ones that were generated, never selected, and never
  played. The ghost control proves an unknown identity still orphans, so the
  machinery is not vacuous — but the orphan check is now a weaker filter than it
  was: a recording bound to a discarded generated candidate resolves silently
  instead of being flagged for a human. That is the correct trade for `44d3802d`,
  whose board was genuinely in a batch file, and it should be understood as a
  trade rather than a free win.
- **R3 — `generated-batch*.json` files are now load-bearing for replay and
  nothing says so.** Deleting or rewriting `generated-batch-02.json` re-orphans
  `44d3802d…`, and with the ceiling at 0 that fails the suite loudly — which is
  the good outcome. The risk is the file being treated as disposable scratch
  output, since nothing in `solver/README.md` or the corpus manifest marks it as
  referenced. Unlike the corpus manifest, there is no signature required to
  remove one.
- **R4 — The ceiling at 0 will fire on the next authoring overwrite, which is the
  point, and will be inconvenient.** Re-authoring a level whose recording exists
  severs the binding and fails the suite immediately (exactly what T-002 did to
  level 54 today, silently). The recovery is a `git show` into
  `candidates-archive/`, as done here. Recorded so the response is recovery
  rather than raising the ceiling back.
- **R5 — `firstReplayable()` is `REPLAYABLE[0]`, whichever file sorts first.**
  Four crafted controls corrupt that recording. It is currently `1352aa7a…`, a
  level 53 win. A new recording whose filename sorts earlier silently becomes the
  substrate for every negative control. Pre-existing, unchanged by this ticket,
  and not a defect today — noted because the recording set grew from 8 to 10
  during this ticket alone.

## Join

Accepted 2026-08-21 by the orchestrator. All seven criteria PASS.

Re-verified independently at the join, not taken from the executor's tally as it
explicitly asked:
- `node --test solver/tests/*.test.js` -> 196 tests, 195 pass, **1 fail**
  (`candidate-levels-52.json`, the expected one).
- `node --test solver/tests/recordingReplay.test.js` -> **0** ORPHAN diagnostics;
  `ORPHAN_CEILING = 0` at line 39.
- **Mutation test of the executor's central claim.** Deleting the
  `chainLegality()` call from `replay()` leaves 20 of 21 tests green, and the
  only test that dies is the one this ticket added
  (`a chain that revisits a tile is refused as illegal, not merely as
  mis-scored`). The executor's finding is confirmed exactly: legality was
  unguarded, and the control named in criterion 4 was catching the value
  mismatch instead.

**On the executor's first open item — was adding that control in scope?** Yes.
Criterion 4 required the crafted controls to still fail *for their stated
reasons*. Discovering one that did not, and binding the property it was supposed
to bind, is the criterion being satisfied rather than exceeded. Keep it.

**This corrects a claim in T-005 and in commit `91321e4`.** Both say chain
legality exists "because a control was written to fail and did." The control did
fail — on the tile-value mismatch. The legality code it prompted was then held
in place by nothing. Half the story was right; the guarantee was not there.

Accepted trade, recorded rather than buried: widening `candidateIndex()` to read
`generated-batch*.json` took it from 5 known identities to **84**. Every
candidate any generation run ever produced now resolves, including ones never
selected or played. The ghost control proves the orphan machinery is not vacuous,
but the filter is genuinely weaker — a recording bound to a discarded generated
candidate now resolves silently instead of being flagged. Correct for freeing
`44d3802d`; still a trade.

Terminal status: **complete**.
