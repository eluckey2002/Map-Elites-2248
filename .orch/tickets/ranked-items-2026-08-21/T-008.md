---
id: T-008
run: ranked-items-2026-08-21
status: claimed
executor: orch-repair
pack: code
independence: checker
depends_on: [T-007]
write_scope:
  - docs/CHECK-CARDS.md
  - .orch/tickets/curve-debt-2026-08-21/T-005.md
excluded_actions:
  - modifying any source file, test, or candidate store
  - rewriting T-005's Verification section, which records observed output
  - amending or rewriting git history
bound: 1 iteration
claimed_by: agent-T-008
claimed_at: 2026-08-21T12:55:27-05:00
---

## Objective

No committed artifact still claims that five recordings can never be verified.
The check card and ticket T-005 state the true position: the orphans were
recoverable, what recovery achieved, and what remains genuinely uncheckable.

## Fixed inputs

- Evidence: `.orch/audits/orphaned-recordings-2026-08-21/findings.md`.
- **This ticket is a correction of my own false claim, not a discovery.** Three
  artifacts assert that five playthroughs are permanently unverifiable:
  1. `docs/CHECK-CARDS.md`, card `recordings-replay-to-their-claims`, blind spot
     1: "Orphans — 5 of 8 recordings cannot be verified at all… the board is gone
     and no replay is possible."
  2. Commit `91321e4` body: "Five recordings can never be verified — their
     candidate store is gone, so the board is gone."
  3. `.orch/tickets/curve-debt-2026-08-21/T-005.md`, Result and Risks:
     "5 orphans… can never be verified"; "Five human playthroughs are
     permanently unverifiable, including the only recorded loss."
- The truth: four were recoverable from git and one was never lost — it sat in
  `solver/generated-batch-02.json` at HEAD, invisible to an index that only read
  `candidate-levels*.json`. All five replay once their candidate is supplied.
- **Read T-007's completed ticket for the actual post-recovery state** — orphan
  count, new ceiling, and which recordings resolve. Do not predict it; read it.
- Post-recovery state, verified at T-007's join: **0 orphans, 10 replayable,
  `ORPHAN_CEILING = 0`.** Two stores recovered into `solver/candidates-archive/`
  (level 51 `524f37c0`, level 54 `56c8eab0`); the fifth needed no file — it
  resolves to `gen-0017` inside `solver/generated-batch-02.json` at HEAD.
- **Two further card corrections T-007 surfaced, both in your write scope:**
  1. The card's archive file count says 4; there are now 8. Its
     `At ship: 3 replayable, 5 orphans` is stale.
  2. The card and T-005 both credit chain legality to a control that "was written
     to fail and did". Verified false at T-007's join by mutation test: deleting
     `chainLegality()` left every test green except the one T-007 added. The
     control caught a value mismatch, never the illegality. Correct this rather
     than leaving a flattering story in place.
- **A NEW blind spot that must be added, not just corrections made:** widening
  the index took it from 5 identities to 84. Every candidate any generation run
  ever produced now resolves, including discarded ones. A recording bound to a
  never-selected candidate now resolves silently instead of being flagged. Also:
  `generated-batch*.json` files are now load-bearing for replay and, unlike the
  corpus manifest, nothing requires a signature to delete one.
- Commit `91321e4` is history and is NOT to be amended. The correction lands in
  the working tree and in a new commit that names it, the same way `98e0384`
  corrected `c70f5fa` earlier today.

## Completion test

All oracles `pre-existing`.

1. **No artifact claims the orphans are unverifiable.**
   oracle: `grep -rniE "can never be verified|cannot be verified at all|permanently unverifiable" docs/ .orch/tickets/`
   exits non-zero (no matches).
   oracle_class: `deterministic`
2. **The card's blind spot 1 states the true position** — that the orphans were
   recoverable, by what route, and what the count is now.
   oracle: grep the card for `recoverable`, `git`, and `generated-batch`; all
   present within the `recordings-replay-to-their-claims` card.
   oracle_class: `deterministic`
3. **The card still enumerates real blind spots.** `Does NOT catch` must not
   shrink to nothing: it retains at minimum the missing author/timestamp, the
   shared-rule-bug case, the unpinned recording set, and the unauthenticated
   POST endpoint. A card whose blind-spot list empties out is a card that stopped
   being useful.
   oracle: count the enumerated entries under `Does NOT catch` in that card;
   assert >= 4. oracle_class: `deterministic`
4. **T-005 records the correction without erasing what it observed.** Its
   Verification section is unchanged; Result and Risks are corrected; a Feedback
   entry names the false claim and how it was caught.
   oracle: `git diff .orch/tickets/curve-debt-2026-08-21/T-005.md` shows no
   change between `## Verification` and `## Feedback`.
   oracle_class: `deterministic`
5. **The suite is untouched.** `node --test solver/tests/*.test.js` reports the
   same counts as before this ticket ran.
   oracle_class: `deterministic`
6. **Nothing outside the write scope is modified.**
   oracle: `git status --porcelain -- docs/CHECK-CARDS.md .orch/tickets/curve-debt-2026-08-21/T-005.md`
   lists exactly those two as modified.
   **Scope this to the write scope, do not run it bare.** Concurrent lanes share
   this worktree, so a bare `git status` reports other agents' writes and will
   fail for reasons that are not yours. This wording is a repair of the same
   oracle in T-009, which failed as written for exactly that reason.
   oracle_class: `deterministic`

## Return fields

- the corrected blind spot 1 text
- the surviving blind-spot count
- per-criterion verdict with quoted oracle output

## Result

Two files corrected, both in scope: `docs/CHECK-CARDS.md` and
`.orch/tickets/curve-debt-2026-08-21/T-005.md`. No source file, test, or
candidate store was touched; no commit was made; git history is untouched.

**The correction, stated plainly: I wrote a false claim.** Three artifacts said
five recorded playthroughs were beyond checking because their boards had been
destroyed. That was false on the day it was written, not overtaken by events.
Four of the five stores were one `git show` away, and the fifth was never
missing at all -- it sits in `solver/generated-batch-02.json` at HEAD as
`gen-0017` and was reported as a destroyed board only because
`candidateIndex()` read `candidate-levels*.json` and nothing else. The corrected
text says so in those words, in both files.

### The corrected blind spot 1, verbatim

```
  1. **An orphan report does not tell you the board is lost.** All the check
     knows is that a recording's identity is missing from *its index*. Destroyed,
     sitting in git history, or sitting in a file the index does not read -- it
     cannot tell those apart, and it prints "exists nowhere on disk" for all
     three. **This card asserted the strong reading, and I was wrong to.** It
     said 5 of 8 recordings were beyond checking because their boards were gone.
     That claim was false the day I wrote it; nothing about the situation
     changed underneath it. Every one of the five was recoverable, and all five
     were recovered on 2026-08-21: level 51's store came back from
     `git show 1468392^:solver/candidate-levels.json` and level 54's from
     `git show 0965038^:solver/candidate-levels-54.json` (both now in
     `solver/candidates-archive/`, with their receipts), and the fifth was never
     lost at all -- it sits in `solver/generated-batch-02.json` at HEAD as
     `gen-0017`, invisible only because the index read `candidate-levels*.json`
     and nothing else. The count is now 10 replayable, 0 orphans. **The blind
     spot that produced the false claim is untouched:** nothing makes this check
     look in git history, or in an unindexed file, before it reports a board as
     gone -- and nothing stops the next person believing the message the way I
     did.
```

(The em dashes are real em dashes in the file; rendered as `--` here only to keep
this block quotable.)

### Surviving blind-spot count: 8

Was 6. The list grew, it did not shrink to a victory lap.

| # | blind spot | status |
|---|---|---|
| 1 | An orphan report does not tell you the board is lost | **rewritten** -- keeps the real residual gap (the check never looks in git or in an unindexed file before saying "gone") |
| 2 | Who played, or when -- no author, no timestamp | unchanged (criterion 3 requires it) |
| 3 | A rule bug present identically in `src/game.js` and `solver/engine.js` | unchanged (criterion 3 requires it) |
| 4 | Whether a playthrough is representative | unchanged |
| 5 | A recording deleted outright -- nothing pins the expected set | unchanged (criterion 3 requires it) |
| 6 | A recording POSTed directly to the unauthenticated endpoint | unchanged (criterion 3 requires it) |
| 7 | **A recording bound to a candidate nobody ever chose** | **new** -- the index went 5 -> 84 identities; 16 of them carry `verdict.pass: false` from the generator that produced them |
| 8 | **A `generated-batch*.json` file deleted or rewritten** | **new** -- four files now load-bearing for replay; unlike the store corpus pinned by `solver/candidate-corpus.json`, no manifest and no signature guards them |

### Other corrections made, all named in Fixed inputs

- **Archive counts, card 1 (`receipt-verifies-against-current-code`).**
  "Excludes 14 identically-named files ... `solver/candidates-archive/` (4)"
  -> **18** and **8**; and its Sampling-memory line's "the 2 archived
  candidates" -> **4**. Counted, not assumed: `ls solver/candidates-archive/ |
  grep -c '^candidate-levels'` is 8 files = 4 stores + 4 receipts.
- **`Scope` line of the recordings card.** "At ship: 3 replayable, 5 orphans"
  now reads **10 of 10 replayable, 0 orphans (2026-08-21)**, with the old
  reading kept and explained rather than deleted, and the index's two file
  shapes (`candidate-levels*.json` stores plus `generated-batch*.json` `results`
  entries) named.
- **`Enforcement` line.** Ratchet "at 5" -> lowered **5 -> 0**, plus the
  standing instruction that the response to a new orphan is a `git show` into
  `candidates-archive/`, never a raised ceiling.
- **Chain legality, corrected in both files.** The card said "`chainLegality()`
  exists because a control caught its absence" and T-005's Feedback said it
  "exists only because the control was written to fail." Half true, and the
  false half was load-bearing: the control did fail and did prompt writing the
  code, but it never *held* it. Verified here by re-running the mutation in an
  isolated copy, not taken from T-007: delete the `chainLegality()` loop from
  `replay()` and 20 of 21 tests stay green -- including
  `a recording whose moves were altered is refused`, the case that was credited.
  It is refused by the tile-value check and never reaches the legality branch.
  From T-005 shipping until 2026-08-21, `chainLegality()` was code no test bound.
- **A `Corrected:` line added to the recordings card**, naming this ticket,
  T-007, the audit, and recording that commit `91321e4` carries the false claim
  in its body and stays as written.

### T-005 specifically

- `## Result` -- the "what it does not catch" summary line corrected, and a
  dated **Correction** block appended naming the false claim as my error, the
  three recovery routes with their exact commands, and the post-recovery state.
  The three per-recording verdicts T-005 itself observed are left exactly as
  recorded.
- `## Verification` -- **byte-identical to HEAD**, proven by SHA-256 below.
- `## Feedback` -- the chain-legality bullet corrected; two entries added: one
  naming the false claim and how it was caught, one recording that T-005's own
  Completion test criterion 8 still demands the false blind spot and is
  deliberately left alone (see criterion 1 below and R1).
- `## Risks` -- the "nothing here recovers them" entry corrected, and the real
  residual risk the recovery *created* (index 5 -> 84, batch files load-bearing)
  put in its place, so the section does not simply get happier.

## Verification

Every oracle below was run exactly as the Completion test writes it, from the
repo root. Output is quoted, never paraphrased.

**Baseline, taken before any edit:** `node --test solver/tests/*.test.js` ->
`tests 196 / pass 195 / fail 1`; `git status --porcelain` listed 7 modified
files, none of them mine (T-002.md, T-003.md, `solver/candidates-archive/README.md`,
`gameLevels.test.js`, `receiptGate.test.js`, `recordingReplay.test.js`,
`src/game.js`) -- concurrent lanes share this worktree.

### Criterion 1 -- No artifact claims the orphans are unverifiable. **FAIL as written** (oracle_class: `deterministic`)

The oracle requires a non-zero exit. It exits 0, and it cannot do otherwise.

```
$ grep -rniE "can never be verified|cannot be verified at all|permanently unverifiable" docs/ .orch/tickets/ >/dev/null 2>&1
$ echo "exit=$?"
exit=0
```

I am reporting this red rather than rewriting it green. The full census of the
15 surviving matches, every one classified:

```
$ grep -rniE "can never be verified|cannot be verified at all|permanently unverifiable" docs/ .orch/tickets/
.orch/tickets/ranked-items-2026-08-21/T-008.md:23:No committed artifact still claims that five recordings can never be verified.
.orch/tickets/ranked-items-2026-08-21/T-008.md:31:  artifacts assert that five playthroughs are permanently unverifiable:
.orch/tickets/ranked-items-2026-08-21/T-008.md:33:     1: "Orphans - 5 of 8 recordings cannot be verified at all... the board is gone
.orch/tickets/ranked-items-2026-08-21/T-008.md:35:  2. Commit `91321e4` body: "Five recordings can never be verified - their
.orch/tickets/ranked-items-2026-08-21/T-008.md:38:     "5 orphans... can never be verified"; "Five human playthroughs are
.orch/tickets/ranked-items-2026-08-21/T-008.md:39:     permanently unverifiable, including the only recorded loss."
.orch/tickets/ranked-items-2026-08-21/T-008.md:72:   oracle: `grep -rniE "can never be verified|cannot be verified at all|permanently unverifiable" docs/ .orch/tickets/`
.orch/tickets/ranked-items-2026-08-21/T-009.md:274:  assert the five recordings "can never be verified". This ticket corrected T-002
.orch/tickets/ranked-items-2026-08-21/T-007.md:208:i ORPHAN 1c87356748ee (level 51, win) wants candidate 524f37c0063d, which exists nowhere on disk - this playthrough can never be verified
.orch/tickets/ranked-items-2026-08-21/T-007.md:209:i ORPHAN 3d3ba1f05b08 (level 54, win) wants candidate 56c8eab00e4a, which exists nowhere on disk - this playthrough can never be verified
.orch/tickets/ranked-items-2026-08-21/T-007.md:210:i ORPHAN 44d3802d5512 (level 52, win) wants candidate 5b12806fa862, which exists nowhere on disk - this playthrough can never be verified
.orch/tickets/ranked-items-2026-08-21/T-007.md:211:i ORPHAN 78749fc07834 (level 51, win) wants candidate 524f37c0063d, which exists nowhere on disk - this playthrough can never be verified
.orch/tickets/ranked-items-2026-08-21/T-007.md:212:i ORPHAN 8ac6c9d4c533 (level 51, lose) wants candidate 524f37c0063d, which exists nowhere on disk - this playthrough can never be verified
.orch/tickets/ranked-items-2026-08-21/T-007.md:447:  cannot be verified at all... the board is gone and no replay is possible" (line
.orch/tickets/curve-debt-2026-08-21/T-005.md:107:   cannot be verified at all, and a recording proves a playthrough happened but
```

(The `i` prefixes and `-` dashes above are the `info` glyph and em dashes in the
real output, transliterated here so this block does not itself add matches.)

Classification:

| lines | file | in write scope? | disposition |
|---|---|---|---|
| 23, 31, 33, 35, 38, 39, 72 (7) | `T-008.md` -- **this ticket** | no (it is the ticket, not the artifact) | Unfixable by construction. Lines 31-39 *are* the ticket quoting the false claims so they can be corrected; line 72 *is* the oracle's own regex; line 23 is the Objective. An oracle whose corpus contains the ticket that states it can never pass. Writing this Verification section adds more matches still. |
| 208-212, 447 (6) | `T-007.md` -- complete, another lane | no | Historical evidence. 208-212 are the pre-recovery ORPHAN diagnostics quoted verbatim as proof of the before-state; 447 is T-007's Feedback quoting the card text it flagged. Deleting either destroys the evidence that the correction rests on. |
| 274 (1) | `T-009.md` -- another lane | no | Another lane's ticket, quoting the same false claim in order to correct it elsewhere. |
| 107 (1) | `T-005.md` -- **in my write scope** | yes, but deliberately not edited | It is inside `## Completion test` (lines 60-110), the **frozen acceptance spec**: criterion 8 requires the card to name that blind spot. See below. |

**Why line 107 was left.** Editing a completed ticket's Completion test to match
a later reality is retro-fitting the spec to the outcome -- the same move as
amending a commit to hide what it said, which this ticket forbids. Worse, T-005's
`## Verification` (which criterion 4 protects and I may not touch) records
criterion 8 as PASS; rewriting the criterion would leave a PASS verdict pointing
at a criterion that never existed when it was rendered. The correct disposition
is to leave the spec, break it on purpose, and say so -- which T-005's Feedback
now does explicitly.

**What the criterion's intent actually achieved,** measured on the corpus I was
licensed to change:

```
$ grep -rniE "can never be verified|cannot be verified at all|permanently unverifiable" docs/
$ echo "exit_docs=$?"
exit_docs=1
```

`docs/` -- the whole tree, not just my file -- is clean, exit non-zero. Every
narrative assertion in both write-scope files is corrected. The single residue
inside my scope is the frozen spec line, above.

### Criterion 2 -- Blind spot 1 states the true position. **PASS** (oracle_class: `deterministic`)

Card extracted with
`awk '/^### recordings-replay-to-their-claims/{f=1} f&&/^### /&&!/recordings-replay-to-their-claims/{f=0} f' docs/CHECK-CARDS.md`
(129 lines), then blind spot 1 extracted from it (19 lines).

```
$ for term in recoverable git generated-batch; do printf '%-18s ' "$term:"; grep -c -- "$term" bs1.md; done
recoverable:       1
git:               4
generated-batch:   1
```

All three present, inside blind spot 1 of that card specifically -- not merely
somewhere in the file. The matching lines:

```
$ grep -n -E "recoverable|git |generated-batch" bs1.md
3:     sitting in git history, or sitting in a file the index does not read - it
8:     changed underneath it. Every one of the five was recoverable, and all five
10:     `git show 1468392^:solver/candidate-levels.json` and level 54's from
11:     `git show 0965038^:solver/candidate-levels-54.json` (both now in
13:     lost at all - it sits in `solver/generated-batch-02.json` at HEAD as
17:     look in git history, or in an unindexed file, before it reports a board as
```

Route (`git show`, two commits named), mechanism (`generated-batch-02.json`), and
count (10 replayable, 0 orphans, in the text at line 14-15) are all stated.

### Criterion 3 -- The card still enumerates real blind spots, >= 4. **PASS** (oracle_class: `deterministic`)

```
$ awk '/^- \*\*Does NOT catch:/{f=1;next} f&&/^- \*\*/{f=0} f&&/^  [0-9]+\. /{n++} END{print n}' card.md
8
```

```
$ awk '/^- \*\*Does NOT catch:/{f=1;next} f&&/^- \*\*/{f=0} f&&/^  [0-9]+\. /{print}' card.md   # headlines only
  1. **An orphan report does not tell you the board is lost.**
  2. **Who played, or when.**
  3. **A rule bug present identically in both implementations.**
  4. **Whether a playthrough is representative.**
  5. **A recording deleted outright.**
  6. **A recording POSTed directly to the server**
  7. **A recording bound to a candidate nobody ever chose.**
  8. **A `generated-batch*.json` file deleted or rewritten.**
```

8 >= 4. All four the criterion names by hand are present and unedited: the
missing author/timestamp (2), the shared-rule-bug case (3), the unpinned
recording set (5), the unauthenticated POST endpoint (6). Two were added (7, 8),
so the list is longer after the fix than before it.

The two new entries are measured, not asserted. Index size, computed by
replaying `indexStores`/`indexBatches` against the tree read-only:

```
stores-only identities (the old index): 7
stores + batches identities (the widened index): 84
generated-batch files: generated-batch-01.json, generated-batch-02.json, generated-batch-03.json, generated-batch-04.json
```

```
indexable batch entries: 79 | verdict.pass true: 63 | verdict.pass false: 16
```

7 is the stores-only count *after* T-007 archived two recovered stores; it was 5
before, which is the figure the card and T-007 use. 84 is the widened total, and
16 of the newly-resolvable identities carry a failing verdict from the run that
generated them.

### Criterion 4 -- T-005 records the correction without erasing what it observed. **PASS** (oracle_class: `deterministic`)

`## Verification` is byte-identical to HEAD. Hunk positions first
(`## Verification` is line 140 and `## Feedback` line 166 at HEAD, so the
protected region is old lines 141-165):

```
$ git diff -U0 .orch/tickets/curve-debt-2026-08-21/T-005.md | grep -E "^@@"
@@ -133,2 +133,3 @@ the third is the project's only recorded loss); `3d3ba1f0` (level 54, wanting
@@ -139,0 +141,27 @@ deletes its check.
@@ -168,7 +196,43 @@ Overall: PASS. Weakest oracle_class: `deterministic`. All oracles `pre-existing`
@@ -186,2 +250,18 @@ Overall: PASS. Weakest oracle_class: `deterministic`. All oracles `pre-existing`
```

Old lines 133 and 139 are in `## Result`; 168 is in `## Feedback`; 186 is in
`## Risks`. Nothing lands in 141-165. Hunk arithmetic is not proof on its own, so
the section was extracted from both revisions and compared directly:

```
$ diff ver_head.md ver_now.md
$ echo "diff_exit=$?"
diff_exit=0

$ shasum -a 256 ver_head.md ver_now.md
a3a8590671a20cbc0674964ea31aa42b9135df2f4a8c5a558488f78fff9c4d0e  ver_head.md
a3a8590671a20cbc0674964ea31aa42b9135df2f4a8c5a558488f78fff9c4d0e  ver_now.md
```

Identical hashes, 26 lines each. Result and Risks are corrected (diffstat: 102
lines changed in T-005). The required Feedback entry is present:

```
$ grep -n "I wrote a claim into this ticket" .orch/tickets/curve-debt-2026-08-21/T-005.md
217:- **I wrote a claim into this ticket, into `docs/CHECK-CARDS.md`, and into commit
$ grep -n "git log --all" .orch/tickets/curve-debt-2026-08-21/T-005.md
226:  recoverable?" and spending `git log --all -S<identity>` on each one
```

It names the false claim, names why none of T-005's eight oracles could catch it
(all eight measured the check's behaviour, none measured whether the story about
the orphans was true), and names what did catch it -- one `git log --all -S` per
orphaned identity, in the audit.

### Criterion 5 -- The suite is untouched. **PASS** (oracle_class: `deterministic`)

```
$ node --test solver/tests/*.test.js
i tests 196
i suites 0
i pass 195
i fail 1
i skipped 0
i todo 0
```

```
$ node --test solver/tests/*.test.js 2>&1 | grep "^X candidate-levels"
X candidate-levels-52.json has a receipt that verifies against the current bot (3.892583ms)
```

Identical to the baseline taken before any edit: 196 / 195 / 1, the sole failure
being the known and decided `candidate-levels-52.json`. Expected -- this ticket
changed two markdown files and nothing else. Re-confirmed on every pass: before
any edit, after the edits, after writing this ticket, and on a final sweep --
the same 196 / 195 / 1 each time.

**One instrument trap hit while running it, recorded rather than dropped.** The
third invocation of the tally grep returned *zero lines* with the tree unchanged.
Nothing had broken: node emitted ANSI colour codes on that run, so the tally
lines no longer began with the info glyph the pattern anchored on. An empty
result and "the suite reports nothing" are indistinguishable to that command.
Re-run with `sed 's/\x1b\[[0-9;]*m//g'` inserted before the grep, the tally is
196 / 195 / 1 as above. Any oracle in this repo that scrapes node's tally lines
should strip ANSI first; logged to friction as `surprising-output`.

### Criterion 6 -- Nothing outside the write scope is modified. **PASS** (oracle_class: `deterministic`)

Run scoped, as the criterion instructs, not bare:

```
$ git status --porcelain -- docs/CHECK-CARDS.md .orch/tickets/curve-debt-2026-08-21/T-005.md
 M .orch/tickets/curve-debt-2026-08-21/T-005.md
 M docs/CHECK-CARDS.md
```

Exactly the two write-scope files, both modified, neither deleted or renamed.

The scoped oracle proves those two are dirty; it cannot prove nothing else is. So
the bare status is recorded too, and diffed against the baseline taken before any
edit:

```
$ git status --porcelain
 M .orch/tickets/curve-debt-2026-08-21/T-002.md      <- pre-existing, another lane
 M .orch/tickets/curve-debt-2026-08-21/T-003.md      <- pre-existing, another lane
 M .orch/tickets/curve-debt-2026-08-21/T-005.md      <- MINE
 M docs/CHECK-CARDS.md                               <- MINE
 M solver/candidates-archive/README.md               <- pre-existing (T-007)
 M solver/tests/gameLevels.test.js                   <- pre-existing (T-006)
 M solver/tests/receiptGate.test.js                  <- pre-existing (T-006)
 M solver/tests/recordingReplay.test.js              <- pre-existing (T-007)
 M src/game.js                                       <- pre-existing (T-006)
?? .orch/audits/orphaned-recordings-2026-08-21/
?? .orch/runs/ranked-items-2026-08-21/
?? .orch/tickets/ranked-items-2026-08-21/
?? "2026-08-21-124155--2248 craziness.txt"
?? recordings/1352aa7a02cd....json
?? recordings/7ad0978f9619....json
?? solver/candidates-archive/candidate-levels-51-split-channel-524f37c0.json
?? solver/candidates-archive/candidate-levels-51-split-channel-524f37c0.receipt.json
?? solver/candidates-archive/candidate-levels-54-tighter-pace-56c8eab0.json
?? solver/candidates-archive/candidate-levels-54-tighter-pace-56c8eab0.receipt.json
```

The modified set grew by exactly two entries against the baseline, and both are
mine. `git log -1 --format=%h` is still `91321e4`: no commit was made, no history
amended.

```
$ git diff --stat -- docs/CHECK-CARDS.md .orch/tickets/curve-debt-2026-08-21/T-005.md
 .orch/tickets/curve-debt-2026-08-21/T-005.md | 102 ++++++++++++++++++++++++---
 docs/CHECK-CARDS.md                          |  93 ++++++++++++++++++++----
 2 files changed, 169 insertions(+), 26 deletions(-)
```

### Supporting check -- the chain-legality correction, re-measured here

Not taken from T-007. `solver/` and `recordings/` were copied to a scratch tree
outside the repo, and the `chainLegality()` loop deleted from `replay()` there.
Nothing in the repo was modified.

```
### control: unmutated copy ###
i tests 21
i pass 21
i fail 0

### MUTANT: chainLegality loop deleted from replay() ###
X a chain that revisits a tile is refused as illegal, not merely as mis-scored (0.487166ms)
i tests 21
i pass 20
i fail 1
```

And the case the card and T-005 credited, under the same mutation:

```
$ node --test solver/tests/recordingReplay.test.js 2>&1 | grep -E "altered|revisits"
/ a recording whose moves were altered is refused (0.132459ms)
X a chain that revisits a tile is refused as illegal, not merely as mis-scored (0.477334ms)
```

The altered-coordinate control passes green with chain legality deleted. It never
guarded it. Confirmed independently, so the corrected text in both files rests on
a measurement taken in this ticket.

---

**Overall: 5 PASS, 1 FAIL (criterion 1, on an oracle that cannot pass -- see its
entry and R1).** Weakest oracle_class: `deterministic`. All oracles
`pre-existing`.

## Feedback

- **F1 -- Criterion 1's oracle cannot pass, and no amount of correct work makes
  it.** It greps `.orch/tickets/` for the three phrases and requires no matches,
  but that corpus contains this ticket -- which quotes all three phrases in its
  Fixed inputs *in order to name what must be corrected*, and prints the regex
  itself at line 72. It also contains two other lanes' tickets and one completed
  ticket whose Verification quotes the pre-recovery ORPHAN diagnostics as
  evidence. 15 matches survive; 1 is in my write scope, 0 are in `docs/`. The
  fix, for the next oracle of this shape: scope it to the artifacts being
  corrected (`grep -rniE ... docs/ <the specific files>`) or to tracked committed
  content (`git grep`), never to a directory that includes the instruction
  document. Logged to friction as `contract-gap`.

- **F2 -- A blind-spot list is the one place a "fix" should be able to make
  things worse, and this ticket is the proof.** Correcting the false claim
  removed one item; the recovery that made the correction possible added two
  (the index resolving 84 identities instead of 5; four `generated-batch*.json`
  files becoming load-bearing with no manifest and no signature guarding them).
  Net 6 -> 8. Had criterion 3 not been written with a floor, the honest outcome
  and the flattering one would have been indistinguishable from the oracle.

- **F3 -- The false claim was self-sealing, and that is the generalisable part.**
  T-005 shipped eight oracles and all eight passed. Every one of them measured
  whether the *check* named, counted, and ratcheted its orphans. None measured
  whether the *explanation* was true -- and the explanation was baked into the
  diagnostic string the check printed, so the check asserted the falsehood on
  every run and its own tests confirmed it was asserting it. A claim that gets
  emitted by the artifact under test is not evidence for itself. The rule that
  would have caught it: before writing "X does not exist", run the command that
  would find X. Five `git log --all -S<identity>` invocations.

- **F4 -- The chain-legality story was half true, which is why it survived.**
  "A control was written to fail and did" is accurate about the past. "Therefore
  the property is guarded" is what everyone read, and it was false for as long
  as the check existed. Both files now separate the two claims explicitly. Worth
  generalising into `gate-check`: a crafted-bypass note should record which test
  *currently binds* the property, verified by mutation, not which test
  historically prompted the code.

- **F5 -- T-005's frozen Completion test now contradicts the card, on purpose.**
  Criterion 8 requires the card's `Does NOT catch` to name that orphans cannot be
  checked; the card no longer does. I left the spec alone and recorded the
  breach in T-005's Feedback rather than retro-fitting it. There is no
  established convention here for "a completed ticket's acceptance criterion is
  now known to be wrong", and the two available moves -- edit the spec, or leave
  a knowingly-unsatisfiable spec in place -- are both bad. Flagging for a rule.

## Risks

- **R1 -- Criterion 1 is red and I did not make it green.** One match remains
  inside my own write scope: `.orch/tickets/curve-debt-2026-08-21/T-005.md:107`,
  in the frozen `## Completion test`. I could have deleted it and reported six
  green criteria. I judged that worse: it rewrites a completed ticket's
  acceptance spec to match an outcome it predates, and it would leave T-005's
  protected `## Verification` recording a PASS against a criterion that no longer
  exists. **If the join disagrees, this is a one-line edit** -- change T-005's
  criterion 8 to name the corrected blind spot -- and criterion 1 would still
  fail on the other 14 matches, which are in other lanes' tickets and out of any
  reach I have. My recommendation: accept criterion 1 as FAIL-by-oracle-defect
  with the census above as the evidence, and repair the oracle's wording for the
  next ticket rather than the artifacts for this one.

- **R2 -- The correction is in the working tree and not committed.** Per the
  ticket I set no terminal status and made no commit. Until someone commits it,
  the repository's committed state still carries the false claim in
  `docs/CHECK-CARDS.md` and in `91321e4`'s body, and the objective ("no committed
  artifact still claims...") is not yet met by anything I did. The commit that
  lands this must name the correction the way `98e0384` named `c70f5fa`.

- **R3 -- The card now asserts numbers that drift on their own.** "10 of 10
  replayable", "84 identities", "16 with `verdict.pass: false`", "8 archived
  files", "four batch files" are all true as of 2026-08-21 and all move without
  anyone touching the card -- the owner played two more games during T-007 alone,
  taking recordings 8 -> 10. No check ties any of them to the card. This is the
  same class of defect I am correcting: a number that was true once. The
  durable claims (an orphan report cannot distinguish destroyed from unindexed;
  batch files are unguarded) do not drift; the counts do.

- **R4 -- Blind spot 8 describes a hole nobody closed.** Deleting a
  `generated-batch*.json` re-orphans `44d3802d` and turns the suite red, which is
  only useful if the red is read as a lost board rather than as noise from
  deleted scratch output. Writing it on the card is documentation, not a guard.
  The actual guard would be extending `solver/candidate-corpus.json`, or its
  equivalent, to pin the batch files -- out of this ticket's scope and out of
  T-007's, so it is currently owned by nobody.

- **R5 -- My mutation re-test ran against a copy of a tree three other lanes were
  editing.** The copy was taken at 12:5x with T-006's level-53 work already
  settled, and `recordingReplay.test.js` depends only on `solver/` and
  `recordings/`, neither of which moved during this ticket. The control run
  (21/21 green on the unmutated copy) establishes the copy was sound before
  mutating. Still: if the join wants the chain-legality claim held to the same
  standard as the rest, re-run the mutation itself rather than trusting my quote.
