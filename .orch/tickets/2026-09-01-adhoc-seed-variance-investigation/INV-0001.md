---
id: INV-0001
run: 2026-09-01-adhoc-seed-variance-investigation
status: complete
executor: orch-investigate
write_scope:
  - .orch/tickets/2026-09-01-adhoc-seed-variance-investigation/INV-0001.md
bound: current tracked checkout, the introducing commit and its parent, and the named root-checkout audit snapshot as a lead only
---

# INV-0001 — Seed-variance subject and consumer

## Question

What exact current repository artifacts can reconstruct the `r = 0.98`
seed-variance claim, quantify within-level versus between-candidate variation,
and make a downstream selection consume the resulting verdict?

## Source policy

- Primary: tracked repository state at
  `4dd93219f69d5288654dd2aee395f6e6388bda4a` and Git history containing
  `c782111d46842e56f2fd10b273e1ff0a53b30ff0`.
- Lead only: the concurrent root checkout's untracked
  `claim-evidence-verdict/` snapshot. It concerns `RESULT-0020`, not the
  seed-variance claim, and supplies no evidence for `r = 0.98`.
- Excluded: web sources, other repositories, and unstamped claims from chat.

## Result

**Status:** complete

**Result identity:** this evidence packet, hash-pinned at integration.

**Verification:** `UNVERIFIED`; the dispatch named no independent oracle.

## Cited findings

1. **High confidence — the historical sentence has provenance but no
   measurement artifact.** `git blame -L 210,223 HANDOFF.md` assigns the claim
   to `c782111d`; `git show --stat c782111d` shows that the commit changed only
   `HANDOFF.md`. The prose names two disjoint 60-game samples but no seed
   ranges, statistic implementation, command, output, or receipt
   (`HANDOFF.md:210-223`).
2. **High confidence — the historical executable subject can be named.** The
   introducing commit and its parent have identical SHA-256 values for
   `src/game.js`, `solver/bot.js`, and `solver/engine.js`. A reconstruction can
   therefore pin the historical level population and evaluator code, but it
   cannot pretend to reproduce an absent original artifact.
3. **High confidence — the real current measurement seam exists.**
   `solver/level-author.js:113-151` exports `playMeasured`, which calls the real
   engine and `chooseMove`; setting `target: Infinity` makes it measure terminal
   achievable score rather than stop on a shipped target.
4. **High confidence — the live downstream selection seam exists but does not
   consume seed-variance entitlement.** `solver/generate-levels.js:180-184`
   defines `rankShortlist`; `main` calls it at lines 271-321 after the 450-game
   authoring pipeline. The inputs contain gate verdicts, but no identity-bound
   seed-variance verdict.
5. **High confidence — current helper tests do not prove the CLI consumes a
   seed-variance result.** `solver/tests/generateLevels.test.js:118-138` calls
   `rankShortlist` directly with hand-built entries. No current test drives
   `main` or a production selection seam with a valid and broken entitlement.

## Contradictions

- `HANDOFF.md:217-219` calls structural ranking reliable at 60 games, while
  the evidence ledger contains no accepted record for that result and the
  introducing commit contains no measurement artifact. The prose is a lead,
  not evidence.

## Dead ends

- Git history contains no tracked `.base-scores.json` or equivalent receipt for
  the claim.
- Memory and rollout-summary searches for the exact sentence and commit did
  not locate the original command or output.
- The concurrent `claim-evidence-verdict/` snapshot audits `RESULT-0020`; it
  does not inspect or reconstruct seed variance.

## Gaps left uncovered

- The original seed ranges and exact Pearson implementation are unknown.
- No current artifact quantifies within-level and between-level variance on one
  scale.
- No production selector currently fails closed on an absent or stale
  seed-variance entitlement.
