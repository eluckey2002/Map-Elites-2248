---
id: PG-001
run: 2026-09-05-policy-grounding
status: complete
executor: orch-investigate
depends_on: []
write_scope:
  - .orch/runs/2026-09-05-policy-grounding/
bound: 30 minutes of active work; exact-corpus diagnostics only, no parameter search
claimed_by: /root
claimed_at: 2026-09-05T23:45:39Z
---

## Objective

Determine what the current source and recorded sessions establish about the
fairness of the human benchmark, the proposed policy-vocabulary gaps, and the
way alternative fitness objectives rank the same examples. Leave the fitness
choice with the owner and change no game, policy, test, receipt, or ledger.

## Fixed inputs

- Source revision: 07a68c0ebd15763207134690acd1d4f7f061465f; game/solver/corpus
  identical to main at 1d1bbdba70011f29518285a55cca558a6af45c0f.
- Source policy: AGENTS.md, EVIDENCE_LEDGER.md, CURRENT.md, HANDOFF.md's newest
  section, BL-0011 through BL-0013; solver/bot.js, engine.js,
  human-benchmark.js, board-trace.js, recording-replay.js, policy-eval.js,
  their direct dependencies and relevant tests; recordings/, pilots/,
  play-sessions/, and referenced candidate identities.
- This foreground agent is the only observed agent using the checkout.
  Existing play/bot-vision servers and memory service remain untouched.
- Administrative .orch commits may advance HEAD; source, recordings, and
  candidate identities must remain fixed. The startup run already tested this
  source revision: 344/348, four documented failures.

## Completion test

1. Enumerate exact benchmark membership, grouping by candidate identity and
   seed. Report candidate move budget, recorded moves, first target crossing,
   and outcome. Resolve every counted row to its raw recording and candidate.
   Oracle: pre-existing candidateIndex(), pilot candidate/receipt identity
   mapping, recording-replay.replay(), and source-resolution procedure matching
   each reported field to those inputs; oracle_class: evidence;
   provenance: pre-existing.
2. Trace actual default move selection through candidate generation, scoring,
   and target/bomb/final-move branches. For every capability claim cite the
   implementation condition or formula; distinguish absence of a direct term
   from absence of the behavior. Oracle: source-resolution against named
   functions and DEFAULT_PARAMS in bot.js and engine.js; oracle_class:
   evidence; provenance: pre-existing.
3. Illustrate fitness differences on exact recordings with common stopping
   rules. For scoring comparisons use the same candidate/seed, remove target
   for the bot, and give it exactly the human's recorded move count. For
   winning-speed comparisons use the original target and the first crossing
   in the human move log, with losses shown separately. Keep raw full-budget
   benchmark values for contrast and report repeated-subject weighting.
   Oracle: existing playBot(candidate, seed, {uncapped:true}) using a copy
   whose moves equals recording.movesUsed, recording-replay.replay(), and
   explicit arithmetic on move scores and unique identity/seed groups;
   oracle_class: evidence; provenance: pre-existing. These are exact-corpus
   diagnostics, not an estimate of general playing strength or new experiment.
4. Source and corpus remain unchanged. Oracle: `git diff
   07a68c0ebd15763207134690acd1d4f7f061465f --name-only` contains only this
   run's .orch paths; oracle_class: deterministic; provenance: pre-existing.

## Return fields

Status, artifact paths, verification, cited findings with confidence,
contradictions, dead ends, gaps, changed_artifacts, and unresolved owner choice.

## Result

Completed at 2026-09-05T23:50:53Z. Artifact store:
`.orch/runs/2026-09-05-policy-grounding/`.

- `report.md`: cited findings, confidence, contradictions, objective examples,
  dead ends, and unresolved choices.
- `inspect.cjs` and `recording-diagnostic.json`: exact 12-recording benchmark
  plus separate 3-recording ordinary-play diagnostics. All human recordings
  replay with no reported discrepancies. All 14 human wins end on their first
  target crossing. Benchmark is 9 candidate/seed cases, 8 initial grids;
  matched recorded-move score comparisons favor bot 7 times and human 5.
- `pilot-position.cjs` and `pilot-position.json`: real human-reached position
  with an immediate winning 37760-point chain absent from the default and
  offerFull candidate pools. No eventual-loss claim follows.
- `archive-diagnostic.cjs` and `archive-diagnostic.json`: matching original
  archive hash and recomputed stored holdout score lifts, including -3.5723%
  for the leading screened representative. Historical evaluator source read
  via `git show 52f500c:solver/policy-eval.js` confirms full-budget scoring.

No source, test, game rule, policy, receipt, canonical ledger, or backlog edits.
Fitness remains undecided. No fresh search, generalizing experiment, broad
test rerun, or policy promotion was performed.

Changed artifacts: this ticket and the seven files named above, all within
the authorized run store or ticket bookkeeping scope.

## Verification

1. PASS, evidence: existing candidate mapping and replay function resolve all
   12 benchmark recordings; ordinary-play cases resolve separately to current
   shipped levels and replay exactly. Per-file/subject hashes and first-target
   crossing arithmetic are retained in recording-diagnostic.json. Bot target
   and full-budget values reproduce the prior startup outputs.
2. PASS, evidence: report cites executable branches/formulas at the fixed
   source revision, distinguishes current behavior from comments and proposed
   capabilities, and exercises the live pilot position. Existing board-trace
   CLI independently prints the same human 37760 versus chosen-bot 1536 scores.
3. PASS, evidence: existing playBot and replay functions produced the fixed
   corpus comparisons. Report explicitly separates full budget, equal recorded
   moves, first target crossing, losses, repeated cases, and ordinary play.
   All three diagnostic commands exit 0. Archive arithmetic is verification
   of stored evidence, not a new run.
4. PASS, deterministic: git diff against the source paths returns 0 and HEAD
   remains 07a68c0 before the artifact commit; git status lists only this run's
   artifact and ticket directories. Administrative commits may advance HEAD
   per the fixed input contract. All 19 report file links resolve.

Join (orch-integrate, /root): accepted; all four criteria covered at fixed
source/corpus identities with pre-existing source/replay oracles. Overall
PASS, weakest oracle_class evidence. No criteria invalidated. The artifact
commit changes none of the sources or recordings covered by these findings.

## Feedback

Two speculative artifact-path probes failed and were logged immediately with
the friction logger; the ledger supplied the correct RESULT-0017 archive.
Contradictions are preserved explicitly in report.md, not silently averaged
or used to rewrite historical claims.

## Risks

Small, selected, repeated human recordings are not a held-out population.
Matching horizon and seed does not recover the human's intention. Findings
will retain that limit; no policy-strength generalization or promotion.
