---
id: T-001
run: 2026-08-21-adhoc-multipath-bot
status: failed
executor: tdd
independence: checker
depends_on: []
write_scope:
  - solver/engine.js
  - solver/bot.js
  - solver/calibration.js
  - solver/tests/bot.test.js
  - solver/tests/calibration.test.js
  - solver/multipath-ablation.js
  - EVIDENCE_LEDGER.md
excluded_actions:
  - change game rules, level targets, candidate receipts, or recorded calibrations
  - discard or revert pre-existing dirty-worktree changes
bound: 90 minutes; one screen sweep and one disjoint confirmation run
claimed_by: root
claimed_at: 2026-08-22T01:11:29Z
---

## Objective

The live 2248 bot can retain several promising partial chain paths instead of committing to one greedy walk, and the feature becomes the default only if a disjoint whole-game confirmation proves higher score without lower win rate.

## Fixed inputs

- Live checkout `/Users/eluckey/Developer/research and games/2248-challenge`, branch `level-curve-retune`, starting HEAD `91321e4`.
- Existing dirty `offerFull` work remains default-off and must be preserved.
- Current policy and evaluation contracts: `solver/bot.js`, `solver/engine.js`, `solver/policy-eval.js`.
- Accepted comparison method: paired log-ratio lift with level/seed clustering and `t > 3` (`EVIDENCE_LEDGER.md`, RESULT-0010 through RESULT-0014).
- Existing calibration `calib-1` remains behaviorally pinned to the pre-multipath bot.

## Completion test

1. The public `chooseMove(state, options)` seam can select the fixed 8-tile, sum-32 chain on the 4x4 literal board `[[null,2,null,2],[4,4,2,null],[4,8,4,4],[2,null,2,null]]` when multi-path search is enabled, while the one-path setting does not. Oracle: a focused Node test named `chooseMove: multi-path search retains a valuable branch the one-path walk discards`; oracle_class: deterministic; provenance: pre-existing.
2. Setting the new path width to one reproduces the pre-change move choice, and the default-off `offerFull` behavior remains unchanged. Oracle: focused bot tests comparing default and explicit one-path choices on the fixed fixture plus the existing bot suite; oracle_class: deterministic; provenance: pre-existing.
3. A screen sweep selects the path width without using confirmation seeds, then exactly one disjoint confirmation across all shipped levels reports positive paired score lift at `t > 3` and no fall in overall win rate. Oracle: `node solver/multipath-ablation.js --confirm`; oracle_class: deterministic; provenance: pre-existing.
4. `calib-1` explicitly pins the new parameter to its pre-change value and remains a literal snapshot. Oracle: `node --test solver/tests/calibration.test.js`; oracle_class: deterministic; provenance: pre-existing.
5. Targeted bot, engine, policy-evaluation, and calibration tests pass. The full solver suite has no failure except pre-existing receipt-identity mismatches for `candidate-levels-52.json`, `candidate-levels-54.json`, and `candidate-levels.json`. Oracle: `node --test solver/tests/bot.test.js solver/tests/engine.test.js solver/tests/policy-eval.test.js solver/tests/calibration.test.js`, followed by `node --test solver/tests/*.test.js`; oracle_class: deterministic; provenance: pre-existing.
6. If criterion 3 passes, the default adopts the confirmed width and the evidence ledger records the exact scope, effect, cost, and heuristic proof class. If it fails, the default remains one and the ticket cannot complete. Oracle: inspect `solver/bot.js` default and the new append-only ledger record against the confirmation output; oracle_class: evidence; provenance: pre-existing.

## Return fields

- status
- changed_artifacts
- screen width and result
- confirmation lift, t statistic, win-rate change, and relative cost
- per-criterion verdicts
- risks

## Result

Executor `root` implemented a bounded beam that retained 2, 4, 6, or 8 partial paths per start and exposed it through `chooseMove` as `pathWidth`. The fixed public-seam fixture improved from sum 16 to sum 32 at width 6. The whole-game screen decisively falsified the implementation: every width hurt both score and win rate. Width 8 was least bad at -23.86% paired score (t=-9.9), 88.54% -> 55.00% win rate, 2.07x cost. No confirmation was run and the default remained one.

Changed artifacts at the failed join: `solver/engine.js`, `solver/bot.js`, `solver/calibration.js`, `solver/tests/bot.test.js`, `solver/tests/calibration.test.js`, `solver/multipath-ablation.js`, plus this ticket. These changes are not accepted and will be superseded by a fresh ticket rather than treated as a completed result.

## Verification

1. PASS — `node --test --test-name-pattern='multi-path search retains' solver/tests/bot.test.js`; fixed fixture sum 16 -> 32.
2. PASS — explicit `pathWidth: 1` produced the pre-change choice; existing bot suite passed.
3. FAIL — screen widths 2/4/6/8 all reduced paired score and win rate; best was width 8 at -23.86%, t=-9.9, win rate -33.54 points. Confirmation correctly not run.
4. PASS — `node --test solver/tests/calibration.test.js` passed with `pathWidth: 1` pinned.
5. UNVERIFIED — targeted suite passed 73/73; full suite was not run after criterion 3 failed.
6. FAIL — the result did not clear the adoption bar; no ledger result was added and the default remained one.

Join disposition: rejected, child under-delivered against the required whole-game improvement criterion. Terminal status: failed.

## Feedback

[]

## Risks

- The failed implementation still exists in the dirty worktree until the immediately following replacement ticket removes or supersedes it. Its default is one, so live behavior is unchanged.
