---
id: TCG-001
run: 2026-09-02-targeted-chain-generator
status: claimed
executor: orch-tdd
pack: orch-code-pack
independence: gate
depends_on: []
write_scope:
  - solver/targeted-chain-generator.js
  - solver/targeted-chain-coverage.js
  - solver/test-fixtures/level52-seed2000000-human-games.json
  - solver/tests/targetedChainGenerator.test.js
excluded_actions:
  - Modify solver/bot.js or any shipped game rule, level, policy default, descriptor, ledger, or CURRENT.md
  - Integrate or promote the challenger into chooseMove
bound: 90 minutes and one correction pass
claimed_by: /root/targeted_chain_impl
claimed_at: 2026-09-02T21:26:31Z
---

## Objective

An opt-in bounded generator, exercised through one real-state coverage CLI and one durable owner-game corpus, materially improves production-pool coverage without changing production selection.

## Fixed inputs

- Spec: `/private/tmp/2248-targeted-chain-generator.IGq5PQ/.orch/runs/2026-09-02-targeted-chain-generator/spec.md`
- Workspace: `/private/tmp/2248-targeted-chain-generator.IGq5PQ`
- Base: `a71195a7355e5d055025cb36f1496d76687ff2ba`
- Standards: `/Users/eluckey/Developer/research and games/2248-challenge/AGENTS.md`
- Craft: `/Users/eluckey/.orchflows/lib/packs/orch-code-pack/references/craft.md`
- Oracle policy: `/Users/eluckey/.orchflows/lib/packs/orch-code-pack/references/oracles.md`
- Source recordings: `/private/tmp/2248-play-level52-seed2000000/*.json`, identified by the four hashes frozen in the spec

## Completion test

1. Four compacted sessions replay through `solver/engine.js` to exact score/move outcomes. Oracle: `node --test solver/tests/targetedChainGenerator.test.js`; oracle_class: deterministic; provenance: authored-here; independence: downstream gate.
2. Coverage CLI evaluates all 39 real decision states, reports production and challenger exact/equivalent coverage plus telemetry, exceeds 4/39 equivalent coverage, and recovers both named anchors. Oracle: `node solver/targeted-chain-coverage.js --fixture solver/test-fixtures/level52-seed2000000-human-games.json`; oracle_class: deterministic; provenance: authored-here; independence: downstream gate.
3. Caller-supplied caps deterministically produce `complete: false`, and a controlled broken configuration fails the same acceptance seam. Oracle: `node --test solver/tests/targetedChainGenerator.test.js`; oracle_class: deterministic; provenance: authored-here; independence: downstream gate.
4. Production chooser parity remains exact. Oracle: `node --test solver/tests/botVision.test.js`; oracle_class: deterministic; provenance: pre-existing.
5. Broad regression identities are no worse than baseline. Oracle: `node --test solver/tests/*.test.js`; oracle_class: deterministic; provenance: pre-existing.

## Return fields

- result revision
- changed artifacts
- per-criterion verdicts and command output identities
- coverage counts and search-bound telemetry
- risks and uncovered remainder
- feedback

## Result

- Progress 2026-09-02: fixed inputs, repository evidence rules, source hashes, and one-writer evidence inspected at base `a71195a7355e5d055025cb36f1496d76687ff2ba`. The first red slice is the real-engine replay gate for the four compacted sessions; implementation remains absent until that check fails for the expected missing-fixture reason.
- Slice 1 red: `node --test solver/tests/targetedChainGenerator.test.js` failed with `MODULE_NOT_FOUND` for `level52-seed2000000-human-games.json`.
- Slice 1 green: added a four-session fixture carrying the frozen SHA-256 identities; the same command passed `1/1`, and each compacted session replayed through `recording-replay.js`/`engine.js` to its exact score and move count.
- Slice 2 red: after adding real-action, opening-anchor, and deterministic-cap checks, `node --test solver/tests/targetedChainGenerator.test.js` failed with `MODULE_NOT_FOUND` for `targeted-chain-generator.js`.
- Slice 2 green: added the opt-in generator with real chain legality, action-identity dedupe, bounded node traversal, bounded path diversity, explicit lattice/high-score/long-chain objectives, and fail-closed telemetry. The same command passed `4/4`; the real 24-tile sum-3456 opening anchor was recovered after a search bounded at 100,000 nodes, and a one-node cap reproduced `complete: false` with cap reason `maxNodes` on both runs.
- Slice 3 red: after adding the 39-state comparison and controlled-broken CLI checks, `node --test solver/tests/targetedChainGenerator.test.js` first failed with `MODULE_NOT_FOUND` for `targeted-chain-coverage.js`; the first implementation then stayed red on the production-bot second-decision sum-2048 anchor.
- Slice 3 green: the generator now preserves one best action per encountered mergeable lattice sum and searches deterministic low-value-first depth-first paths before the node cap. `node --test solver/tests/targetedChainGenerator.test.js` passed `6/6`. The exact CLI passed with production `2/39` exact and `4/39` equivalent versus challenger `5/39` exact and `20/39` equivalent; both named anchors were true. Search telemetry covered 39 states, visited 2,312,264 nodes, considered 2,305,032 actions, returned 16,121 candidates, and reported 39 capped states (`candidateLimit`: 39, `maxNodes`: 14) rather than claiming exhaustiveness.
- Implementation result revision: `e5a9a6c` (`Prove targeted coverage on real states`), following slice commits `c5864b7` and `dc39d65`.
- Changed artifacts:
  - `solver/targeted-chain-generator.js` — SHA-256 `21823adb97f3baa12821fbbbbf2c7d58424c0c0a06699d59ec054b700ee2f333`
  - `solver/targeted-chain-coverage.js` — SHA-256 `e455009158cc60df574ebf00b8badfa186d6ce27f674ed24f399aa7cd8de4f0e`
  - `solver/test-fixtures/level52-seed2000000-human-games.json` — SHA-256 `a6746fd2bfdd7549a02d8ec0001a6176b58a20daef4a4f5166509461c65b4eec`
  - `solver/tests/targetedChainGenerator.test.js` — SHA-256 `bd0f2b5c00352ab5bc74d6e0a14e2adfffb0bdcdf321d83af6a3dfd275c65e71`
- Protected-path audit: `git diff a71195a7355e5d055025cb36f1496d76687ff2ba -- solver/bot.js src/game.js EVIDENCE_LEDGER.md CURRENT.md` produced no output. No chooser, game rule, level, weight, descriptor, evidence ledger, or policy-selection path changed.

## Verification

- Criterion 1 — PASS, deterministic authored-here/downstream gate. `node --test solver/tests/targetedChainGenerator.test.js` passed `6/6`; its replay case exercised all four sessions through the pre-existing `recording-replay.js` and `engine.js` seams and reproduced `119808/11`, `120256/10`, `113536/9`, and `110464/9`.
- Criterion 2 — PASS, deterministic authored-here/downstream gate. `node solver/targeted-chain-coverage.js --fixture solver/test-fixtures/level52-seed2000000-human-games.json` exited 0 with verdict `PASS`: 39 states; production exact/equivalent `2/4`; challenger exact/equivalent `5/20`; both named anchors `true`; 2,312,264 nodes; 2,305,032 actions considered; 16,121 candidates returned; 0 complete and 39 capped states; cap reasons `candidateLimit: 39`, `maxNodes: 14`; observed aggregate elapsed `2354.877ms`.
- Criterion 3 — PASS, deterministic authored-here/downstream gate. The focused suite reproduced a caller-supplied one-node cap with `complete: false` and `capReasons: ['maxNodes']`; its spawned controlled-broken CLI used `--max-nodes 1 --candidate-limit 1 --path-width 1`, exited 1 through `assessCoverage`, returned verdict `FAIL`, and named the missing opening anchor.
- Criterion 4 — PASS, deterministic pre-existing oracle. `node --test solver/tests/botVision.test.js` passed `5/5`, including the exact Level 52 seed 2000000 production outcome `109440/12` and chooser/analyzer parity.
- Criterion 5 — PASS by failure identity, deterministic pre-existing oracle. `node --test solver/tests/*.test.js` reported `303/308` passing and the same five baseline failure identities: three `receiptGate.test.js` stale-receipt cases and two `repoBaseline.test.js` durable-state cases. No new test failure identity appeared.

## Feedback

- The named 2048 anchor caught a real first-pass selection defect: globally ranking lattice sums allowed larger sums to crowd 2048 out. Retaining the best action per encountered lattice target both simplified the intent and closed the same acceptance seam.
- The aggregate elapsed value is telemetry, not a deterministic identity; coverage counts, action identities, node counts, cap reasons, and verdict are deterministic for the fixed inputs and limits.

## Risks

- Every default corpus search was capped, so this result does not claim exhaustive action coverage, absence of other actions, or global optimality. Fourteen states hit the node cap; all 39 intentionally returned only a bounded candidate pool.
- The measured lift is exact for this four-session, one-level, one-seed corpus only. It is not a generalized experiment result and does not establish that a policy should select any challenger candidate.
- The run spec/worklog directory remains parent-owned untracked orchestration state. It contributes text to the existing `repoBaseline.test.js` durable-state failure until the join records and commits that run state.

## Accepted-defect correction pass — 2026-09-02

- Fixed result inspected: `0ba3304f147cbac8c43b01d70af5f21b7e45b999`. Repair scope stayed within `solver/targeted-chain-generator.js`, `solver/targeted-chain-coverage.js`, `solver/tests/targetedChainGenerator.test.js`, and this append-only ticket update.
- Red evidence: after adding both accepted-defect regressions, `node --test solver/tests/targetedChainGenerator.test.js` reported `5/8` passing and three failures. The 3x3 real-search fixture considered 1,332 actions and returned 185 candidates below its 512 outward limit but incorrectly returned `complete: false`; the real-state report had no `search.records`; and terminal `--max-nodes` silently ran the default search and exited 0 instead of rejecting the malformed present flag.
- CLI disposition: present `--max-nodes`, `--candidate-limit`, and `--path-width` flags now require a following positive safe integer before fixture loading or coverage evaluation. The focused real-CLI regression runs all three terminal flags and requires exit 1, empty stdout, and the named flag error on stderr.
- Telemetry disposition: candidate-limit status now comes from actual outward truncation (`candidatesAvailable > candidateLimit`), not from comparing all legal actions considered with the objective-filtered output. Each corpus search now reports source filename, source SHA-256, zero-based move index, human action identity, complete/capped status, limits, cap reasons, and per-search counts.
- Green focused evidence: `node --test solver/tests/targetedChainGenerator.test.js` passed `8/8`. The false-cap fixture now reports complete with no cap reason while retaining 185 candidates from 1,332 considered actions.
- Green real-coverage evidence: `node solver/targeted-chain-coverage.js --fixture solver/test-fixtures/level52-seed2000000-human-games.json` exited 0 with verdict `PASS`: 39 per-search records; production exact/equivalent `2/4`; challenger exact/equivalent `5/20`; both anchors `true`; 2,312,264 nodes; 2,305,032 actions considered; 16,137 candidates available and 16,121 returned; 25 complete and 14 capped searches. Aggregate and per-record cap counts agree at `maxNodes: 14` and `candidateLimit: 2`; both candidate-limited searches returned exactly 512 and had additional available candidates. This supersedes the earlier ticket statement that all 39 searches hit `candidateLimit`.
- Protected and broad evidence: `node --test solver/tests/botVision.test.js` passed `5/5`. `node --test solver/tests/*.test.js` reported `305/310` passing and only the same five baseline failure identities: the three stale receipt cases and two durable-state cases. `git diff --check` passed.
- Queued scope: none. Threshold redesign, fixture provenance hardening, production integration, and performance tuning remain outside this correction pass.
