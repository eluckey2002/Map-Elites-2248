---
id: T-001
run: 2026-08-29-generated-level-corpus-runner
status: complete
executor: orch-tdd
depends_on: []
write_scope:
  - solver/generated-level-corpus.js
  - solver/tests/generatedLevelCorpus.test.js
excluded_actions:
  - execute the 480-shape corpus or any real screening, authoring, receipt replay, or policy optimization run
  - modify the frozen preregistration or any pinned source file, including solver/generate-levels.js, solver/level-author.js, solver/calibration.js, solver/bot.js, solver/engine.js, and src/game.js
  - write live candidate stores, historical receipts, the evidence ledger, CURRENT.md, Universe Map artifacts, game rules, scoring, or champion state
bound: one deterministic runner module and one focused test module; red-green slices; no corpus execution
claimed_by: codex-root/orch-tdd
claimed_at: 2026-08-29T05:04:52-05:00
---

## Objective

A new, separately testable runner can plan and, only behind an explicit execution seam, carry out `generated-level-corpus-v1` exactly as frozen, while dry-run and all tests perform zero real games and no pinned source identity changes.

## Fixed inputs

- Workspace: `/Users/eluckey/Developer/research and games/2248-challenge/.orch/runs/2026-08-29T01-57-14Z-adhoc-research-worktree/worktree`, clean at `a44608da47ac877e52752ec4a73a4c77c50d1578` on `codex/research-session-2026-08-28`.
- Frozen protocol: `.orch/runs/2026-08-29-generated-level-corpus-preregistration/preregistration.md`, committed at `a44608da47ac877e52752ec4a73a4c77c50d1578`.
- Pinned source identities and calibration stamp: the exact table and `calib-1` values in that protocol.
- Existing public seams: exports of `solver/generate-levels.js`, `solver/level-author.js`, `solver/calibration.js`, and `solver/engine.js` at the fixed workspace identity.
- Craft: `/Users/eluckey/.orchflows/lib/packs/orch-code-pack/references/craft.md`.
- Checksum interpretation forced by non-self-reference: `manifest.json` records hashes of child evidence artifacts; `sha256sums.txt` externally records the final hashes of `manifest.json` and every child artifact and is itself the checksum index, not an artifact required to contain its own hash.

## Completion test

1. Planning is deterministic, game-free, and complete: exactly 480 raw slots arise from the four frozen sampler seeds, global deduplication is canonical, all 72 family keys are materialized, family boundaries are exact, the big-endian hash split is 40/15/17, and two plans from identical inputs serialize byte-for-byte equally.
   - Oracle: pre-specified cases in `node --test solver/tests/generatedLevelCorpus.test.js` use injected functions that throw if any screen or authoring seam is called during planning; negative fixtures alter a family boundary, partition endianness, duplicate signature, and sampler seed and must fail the asserted result.
   - oracle_class: deterministic
   - provenance: pre-existing
2. Full-measurement selection is outcome-independent and quota exact: family-first lexical round robin selects at most 60 clean and 12 screen-rejected shapes, never transfers unused quota, never selects a duplicate, and is unchanged when score, target, gate-margin, win-rate, or incumbent-behavior fields change without changing the categorical screen pool.
   - Oracle: pre-specified synthetic pools in the focused test compare selected slot identities before and after outcome-field mutations, exercise exhausted pools, and assert whole-family partition equality.
   - oracle_class: deterministic
   - provenance: pre-existing
3. Preflight fails closed before play: current pinned sources and `calib-1` pass; a changed source hash, calibration stamp, protected champion identity, protocol identity, or overlapping declared gameplay seed produces `INVALIDATED` with zero calls to injected screen, derive, or replay functions.
   - Oracle: pre-specified current-tree and wrong-result fixtures in the focused test, with call counters asserting zero on every negative case.
   - oracle_class: deterministic
   - provenance: pre-existing
4. Stubbed execution accounts for every slot and result: it records screens, 60/12 selection origin, candidate and receipt identities, replay integrity, exact gate dispositions, `playable-core`, `adversarial-stress`, and `unverified` separately; computes all five coverage conditions once; returns only `PASS`, `INCONCLUSIVE`, `BLOCKED`, or `INVALIDATED`; and never exceeds the frozen 76,320-game accounting ceiling.
   - Oracle: pre-specified injected stub runs in the focused test cover every disposition and terminal state, include a wrong replay fixture, prove stress cases cannot satisfy coverage, and assert exact budget arithmetic without invoking the real game engine.
   - oracle_class: deterministic
   - provenance: pre-existing
5. CLI safety and output confinement are fail-closed: no mode or dry-run writes nothing and plays zero games; planning may write only a run-scoped deterministic plan; execution requires an explicit flag and output directory; artifact writing cannot target live candidate stores or paths outside the named run directory; the non-self-referential checksum convention is exact.
   - Oracle: pre-specified temporary-directory CLI and writer fixtures in the focused test assert nonzero refusal for unsafe invocations, byte-stable allowed output, no files outside the temporary run root, and exact hashes for manifest plus child artifacts.
   - oracle_class: deterministic
   - provenance: pre-existing
6. Existing contracts and change boundaries remain intact.
   - Oracle: `node --test solver/tests/generatedLevelCorpus.test.js solver/tests/generateLevels.test.js solver/tests/levelAuthor.test.js solver/tests/calibration.test.js solver/tests/mapElites.test.js`; exact SHA-256 comparison of every pinned source against the preregistration; `git diff --check`; diff from `a44608da47ac877e52752ec4a73a4c77c50d1578` contains only the two write-scope paths.
   - oracle_class: deterministic
   - provenance: pre-existing

## Return fields

- committed slice identities and red/green evidence
- changed artifacts
- public runner seams and safety boundary
- per-criterion verdicts
- execution status
- feedback and risks

## Result

- Slice commits:
  - `02f49d020ed12cc171950bb6874eae1184d472b6` — deterministic 480-slot planning, family map, global deduplication, and 60/12 selection.
  - `882a9ecb23ecf639a37dd5fd1ba882beabaee21d` — pinned preflight, fail-before-play invalidation, stubbed measurement dispositions, coverage, and game-budget accounting.
  - `c4a42fcf7ea83201901e165cccffa5555aec2dd0` — tracked seed-collision scan, explicit CLI, run-scoped plan/evidence writers, current champion/repository receipts, and external checksum index.
- Changed artifacts: `solver/generated-level-corpus.js` and `solver/tests/generatedLevelCorpus.test.js` only.
- Public seams: `buildPlan`, `familyForShape`, `partitionForFamily`, `selectForFullMeasurement`, `preflight`, `scanDeclaredSeedRanges`, `runCorpus`, `evaluateCoverage`, `writePlan`, `writeEvidence`, and `main`.
- Safety boundary: no CLI mode refuses; `--dry-run` plays zero games and writes nothing; `--plan --out <direct-child-of-.orch/runs>` writes only `plan.json`; execution requires `--execute --confirm generated-level-corpus-v1 --out <direct-child-of-.orch/runs>`.
- The canonical manifest embeds child-artifact hashes; `sha256sums.txt` externally binds the final `manifest.json` and `report.md` hashes and is explicitly the non-self-hashed checksum index.
- Execution status: **not run**. Every execution-path test used injected stubs; no real screen, authoring, replay, candidate write, or policy optimization occurred.
- Join disposition: accepted. The returning executor matches `claimed_by`, both changed artifacts are inside the fixed write scope, all six pre-existing deterministic criteria pass at `c4a42fcf7ea83201901e165cccffa5555aec2dd0`, and no covered identity changed after verification.

## Verification

- Red slice 1: focused test failed with `Cannot find module '../generated-level-corpus'`; green: seven planning and selection tests passed.
- Red slice 2: five focused cases failed because `preflight`, `evaluateCoverage`, and `runCorpus` did not exist; green: twelve focused tests passed.
- Red slice 3: five focused cases failed because CLI, seed scan, and evidence writer seams did not exist; the safety fixture then caught an uncaught unsafe-output exception; green: seventeen focused tests passed after fail-closed handling.
- Criterion 1: PASS (`deterministic`) — exact 480 slots, zero planning game calls, byte-identical plans, canonical deduplication, boundary fixtures, 72 family keys, and the 40/15/17 big-endian map all pass.
- Criterion 2: PASS (`deterministic`) — outcome-field mutations leave selected slot identities unchanged; clean/stress quotas are 60/12 without transfer; duplicates cannot enter; every family has one hash partition.
- Criterion 3: PASS (`deterministic`) — current-tree preflight reports `PASS`, no failures, HEAD `c4a42fcf7ea83201901e165cccffa5555aec2dd0`, clean state, and zero seed collisions. Five wrong-result fixtures each return `INVALIDATED` with zero screen, derive, and replay calls.
- Criterion 4: PASS (`deterministic`) — stub execution accounts for 480 slots, screen and full-selection dispositions, candidate/receipt identities, replay integrity, all three admission labels, five one-shot coverage checks, terminal states, and a computed maximum no greater than 76,320 games.
- Criterion 5: PASS (`deterministic`) — no-mode, missing-confirmation, unsafe path, and overwrite fixtures fail closed; dry-run is game-free; two plan outputs are byte-identical; stub execution emits only `manifest.json`, `report.md`, and an exact external `sha256sums.txt`.
- Criterion 6: PASS (`deterministic`) — the required focused command reports 50 tests, 50 pass, 0 fail; pinned hashes exactly match the preregistration; syntax and `git diff --check` pass; the diff from `a44608d` contains only the two write-scope files; worktree is clean.
- Proportional regression: full `node --test solver/tests/*.test.js` reports 254 tests, 252 pass, and only the same two deliberately retained receipt-identity failures: `candidate-levels-52.json` and `candidate-levels.json`.

## Feedback

- The preregistration's self-referential “hash every emitted artifact” wording is implemented with the standard non-self-referential boundary frozen in this ticket: the manifest hashes child evidence, while an external checksum index hashes the final manifest and report.

## Risks

- The tracked-file collision scan detects numeric literals that land inside the registered ranges. A future computed range that overlaps without containing a literal inside those ranges must be supplied through the explicit `declaredSeedRanges` preflight seam; no source scanner can generally infer arbitrary runtime range arithmetic.
- Passing implementation tests proves protocol fidelity and fail-closed behavior, not that the generated corpus will achieve its 36-family adequacy bar or represent human-relevant level diversity.
