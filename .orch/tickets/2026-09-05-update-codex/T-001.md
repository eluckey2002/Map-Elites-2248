---
id: T-001
run: 2026-09-05-update-codex
status: in_progress
executor: orch-investigate
depends_on: []
write_scope:
  - local Codex application installation
  - this ticket
excluded_actions:
  - repository source changes
  - Codex configuration changes
  - uninstall or downgrade
bound: one built-in Codex update attempt plus three post-update probes
independence: pre-existing updater, version command, executable resolution, and doctor command
claimed_by: root
claimed_at: 2026-09-05
---

## Objective

Update the active Windows Codex installation to the latest version offered by its built-in updater, then verify which executable and version are active.

## Fixed inputs

- Active executable before update: `C:\Users\eluck\AppData\Local\Programs\OpenAI\Codex\bin\codex.exe`
- Reported version before update: `codex-cli 0.149.1`
- Official OpenAI documentation states that the Windows desktop app normally checks for and installs updates itself.
- The installed CLI exposes the pre-existing `codex update` command.

## Completion test

1. The built-in updater completes without an error, or explicitly reports that the installed version is current.
   - Oracle: `codex update` process exit and output; oracle_class: deterministic; provenance: pre-existing.
2. The active CLI version and executable path are recorded after the update.
   - Oracle: `codex --version` and `Get-Command codex`; oracle_class: deterministic; provenance: pre-existing.
3. The updated installation passes its packaged health check.
   - Oracle: `codex doctor`; oracle_class: deterministic; provenance: pre-existing.

## Return fields

- status
- result identity
- verification
- cited findings with confidence
- contradictions
- dead ends
- gaps

## Result

Pending.

## Handoff

None.
