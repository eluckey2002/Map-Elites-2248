---
id: T-001
run: 2026-08-21-adhoc-low-value-multipath
status: complete
executor: tdd
independence: checker
depends_on: []
write_scope:
  - solver/engine.js
  - solver/bot.js
  - solver/calibration.js
  - solver/tests/bot.test.js
  - solver/tests/calibration.test.js
  - solver/multipath-ablation.js
  - EVIDENCE_LEDGER.md
excluded_actions:
  - change game rules, level targets, candidate receipts, or recorded calibrations
  - discard or revert pre-existing dirty-worktree changes
bound: 75 minutes; one screen sweep and one disjoint confirmation run
claimed_by: root
claimed_at: 2026-08-22T01:17:19Z
---

## Objective

The bot retains several low-value-first chain paths instead of one, preserving the accepted low-value strategy while recovering useful alternatives; it becomes the default only if disjoint whole-game confirmation proves higher score without lower win rate.

## Fixed inputs

- Failed predecessor: `.orch/tickets/2026-08-21-adhoc-multipath-bot/T-001.md`; branching across higher-value extensions is rejected.
- Live checkout branch `level-curve-retune`, starting HEAD `91321e4`, with all pre-existing dirty work preserved.
- Existing paired estimator and `t > 3` adoption bar in `solver/policy-eval.js` and the evidence ledger.
- Screen seeds 9,000,000-9,000,039; confirmation seeds 10,000,000-10,000,299; no reuse across stages.

## Completion test

1. Through the public `chooseMove` seam, path width 6 returns a mergeable 16-tile chain on the fixed 5x5 all-two fixture `[[2,2,2,2,2],[2,2,2,null,2],[2,2,2,2,2],[2,null,null,2,2],[2,null,2,null,null]]`, while path width 1 returns 8 tiles. Oracle: focused test `chooseMove: low-value multi-path search keeps an alternate long route`; oracle_class: deterministic; provenance: pre-existing.
2. The generator never branches to a double-valued extension while an equal-valued extension is available; the predecessor fixture remains sum 16 rather than the harmful sum-32 route. Oracle: focused public-seam negative-control test on the predecessor's 4x4 literal board; oracle_class: deterministic; provenance: pre-existing.
3. Screen widths 2, 4, 6, and 8 are selected only on screen seeds, followed by exactly one confirmation of the selected width across every shipped level and disjoint confirmation seeds. Confirmation must show positive paired score lift at `t > 3` and no fall in overall win rate. Oracle: `node solver/multipath-ablation.js --confirm`; oracle_class: deterministic; provenance: pre-existing.
4. Path width 1 reproduces the pre-change bot, `offerFull` stays default-off, and `calib-1` pins both values. Oracle: bot and calibration tests; oracle_class: deterministic; provenance: pre-existing.
5. Targeted tests pass; the full suite has no failure except the three pre-existing receipt-identity mismatches. Oracle: targeted `node --test` command and `node --test solver/tests/*.test.js`; oracle_class: deterministic; provenance: pre-existing.
6. On criterion 3 PASS, adopt the selected path width and append an evidence-ledger result with scope, lift, t, win-rate change, relative cost, and heuristic proof class. Otherwise leave the default at one and fail the ticket. Oracle: inspect live default and ledger against confirmation output; oracle_class: evidence; provenance: pre-existing.

## Return fields

- status
- changed_artifacts
- screen and confirmation measurements
- per-criterion verdicts
- risks

## Result

Status: complete.

The live bot now keeps its historical greedy route and supplements it with a bounded beam of up to eight alternative routes. Alternatives branch only among tiles tied at the lowest available value, so the accepted low-value-first strategy remains intact. `solver/bot.js` adopts beam width 8; `calib-1` explicitly remains at `pathWidth: 1`.

The first implementation branched to doubled values and failed its screen. A second implementation passed confirmation but failed a pre-existing regression test because alternatives could replace the historical route; its +13.83% measurement was invalidated. The corrected additive implementation was rerun at the same frozen screen and confirmation cells. Width 8 won the screen at +24.32%. Confirmation across all 53 levels x 300 seeds measured +23.00% paired score (t=50.8), mean score 42,355.9 -> 52,268.0, win rate 93.64% -> 99.58% (+5.94 points), and 2.68x relative compute cost.

Changed artifacts:

- `solver/engine.js` — additive low-value-first path beam; SHA-256 `4e2323b9218aed6a552017ca37eab16becf8ef0a314f7081d02717ef1f7a12c6`
- `solver/bot.js` — live beam width 8; SHA-256 `9abe8ca83dc26d8596320ce3e9b84aae9e3c342e6e79cac2c4c743c43c70b840`
- `solver/calibration.js` — `calib-1` pins path width 1; SHA-256 `584f99aae3dafd7fccd7dc25e0adcb2ae9867f85d267a79261d528c40e1f774f`
- `solver/tests/bot.test.js` — public-seam positive, negative, and preserved-candidate coverage; SHA-256 `13fe4d7ecf865a97905c2f4548cff0557d9e1e629ec5944534946330f791b94d`
- `solver/tests/calibration.test.js` — literal calibration pin; SHA-256 `60db7c669324d265aedfc2b130bfa80ab39ffcfd2ec369d68572b0dca5b7f3de`
- `solver/multipath-ablation.js` — frozen screen/confirmation evaluator; SHA-256 `835ead8c2ab258dce1ca656218d5a179c1aaf6e9021200788b7032a96eae54c1`
- `EVIDENCE_LEDGER.md` — invalidated RESULT-0015 preserved and superseded by accepted RESULT-0016 / CORRECTION-0004
- this ticket

## Verification

1. PASS — public `chooseMove` test: width 1 returns the historical 8-tile route and multi-path/default returns a 16-tile mergeable route on the fixed 5x5 fixture.
2. PASS — negative control: the beam never takes a doubled-value extension while an equal-valued extension remains; the harmful predecessor route is rejected.
3. PASS — corrected screen selected width 8 at +24.32%; corrected confirmation measured +23.00%, t=50.8, and win rate +5.94 points. No confirmation criterion was weakened.
4. PASS — default `offerFull` remains 0; `calib-1` pins `offerFull: 0` and `pathWidth: 1`; calibration tests pass.
5. PASS — targeted suite 74/74. Full suite 195/198; the only failures are the three criterion-authorized pre-existing receipt-identity mismatches.
6. PASS — `solver/bot.js` adopts beam width 8; EVIDENCE_LEDGER RESULT-0016 records exact scope, result, cost, and heuristic proof class. The invalidated intermediate measurement is retained as superseded RESULT-0015.

Overall verdict: PASS; weakest oracle class `evidence`. Join disposition: accepted. No covered code identity changed after the corrected confirmation.

## Feedback

[]

## Risks

- The live bot now uses about 2.68x as much computation as the one-path bot.
- The +23.00% is relative to the prior bot, not a measurement against the human owner.
- Existing level targets remain intentionally tied to `calib-1`; this change does not re-price shipped levels.
