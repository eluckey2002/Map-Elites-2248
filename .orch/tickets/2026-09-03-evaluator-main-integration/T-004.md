---
id: T-004
run: 2026-09-03-evaluator-main-integration
status: completed
executor: orch-integrate
independence: checker
checked_by: evaluator_ledger_checker
depends_on:
  - T-003
write_scope:
  - EVIDENCE_LEDGER.md
  - UNIVERSE.md
  - universe/map.html
  - universe/resolved.json
excluded_actions:
  - change any game, solver, candidate, receipt, recording, experiment, or historical evaluator-goal artifact
  - alter or supersede any prior ledger record
  - claim human difficulty, shipping approval, optimality, or a MAP-Elites result
  - push, open or update a pull request, merge, or delete worktrees
bound: 20 minutes
claimed_by: root
claimed_at: 2026-09-03T03:16:09-05:00
---

## Objective

Admit the verified evaluator-main integration as one new source-pinned ledger result and refresh only the generated Universe Map views that consume the ledger.

## Fixed inputs

- Accepted implementation commit `e95b1159ea496f0d78a9d821a1f2928f2b7af82b` in clean worktree `C:\ooo\Map-Elites-2248-evaluator-integration-20260903`.
- Accepted T-003 result, fresh checker findings with no correction and no invalidated entry, and caller rerun evidence.
- Candidate identity `a6c3e36031a17276354ba0d8b16e796d4338038fb984a581ffbe96cb9c3387f7`; target 102,000; fitting median 107,904; holdout 196 wins / 104 out-of-moves / 0 lockouts / 0 bombs / 300 total.
- Calibration solver identity `c3cefaf3ab0387180349948ba1adc9a0737cc07ae1ebfcdf51b1e8c0396f77ed`.
- Historical shipped candidate identity `043ca53f234d4092202677b3e48a1855c0194c208742bba94a2c75ebd2227f16`, target 101,000, preserved byte-identically in the candidate archive with all three bound recordings replayable.
- Existing result IDs run through RESULT-0026; use RESULT-0027 and do not reuse the historical branch's colliding RESULT-0015 label.

## Completion test

1. Append RESULT-0027 using the ledger schema and exact proof-class limits; prior records remain byte-for-byte unchanged.
2. Record only evaluator-boundary identity, deterministic replay outcomes, exact frozen-sample measurements, and historical candidate preservation. Explicitly disclaim human difficulty, shipping approval, optimality, and MAP-Elites conclusions.
3. `node tools/build-universe-map.js` changes only the three declared generated outputs; `node tools/verify-universe-map.js` and `node tools/build-universe-map.js --check` both exit 0.
4. `git diff --check e95b115...` exits 0, changed paths relative to e95b115 are exactly `write_scope`, and the worktree is clean after one evidence-admission commit.

## Return fields

- `status`
- `result_identity`
- `changed_artifacts`
- `verification`
- `known_failures`
- `risks`

## Result

Evidence admission authored and committed at
`5013dc54d78367ac4043875e4542951619176bb7` on
`integrate/evaluator-foundation-20260903`. RESULT-0027 was appended without
editing any prior record. The generated Universe model was rebuilt; only
`universe/resolved.json` changed because the rendered five-card views remain
semantically unchanged.

### Independent checker findings — `evaluator_ledger_checker`

- **One correction pass consumed — result identity.** The executor-recorded
  commit `5013dc54d78367ac4043875e4542951619176bb7` does not resolve in the
  supplied repository. The actual clean evidence-admission commit is
  `5013dc571937333689ba6e8db8829b2f37fcfae1`, whose parent is the fixed
  pre-ledger implementation commit
  `e95b1159ea496f0d78a9d821a1f2928f2b7af82b`. This ticket correction changes
  no workspace artifact.
- The actual result commit changes only `EVIDENCE_LEDGER.md` and
  `universe/resolved.json`. The ledger diff is 17 additions and zero deletions:
  one snapshot pointer plus one schema-complete `RESULT-0027`, inserted after
  `RESULT-0026`. The baseline contains no `RESULT-0027`; the result contains
  exactly one, while the pre-existing main-ledger `RESULT-0015` remains
  exactly one and unchanged. The generated-model diff changes only the ledger
  and current-navigation SHA-256 fields; both equal the exact source bytes.
- The ledger's current identities were read from the bound artifacts and
  reproduced by the named commands: candidate `a6c3e360...`, receipt
  `3b83de41...`, calibration solver `c3cefaf3...`, fitting median 107,904,
  target 102,000, and holdout 196 wins / 104 out-of-moves / 0 lockouts / 0
  bombs / 300 total. The archived 101,000 candidate remains `043ca53f...`;
  its two files have the exact fixed Git blobs `1307a3c...` and `4b244c8...`.
  All 53 focused evaluator, authoring, recording, human-pilot, and
  seed-variance tests passed; all ten recordings replayed, including the three
  Level 53 recordings, and the orphan ceiling remained zero. The 53-level
  curve gate ended with `RESULT: PASS`. Protected shipped game and live-bot
  paths have no diff from `8de076a...`.
- `node tools/verify-universe-map.js` exited 0 with `UNIVERSE MAP PASS`, and
  `node tools/build-universe-map.js --check` exited 0 with
  `UNIVERSE MAP BUILD CHECK PASS`. A copy-based wrong-result fixture, outside
  the repository tree, replaced only `universe/resolved.json` and the shared
  verifier rejected it specifically as `generated output drift:
  universe/resolved.json`.
- The record keeps fixed-sample score outcomes at `heuristic_observation` and
  limits `exact_result` to identified deterministic outputs, dependency
  identities, and complete fixed-seed or recording replays. It explicitly
  excludes a policy-independent bound, human performance or difficulty,
  shipping or retargeting approval, optimality, and a MAP-Elites conclusion.
- No ledger or generated-view correction was required or made.
- `invalidated_verification_entries`:
  - `PASS — record scope and proof class`
  - `PASS — non-claims`
  - `PASS — generated views`
  - `PASS — scope`

  These executor entries were filed under the nonexistent result identity and
  require caller re-verification against
  `5013dc571937333689ba6e8db8829b2f37fcfae1`.

## Verification

- **PASS — record scope and proof class.** RESULT-0027 names the exact evaluator,
  candidate, receipt, calibration, archive, seed ranges, and replay outcomes;
  fitting/holdout outcomes remain `heuristic_observation`, while deterministic
  identities and complete fixed-seed replays are `exact_result`.
- **PASS — non-claims.** The record explicitly excludes shipping the 102,000
  candidate, Level 53 retargeting, human difficulty, optimality, and a
  MAP-Elites result.
- **PASS — generated views.** `node tools/verify-universe-map.js` printed
  `UNIVERSE MAP PASS`; `node tools/build-universe-map.js --check` printed
  `UNIVERSE MAP BUILD CHECK PASS`.
- **PASS — scope.** The evidence commit changes only `EVIDENCE_LEDGER.md` and
  `universe/resolved.json`; `git diff --check e95b115...` passed and the
  worktree is clean.

## Feedback

None.

## Risks

The result admits evaluator reproducibility, not player-facing qualification.
The active 102,000 candidate still requires the owner's play judgment before
any shipping decision.

## Handoff

Ready for the ticket's fresh checker. Terminal status remains caller-owned.

## Caller adjudication

Accepted at the corrected result identity
`5013dc571937333689ba6e8db8829b2f37fcfae1`. After the checker invalidated all
four executor verification entries because they named a nonexistent expanded
hash, the caller rebound and reran them against the actual commit: the diff is
exactly `EVIDENCE_LEDGER.md` plus `universe/resolved.json`; the base has zero
and the result has one `RESULT-0027`; `git diff --check` passes; both Universe
checks pass; candidate verification reproduces identity `a6c3e360...` and
196/0/0/300 holdout totals; and all 53 focused evaluator, authoring, human-pilot,
recording-replay, and seed-variance tests pass. T-004 is complete.
