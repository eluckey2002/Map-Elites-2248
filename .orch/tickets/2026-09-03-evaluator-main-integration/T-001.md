---
id: T-001
run: 2026-09-03-evaluator-main-integration
status: failed
executor: orch-build
independence: checker
depends_on: []
write_scope:
  - docs/goals/evaluator-foundation/ATTACK-01.md
  - docs/goals/evaluator-foundation/ATTACK-02.md
  - docs/goals/evaluator-foundation/BUILD_CLAIMS.md
  - docs/goals/evaluator-foundation/CONTRACT.md
  - docs/goals/evaluator-foundation/INTENT_BRIEF.md
  - docs/goals/evaluator-foundation/PRE_LEDGER_CHECK.md
  - docs/goals/evaluator-foundation/VERDICT.md
  - solver/calibration.js
  - solver/calibrations/calib-1.js
  - solver/candidate-levels.json
  - solver/candidate-levels.receipt.json
  - solver/level-author.js
  - solver/tests/calibration.test.js
  - solver/tests/evaluatorBoundary.test.js
  - solver/tests/levelAuthor.test.js
excluded_actions:
  - edit EVIDENCE_LEDGER.md before independent integration verification passes
  - change src/game.js, src/index.html, solver/engine.js, solver/bot.js, shipped levels, targets, game rules, or live-bot behavior
  - push, open or update a pull request, merge, or delete worktrees
bound: 45 minutes
claimed_by: evaluator_main_integration_gpt_5_6_sol_high
claimed_at: 2026-09-03T03:03:00-05:00
---

## Objective

Integrate the independently verified evaluator foundation from `feat/evaluator-foundation` onto current `origin/main` without changing the shipped game or live bot, producing a clean candidate receipt and a result ready for independent checking and ledger admission.

## Fixed inputs

- Current remote base: `origin/main` at `8de076a000ead141d45f2a34629f51ea5aa1cbfe` after the 2026-09-03 fetch.
- Historical evaluator result: `feat/evaluator-foundation` at `5b6f92de7fc9b364c204d807fbb7b4108d38911e`.
- Historically verified code identity: `3a2f5bb02724cf9e7483eaf2c16992b3fd530a8b`.
- Frozen contract: `docs/goals/evaluator-foundation/CONTRACT.md` at `5b6f92d...`.
- Historical cold evidence: `PRE_LEDGER_CHECK.md` and `VERDICT.md` at `5b6f92d...`.
- Current-main facts that must be preserved: Level 53 is already shipped at target 101,000; current ledger IDs run through `RESULT-0026`; current `solver/engine.js` and `solver/bot.js` have post-freeze improvements.
- Current-main baseline: the full suite has nine pre-existing failures under a byte-preserving checkout—three experiment-fixture failures, three deliberately retained stale candidate-receipt failures, one linked-worktree dirty-state guard failure caused by preserved `.orch` tickets, and two Universe Map/current-view failures. Integration must introduce no additional failure or alter these failure subjects.

## Completion test

1. **Protected-main identity — deterministic, pre-existing.** `git diff --exit-code 8de076a... -- src/game.js src/index.html solver/engine.js solver/bot.js` exits 0.
2. **Independent evaluator boundary — deterministic, pre-existing.** `solver/level-author.js` imports `./calibrations/calib-1`, the evaluator imports no live bot, the evaluator-boundary negative control fails for a planted live-bot import, and the real boundary tests pass.
3. **Historical behavior — deterministic, pre-existing.** The original 450-game plus 8-fixture base-equivalence oracle passes on the integrated evaluator; live-bot fault injection cannot change its candidate/receipt output.
4. **Reproducible gen-0014 receipt — deterministic, pre-existing.** Clean author, verify, and second author preserve tracked bytes; independent replay returns target 102,000, fitting median 107,904, and holdout 196 wins / 104 out-of-moves / 0 lockouts / 0 bombs. New current-main code identities may differ and must be recorded rather than forced to old hashes.
5. **Current-main regression health — deterministic, pre-existing.** Every evaluator-specific test passes; `node solver/verify-loop.js` exits 0; the full suite introduces no failure beyond the nine fixed baseline subjects recorded above. `node tools/verify-universe-map.js` is reported separately because its generated view is already stale on the untouched base; if ledger admission changes the bound evidence identity, refresh its normal generated outputs rather than suppressing the gate.
6. **Evidence history — evidence, pre-existing.** Historical contract/check/verdict files remain byte-identical to `5b6f92d...`; no existing ledger record is edited or overwritten. Ledger admission remains outside executor authority.
7. **Bounded change set — deterministic, pre-existing.** Every changed path relative to `8de076a...` is inside `write_scope`; no untracked or unrelated work is included.

## Return fields

- `status`
- `result_identity`
- `changed_artifacts`
- `verification` with PASS/FAIL/UNVERIFIED for criteria 1-7
- `known_failures`
- `risks`

## Result

In progress. Baseline pinned before implementation: task worktree HEAD is
`8de076a000ead141d45f2a34629f51ea5aa1cbfe` on
`integrate/evaluator-foundation-20260903`, with worktree-local
`core.autocrlf=false`. Historical source `5b6f92d` and verified code identity
`3a2f5bb` are available locally.

## Verification

- Criterion 1: **PASS (baseline)** — protected-path diff against `8de076a...`
  exits 0.
- Criterion 2: **UNVERIFIED** — implementation not yet integrated.
- Criterion 3: **UNVERIFIED** — implementation not yet integrated.
- Criterion 4: **UNVERIFIED** — candidate not yet re-authored.
- Criterion 5: **PASS (baseline characterization)** — curve gate passes 53/53.
  Full suite reports 321 pass / 8 fail: three experiment fixtures, three stale
  candidate receipts, the linked-worktree `.orch` guard, and the generated-view
  test. The separately run Universe Map gate fails only on pre-existing
  `universe/resolved.json` drift.
- Criterion 6: **UNVERIFIED** — historical documents not yet integrated.
- Criterion 7: **PASS (baseline)** — task worktree is clean at the fixed base.

## Feedback

[]

## Risks

[]

## Join

- **disposition:** rejected(caller)
- **blame:** caller under-supplied a false fixed input: the full-suite baseline is 321 pass / 8 fail, not nine failures. The standalone Universe Map verifier is a separate failing command.
- **integrated state:** no implementation changes; task worktree remains clean at `8de076a...`.
