---
id: T-003
run: 2026-09-03-evaluator-main-integration
status: completed
executor: orch-repair
independence: checker
checked_by: evaluator_integration_checker
depends_on:
  - T-002
write_scope:
  - solver/tests/receiptGate.test.js
  - solver/seed-variance.js
  - solver/tests/seedVariance.test.js
  - solver/candidates-archive/README.md
  - solver/candidates-archive/candidate-levels-gen0014-wide-sprint-043ca53f.json
  - solver/candidates-archive/candidate-levels-gen0014-wide-sprint-043ca53f.receipt.json
excluded_actions:
  - change src/game.js, src/index.html, solver/engine.js, solver/bot.js, solver/level-author.js, solver/calibration.js, or solver/calibrations/calib-1.js
  - change either active gen-0014 candidate artifact or any recording
  - weaken, skip, delete, or broadly exempt an existing test or threshold
  - edit EVIDENCE_LEDGER.md, push, open or update a pull request, merge, or delete worktrees
bound: 30 minutes
claimed_by: evaluator_integration_repair
claimed_at: 2026-09-03T02:56:24-05:00
---

## Objective

Repair the four accepted current-main integration defects from T-002 without changing gameplay, the live bot, the frozen evaluator, either active candidate artifact, or historical recordings. Preserve the shipped 101,000 gen-0014 identity as replayable history while the evaluator's 102,000 gen-0014 remains the active authoring candidate.

## Fixed inputs

- Clean implementation worktree: `C:\ooo\Map-Elites-2248-evaluator-integration-20260903`.
- Fixed input commit: `02178de9dcc89305fd83c240fe76a3da7b4ef914` on `integrate/evaluator-foundation-20260903`.
- T-002 handoff is the accepted defect report and evidence source.
- Base shipped store and receipt must be copied byte-identically from `8de076a000ead141d45f2a34629f51ea5aa1cbfe:solver/candidate-levels.json` and `...:solver/candidate-levels.receipt.json`. Their candidate identity is `043ca53f234d4092202677b3e48a1855c0194c208742bba94a2c75ebd2227f16`, target 101,000.
- Frozen evaluator contract C5 requires every non-test production caller of `playMeasured` to use `calib-1`; seed variance therefore follows the frozen evaluator, not an alternate live-bot play seam.
- Full-suite baseline on untouched `8de076a...` was 321 pass / 8 fail. The active candidate receipt has since been made current, so a repaired suite may have fewer failures; it must have no failure outside the original eight baseline subjects.

## Accepted defects and deciding oracles

1. **Hashed dependency closure.** `solver/tests/receiptGate.test.js` fails `every file the measurement depends on is one of the hashed inputs` because its hard-coded map still names `bot.js` and omits the frozen calibration dependencies. Repair the assertion and negative control so the closure exactly covers the current `defaultInputIdentities()` and local require graph. Oracle: the receipt-gate file passes its closure and drift negative controls without weakening them.
2. **Shipped Level 53 evidence identity.** The receipt-gate test `the real exemption facts hold for the shipped corpus` incorrectly asks the new active 102,000 store to prove the three historical wins recorded against 101,000. Point the factual assertion at the archived 101,000 store/receipt and keep the computed shipped-plus-winning-recording rule intact. Oracle: the test passes and a wrong identity remains non-exempt.
3. **Historical candidate preservation.** The three Level 53 recordings became orphans when the active store moved to the new 102,000 identity. Add the two named archive files as byte-identical base copies and document their exact provenance and the rule that their bot measurements are stale/non-quotable while their board, target, and human replays remain valid. Oracles: SHA-256/byte comparison with `git show 8de076a...` passes; `humanPilot.test.js` and `recordingReplay.test.js` pass with orphan ceiling zero; no recording changes.
4. **Seed-variance evaluator identity and fixture.** The real-seam fixture was selected under the former live-bot behavior and now fails under the contractually frozen `calib-1`. Keep the production path on `solver/level-author.js#playMeasured`, make its covered identity map truthfully include every evaluator dependency that feeds measurement, and update only the bounded real-seam fixture inputs as needed to demonstrate the unchanged `REAL_FIXTURE_RULE`; do not lower its thresholds. Oracles: `seedVariance.test.js` passes, its valid/broken controls remain PASS/FAIL, and a calibration identity mutation fails closed.

## Completion test

1. All deciding oracles above pass.
2. `node --test solver/tests/calibration.test.js solver/tests/evaluatorBoundary.test.js solver/tests/levelAuthor.test.js solver/tests/receiptGate.test.js solver/tests/humanPilot.test.js solver/tests/recordingReplay.test.js solver/tests/seedVariance.test.js` exits 0 except any pre-existing deliberately stale candidate receipt tests emitted by the receipt-gate corpus walk; there are no new failure subjects.
3. `node solver/verify-loop.js` exits 0 with 53/53 and final `RESULT: PASS`.
4. The complete test suite has no failure outside the original eight T-002 baseline subjects.
5. `node tools/verify-universe-map.js` has no failure beyond the pre-existing `universe/resolved.json` drift.
6. `git diff --exit-code 8de076a... -- src/game.js src/index.html solver/engine.js solver/bot.js solver/level-author.js solver/calibration.js solver/calibrations/calib-1.js` shows only the already accepted T-002 evaluator paths and no T-003 change to any excluded implementation.
7. Every T-003 change relative to `02178de...` is inside `write_scope`; worktree is clean after one repair commit.

## Return fields

- `status`
- `result_identity`
- `changed_artifacts`
- per-defect disposition with rerun evidence
- `known_failures`
- `risks`

## Result

`status`: **executor PASS; ready for the required independent checker and
caller-owned join.** Terminal ticket status remains caller-owned.

`result_identity`: repair commit
`e95b1159ea496f0d78a9d821a1f2928f2b7af82b` on
`integrate/evaluator-foundation-20260903`, with a clean worktree ten commits
ahead of `origin/main`.

`changed_artifacts`:

- `solver/tests/receiptGate.test.js`
- `solver/seed-variance.js`
- `solver/tests/seedVariance.test.js`
- `solver/candidates-archive/README.md`
- `solver/candidates-archive/candidate-levels-gen0014-wide-sprint-043ca53f.json`
- `solver/candidates-archive/candidate-levels-gen0014-wide-sprint-043ca53f.receipt.json`

Per-defect dispositions:

1. **Hashed dependency closure — repaired.** The receipt gate now distinguishes
   direct input identities (`engine`, `levelAuthor`) from the frozen calibration
   solver identity (`engine.js` plus `calibrations/calib-1.js`), walks nested
   `./` and `../` local requires, and pins `calibration.js` as the sole
   receipt-only dependency. The planted `heuristics.js` dependency is still
   detected, and the drift control now mutates the real `engine` identity rather
   than the removed live-bot identity. The closure and both negative controls
   passed in the 72/74 focused rerun; only the two named stale corpus receipts
   failed.
2. **Shipped Level 53 identity — repaired.** The computed real exemption now
   reads the archived 101000 store and receipt and returns `exempt: true` from
   the real shipped-level and winning-recording sets. The active 102000 store is
   separately asserted to be `{ exempt: false, ships: true, played: false }`, so
   the wrong identity remains non-exempt.
3. **Historical candidate preservation — repaired.** Both archive files have
   the exact Git blob identities of the base inputs:
   `1307a3c7873b01782989af594a6be347169b04b7` for the store and
   `4b244c82c6c7555271d234d70c7a28fae680bebd` for the receipt. Their SHA-256
   identities are respectively
   `0b046cfe11090e2b8a3836e0964913800c115b2a4d16f558acc08b725bdfedda`
   and
   `261ad85e9566974cf709429bd566a627a4fe5413db0d15e90fe8efa0ec8a6e0b`.
   All ten recordings now replay, the three restored Level 53 wins replay
   exactly, and the orphan ceiling is zero. The README records exact provenance
   and bars the archived receipt's stale measurements from quotation.
4. **Seed-variance evaluator identity and fixture — repaired.** Production still
   uses `solver/level-author.js#playMeasured`. Covered identities now include
   `calibration.js` and `calibrations/calib-1.js` and no longer name the live
   bot. Only the bounded fixture's sample-B start changed, from `9200000` to
   `9200004`; candidates, counts, seam, and thresholds are unchanged. The real
   fixture measured Pearson `0.9992542534356237` and single-seed reliability
   `0.9928220176636481`, so it passes the unchanged `> 0.99` and `> 0.98`
   thresholds. Its reversed twin measured Pearson `-0.8543764782788204` and
   reliability `0`, so it fails. A named `calibrationSolver` identity mutation
   also fails closed with `covered identity mismatch`.

`known_failures`: the full solver suite reported 333 tests, 326 pass, 7 fail.
All seven are original-baseline subjects: three experiment-registration
fixtures, the deliberately stale Level 52 and Level 54 receipts, the linked
worktree dirty-`.orch` guard, and the generated Universe Map view. The formerly
stale active candidate receipt is current, so this is one fewer failure than the
eight-failure base count. No new failure subject remains.

`risks`: historical seed-variance artifacts retain the evaluator identities
they actually measured and were not restamped. Any claim that needs a fresh
current-evaluator seed-variance result requires a newly registered measurement;
this repair establishes gate behavior, not a new empirical result.

### Independent checker findings — `evaluator_integration_checker`

- Checked result identity `e95b1159ea496f0d78a9d821a1f2928f2b7af82b`
  as the single clean commit after fixed input
  `02178de9dcc89305fd83c240fe76a3da7b4ef914`. Its six changed paths are
  exactly the six paths in `write_scope`; no protected implementation path or
  recording changed, and `git diff --check` produced no finding.
- The repaired receipt-closure assertion is not self-fulfilling: it independently
  recomputes both `defaultInputIdentities()` file hashes and the calibration
  solver identity, then compares those identities with the recursively reached
  static local-require graph. Cold source inspection reached `engine.js`,
  `calibrations/calib-1.js`, and the separately named receipt-only
  `calibration.js`; `calib-1.js` itself reaches only `engine.js`. Treating
  `calibration.js` as receipt-only is consistent with frozen CONTRACT C3, which
  defines the solver identity as exactly engine plus `calib-1`. The staged
  `heuristics.js` edge and the engine-identity drift control exercise distinct
  failure paths.
- The archived Level 53 store and receipt have Git blob identities
  `1307a3c7873b01782989af594a6be347169b04b7` and
  `4b244c82c6c7555271d234d70c7a28fae680bebd`, exactly equal to the two fixed
  `8de076a...` source blobs. The live factual check derives shipped levels and
  winning recording identities rather than trusting an exemption field: the
  archived 101000 identity is exempt, while the active unplayed 102000 identity
  remains non-exempt. All ten real recordings replayed and the unknown-identity
  control remained an orphan; no recording byte changed. A direct archive walk
  found all five archived receipts stale with `calibration stamp mismatch`, as
  documented, so no historical bot measurement became quotable accidentally.
- The seed-variance production seam remains
  `solver/level-author.js#playMeasured`. Relative to the fixed input, only the
  bounded fixture's sample-B start moved (`9200000` to `9200004`); candidates,
  sample counts, execution seam, and `0.99` / `0.98` thresholds did not change.
  Independent recomputation produced Pearson `0.9992542534356237` and
  reliability `0.9928220176636481`; the same-predicate reversed twin produced
  Pearson `-0.8543764782788204` and reliability `0`. Both the unit entitlement
  mutation and the end-to-end bundle verifier reject a changed
  `calibrationSolver` identity with `covered identity mismatch`.
- The exact focused command reproduced `74 tests / 72 pass / 2 fail`, with only
  the named stale Level 52 and Level 54 receipts. The curve gate exited 0 with
  `53/53` and final `RESULT: PASS`. The complete suite reproduced
  `333 tests / 326 pass / 7 fail`, with exactly the seven executor-named
  baseline subjects. The standalone Universe Map verifier reported only
  `generated output drift: universe/resolved.json`.
- No checker correction was required or made.
- `invalidated_verification_entries`: `[]`.

## Verification

1. **PASS — deciding oracles.** Receipt closure/drift controls, computed
   exemption facts, human-pilot identity challenge, all ten recording replays,
   zero-orphan ratchet, calib-1 real fixture, reversed twin, and calibration
   identity mutation all produced their required outcomes.
2. **PASS with named baseline debt — focused regression.** The exact completion
   command reported 74 tests, 72 pass, and only
   `candidate-levels-52.json has a receipt...` plus
   `candidate-levels-54.json has a receipt...` failed as deliberately stale.
3. **PASS — curve gate.** `node solver/verify-loop.js` exited 0, reported
   `53/53`, all seven checks PASS, and final `RESULT: PASS`.
4. **PASS — full regression identity.** `node --test solver/tests/*.test.js`
   reported 326/333 with exactly the seven allowed baseline subjects named in
   `known_failures`; none lies in the repaired subject set.
5. **PASS with named baseline debt — Universe Map.** The standalone verifier
   reported only `generated output drift: universe/resolved.json`.
6. **PASS — protected paths.** Diff from `02178de...` is empty for every excluded
   implementation path; diff from `8de076a...` is empty for
   `src/game.js`, `src/index.html`, `solver/engine.js`, and `solver/bot.js`.
   No recording changed.
7. **PASS — bounded change set.** All six changed paths are in `write_scope`,
   `git diff --check 02178de...` passed, archive Git blobs match the fixed base,
   and the worktree is clean after the single repair commit.

Weakest oracle class is deterministic executor evidence. Because the repair
changed checks as well as code, final acceptance remains pending the ticket's
fresh checker.

## Feedback

- The ticket omitted the `Verification`, `Feedback`, and `Risks` body sections
  required by the work-item contract. They were added here without widening
  workspace scope; the omission was logged as orchflows friction.

## Risks

- The seven named baseline failures remain intentionally unresolved here.
- Archived receipt measurements are stale; only their candidate board, target,
  identity, provenance, and human replays remain valid.

## Handoff

None. The repair is committed and ready for independent checking.

## Caller adjudication

Accepted at `e95b1159ea496f0d78a9d821a1f2928f2b7af82b` after the fresh checker
reported no finding and invalidated no verification entry. The caller then
independently confirmed the six-path scope, clean diff, exact base/archive Git
blob equality, 53/53 focused evaluator/replay/seed-variance tests, the receipt
gate's 19 passes plus only the two named stale receipts, and direct candidate
verification at identity
`a6c3e36031a17276354ba0d8b16e796d4338038fb984a581ffbe96cb9c3387f7`
with holdout 196 wins / 0 lockouts / 0 bombs / 300 total. T-003 is complete;
the seven named baseline failures remain outside this repair.
