---
id: T-002
run: 2026-09-03-evaluator-main-integration
status: suspended
executor: orch-build
independence: checker
depends_on: []
write_scope:
  - docs/goals/evaluator-foundation/ATTACK-01.md
  - docs/goals/evaluator-foundation/ATTACK-02.md
  - docs/goals/evaluator-foundation/BUILD_CLAIMS.md
  - docs/goals/evaluator-foundation/CONTRACT.md
  - docs/goals/evaluator-foundation/INTENT_BRIEF.md
  - docs/goals/evaluator-foundation/PRE_LEDGER_CHECK.md
  - docs/goals/evaluator-foundation/VERDICT.md
  - solver/calibration.js
  - solver/calibrations/calib-1.js
  - solver/candidate-levels.json
  - solver/candidate-levels.receipt.json
  - solver/level-author.js
  - solver/tests/calibration.test.js
  - solver/tests/evaluatorBoundary.test.js
  - solver/tests/levelAuthor.test.js
excluded_actions:
  - edit EVIDENCE_LEDGER.md before independent integration verification passes
  - change src/game.js, src/index.html, solver/engine.js, solver/bot.js, shipped levels, targets, game rules, or live-bot behavior
  - push, open or update a pull request, merge, or delete worktrees
bound: 45 minutes
claimed_by: evaluator_main_integration_gpt_5_6_sol_high_1
claimed_at: 2026-09-03T03:10:00-05:00
---

## Objective

Integrate the independently verified evaluator foundation from `feat/evaluator-foundation` onto current `origin/main` without changing the shipped game or live bot, producing a clean candidate receipt and a result ready for independent checking and ledger admission.

## Fixed inputs

- Current remote base: `origin/main` at `8de076a000ead141d45f2a34629f51ea5aa1cbfe` after the 2026-09-03 fetch.
- Historical evaluator result: `feat/evaluator-foundation` at `5b6f92de7fc9b364c204d807fbb7b4108d38911e`.
- Historically verified code identity: `3a2f5bb02724cf9e7483eaf2c16992b3fd530a8b`.
- Frozen contract and historical evidence: the seven files under `docs/goals/evaluator-foundation/` at `5b6f92d...`.
- Current-main facts to preserve: Level 53 already ships at target 101,000; ledger IDs run through `RESULT-0026`; `solver/engine.js` and `solver/bot.js` contain post-freeze improvements.
- Byte-preserving current-main baseline: full suite 321 pass / 8 fail. Fixed failure subjects are three experiment fixtures, three stale candidate receipts, one linked-worktree `.orch` guard, and one generated-view test. The separate Universe Map verifier fails only for `universe/resolved.json` drift. Curve gate passes 53/53.

## Completion test

1. **Protected-main identity — deterministic, pre-existing.** `git diff --exit-code 8de076a... -- src/game.js src/index.html solver/engine.js solver/bot.js` exits 0.
2. **Independent evaluator boundary — deterministic, pre-existing.** `solver/level-author.js` imports `./calibrations/calib-1`; that evaluator imports no live bot; the planted live-bot-import negative control fails for the intended reason; all real boundary tests pass.
3. **Historical behavior — deterministic, pre-existing.** The original 450-game plus 8-fixture base-equivalence oracle passes on the integrated evaluator, and live-bot fault injection cannot change its candidate or receipt output.
4. **Reproducible gen-0014 receipt — deterministic, pre-existing.** Clean author, verify, and second author preserve tracked bytes; independent replay returns target 102,000, fitting median 107,904, and holdout 196 wins / 104 out-of-moves / 0 lockouts / 0 bombs. Current-main code identities are recorded honestly.
5. **Current-main regression health — deterministic, pre-existing.** Every evaluator-specific test passes; curve gate exits 0; the full suite has no failure beyond the eight named baseline subjects. Universe Map remains separately reported; executor does not refresh it before ledger admission.
6. **Evidence history — evidence, pre-existing.** Historical contract/check/verdict files are byte-identical to `5b6f92d...`; no existing ledger record is edited or overwritten.
7. **Bounded change set — deterministic, pre-existing.** Every changed path relative to `8de076a...` is inside `write_scope`; no unrelated or untracked work is included.

## Return fields

- `status`
- `result_identity`
- `changed_artifacts`
- `verification` with PASS/FAIL/UNVERIFIED for criteria 1-7
- `known_failures`
- `risks`

## Result

Suspended for scope adjudication at commit `02178de9dcc89305fd83c240fe76a3da7b4ef914` in clean workspace `C:\ooo\Map-Elites-2248-evaluator-integration-20260903` on `integrate/evaluator-foundation-20260903`. The historical evaluator and seven goal documents are integrated; gen-0014 re-authors reproducibly at target 102,000. The full-suite no-new-failure criterion cannot be completed inside the stamped write scope.

Result identities:

- commit: `02178de9dcc89305fd83c240fe76a3da7b4ef914`
- candidate: `a6c3e36031a17276354ba0d8b16e796d4338038fb984a581ffbe96cb9c3387f7`
- receipt: `3b83de41726b57d5d39d5c83bb44fda412fb6413da7bdaa7aaf11a7db8953bc1`
- calibration solver: `c3cefaf3ab0387180349948ba1adc9a0737cc07ae1ebfcdf51b1e8c0396f77ed`
- engine input: `0ed4b31004df13e3eae45b1cd0ad692f5956c636630b89e1f96f068e5a451873`
- level-author input: `c0eef4a582ea0c61e3d8b9601320e53266016785318334301a46ef29f76b04f4`

## Verification

- Criterion 1: **PASS** — `git diff --exit-code 8de076a... -- src/game.js src/index.html solver/engine.js solver/bot.js` exited 0 after integration.
- Criterion 2: **PASS** — all 16 evaluator/calibration/level-author tests passed; the compliant boundary control exited 0; the planted `./bot` control exited 1 with `production authoring must import ./calibrations/calib-1 and not ./bot`; the evaluator's only local import is `../engine`.
- Criterion 3: **PASS** — the unchanged historical probe printed `PASS calib-1 base equivalence: 450 games + 8 fixtures`; a module-loader fault proven by a separate `LIVE_BOT_FAULT_INJECTION` negative control left bounded candidate and receipt output byte-identical (`storeSha256=f32bba2c...`, `receiptSha256=1822f861...`).
- Criterion 4: **PASS** — clean author and verify returned target 102,000 and holdout 196 wins / 104 out-of-moves / 0 lockouts / 0 bombs; fitting median is 107,904; a second author preserved store SHA-256 `d2b6bf08...` and receipt SHA-256 `fc01c439...` with clean status. The historical independent replay reproduced all 150 fitting and 300 holdout games and the exact identities through an EOL-neutral read wrapper; its unwrapped strict formatting check is incompatible with this ticket's required `core.autocrlf=false` because the pre-existing shape is LF while authored output is CRLF.
- Criterion 5: **FAIL** — evaluator-specific tests pass, but the full suite is 317 pass / 12 fail rather than the fixed 321 pass / 8 fail. The integration clears the previously stale `candidate-levels.json` receipt but introduces five failure subjects across four omitted dependency/corpus areas, a net increase of four failures. The curve gate was started but stopped without a verdict when scope suspension was ordered. The standalone Universe verifier was not run.
- Criterion 6: **PASS** — all seven goal-document blob identities exactly match `5b6f92d...`; `EVIDENCE_LEDGER.md` has no diff from `8de076a...`.
- Criterion 7: **PASS** — the worktree is clean and every one of the 15 changed paths relative to `8de076a...` is in `write_scope`.

## Feedback

- The delegated `orch-build` executor contract is for library/custom skill, pack, composition, or contract items, while this ticket integrates project evaluator code. The caller explicitly required `orch-build`; execution follows the ticket's narrower implementation contract and records the routing mismatch as friction.
- The historical independent replay's byte-format assertion assumes CRLF for every JSON file on Windows, but the required `core.autocrlf=false` checkout preserves the pre-existing shape as LF. The EOL-neutral wrapper altered only that read-time formatting comparison; all independent gameplay, identity, measurement, canonical JSON, and terminal-count checks remained active.
- Preserving the historical seven-parameter `calib-1` stamp is intentional. Current-main-only `offerFull` and `pathWidth` are mutated in the live-bot fault test but are not added to the frozen evaluator contract.

## Risks

- Historical evaluator code predates current-main engine and bot improvements; adaptation must preserve evaluator isolation without overwriting protected current-main implementations.
- Current commit is not admissible while criterion 5 fails. Do not edit the ledger, push, merge, or discard the clean integration commit before scope adjudication.

## Handoff

Current state: clean worktree, branch `integrate/evaluator-foundation-20260903`, HEAD `02178de9dcc89305fd83c240fe76a3da7b4ef914`, nine commits ahead of `origin/main`, worktree-local `core.autocrlf=false`. No protected source or ledger file changed. The interrupted curve-gate process exited 1 only because it was stopped after the suspension instruction and produced no verdict.

Four uncovered dependency/corpus gaps require paths outside the current write scope:

1. **Hashed dependency closure** — `solver/tests/receiptGate.test.js`, test `every file the measurement depends on is one of the hashed inputs`, still hardcodes `bot.js`, `engine.js`, and `level-author.js`. It must be updated to cover `solver/calibration.js` and `solver/calibrations/calib-1.js` and to remove live `bot.js` from the authoring closure. Current failure: `defaultInputIdentities() changed shape`.
2. **Level 53 exemption semantics** — `solver/tests/receiptGate.test.js`, test `the real exemption facts hold for the shipped corpus`, assumes the live `candidate-levels.json` identity is the human-played 101,000 candidate. The re-authored 102,000 candidate is intentionally a different, unplayed identity, so this assertion needs an explicit ruling rather than an implementation workaround.
3. **Historical candidate preservation** — add byte-identical copies of the base `8de076a...` 101,000 store and receipt at `solver/candidates-archive/candidate-levels-gen0014-wide-sprint-043ca53f.json` and `solver/candidates-archive/candidate-levels-gen0014-wide-sprint-043ca53f.receipt.json`, and document them in `solver/candidates-archive/README.md`. This restores identity `043ca53f...` for the three Level 53 recordings and resolves both `solver/tests/humanPilot.test.js` (`challenge refuses replay-compatible candidate content with the wrong identity`) and `solver/tests/recordingReplay.test.js` (`recordings whose candidate is gone are named, and their count cannot grow`). Do not replace or retarget those recordings.
4. **Seed-variance consumer semantics** — `solver/seed-variance.js` imports the now-frozen `playMeasured` from `solver/level-author.js`, while `solver/tests/seedVariance.test.js` has a real-play reliability fixture calibrated to the live bot. The failing subject is `a bounded artifact measured through real playMeasured reaches PASS and its broken twin reaches FAIL`. Scope owner must decide whether seed variance intentionally follows `calib-1` (then re-freeze its fixture/oracle) or retains a separate live-bot seam (then implement and identify that dependency outside level authoring).

The full-suite baseline's stale `candidate-levels.json` receipt failure is now correctly gone, so the five introduced subjects above yield 12 total failures rather than 8 (net +4). Remaining work after scope adjudication: resolve the four gaps without weakening their oracles, rerun evaluator tests and boundary controls, rerun the unchanged 450+8 probe and independent replay, run `node solver/verify-loop.js` to completion, rerun the full suite and confirm only the eight named baseline subjects, run the standalone Universe verifier and confirm only pre-existing `universe/resolved.json` drift, recheck criteria 1/6/7, then send the clean result to an independent checker before any ledger admission.
