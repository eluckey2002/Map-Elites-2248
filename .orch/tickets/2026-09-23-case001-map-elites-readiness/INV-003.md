# INV-003 — Case 001 L1 readiness after protocol repair

## Verdict

`PASS`

The sole concrete incompatibility identified by the prior L1 gate has been repaired and the complete protocol-before-run verification now passes on the target Windows host. The repository authority, intended source cuts, unit dispositions, integration plan, and next experiment identities remain as recorded in `INV-002.md`.

## Repair identity

- `tools/verify-experiments.js`
  - Added one Git-boundary normalization function that converts `\` to `/`.
  - Applied it to both `git cat-file <sha>:<path>` and `git show <sha>:<path>` object expressions.
  - Filesystem paths remain host-native.
- `solver/tests/experiments.test.js`
  - Added a regression test proving that a known committed protocol is readable when the caller supplies a Windows-separator relative path.

## Verification

- Focused regression before repair: FAIL; the valid committed protocol read returned `null`.
- Focused regression after repair: PASS; 1/1.
- Full protocol suite: PASS; 32/32.
- Live experiment gate: `EXPERIMENT GATE PASS`, exit 0.
- Existing negative controls remain green, including missing/unregistered, malformed, placeholder, rewritten, reopened, already-reported, forged, unreachable, and out-of-order protocols.
- `git diff --check`: PASS.
- Scoped status inspection found no source change outside `tools/verify-experiments.js` and `solver/tests/experiments.test.js`; authority and experiment contents were not changed.

## L1 readiness gate

1. Canonical base and planned branch: resolved in `INV-002.md`.
2. Reusable oracle and MAP-Elites capability source cuts: resolved in `INV-002.md`.
3. U1/U4/U2/U3/U5/U6/U7 conformance matrix: resolved in `INV-002.md`.
4. Integration conflicts and path-scoped reuse strategy: resolved in `INV-002.md`.
5. Next candidate result identities: `RESULT-0049` and `RESULT-0050`, not reserved.
6. Protocol-before-run behavior: demonstrated on the target Windows host by the passing full suite and live gate.

The state is sufficiently resolved to begin U1 when authorized. This record does not begin U1, create a branch, merge a lineage, or reserve an experiment identity.

## Preserved Case 001 framework findings

1. **Identity binding:** controls must refer to authoritative, sufficiently disambiguated state.
2. **Control realization:** controls that govern execution must be demonstrated to function in the actual target execution environment.

## Framework observation

L1 was sufficient. The uncertainty was resolved by compiling repository identities into one authority map and executing the existing control on its real host. No challenge/repair loop against the handoff artifact was needed beyond repairing the concrete control-realization defect exposed by L1 verification.

## Contradictions

- None newly introduced. The commit-qualified historical identity conflicts remain preserved and accounted for by the path-scoped integration plan in `INV-002.md`.

## Gaps left by the bound

- The selected implementation branch has not been created.
- The repair has not been committed or published.
- U1 has not begun.
- No experiment identity has been reserved.
