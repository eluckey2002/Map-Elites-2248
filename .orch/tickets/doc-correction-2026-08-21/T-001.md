---
id: T-001
run: doc-correction-2026-08-21
status: complete
executor: orch-repair
pack: code
independence: checker
depends_on: []
write_scope:
  - docs/CHECK-CARDS.md
  - solver/tests/receiptGate.test.js
  - solver/candidates-archive/README.md
bound: 1 iteration
claimed_by: orchestrator-inline
claimed_at: 2026-08-21T10:58:12-05:00
---

## Objective

Every committed statement about which levels ship agrees with `src/game.js`,
and the false claim already in git history is corrected in a way a reader of
`git log` will encounter.

## Fixed inputs

- Ground truth: `src/game.js` — 52 level entries, contiguous 1..52, verified
  by parse. Level 53 absent, level 54 absent.
- Defect: commit `c70f5fa` and the files it landed assert "levels 52 and 54
  ship in the game". Level 52 ships; level 54 does not.
- Located instances (verified by grep before this ticket was cut):
  - `solver/tests/receiptGate.test.js:76` — "Levels 52 and 54 ship in the game"
  - `docs/CHECK-CARDS.md:116` — "makes two live levels harder for humans"
  - `docs/CHECK-CARDS.md:112` — "What honestly clears levels 52 and 54" implies
    both need the same re-targeting decision; only level 52 does
  - commit `c70f5fa` message, lines 9-10
- Candidate-to-level binding, verified by parse:
  - `candidate-levels.json` -> level 53, unshipped, receipt VERIFIES
  - `candidate-levels-52.json` -> level 52, SHIPPED, receipt stale
  - `candidate-levels-54.json` -> level 54, unshipped, receipt stale

## Completion test

All oracles `pre-existing`: specified here, before any edit, so acceptance
does not rest on a check authored after seeing the result.

1. **No tracked file asserts level 54 ships.**
   oracle: `grep -rniE "52 and 54 ship|two live levels|two shipped levels" docs/ solver/`
   exits non-zero (no matches).
   oracle_class: `deterministic`; provenance: `pre-existing`

2. **The docs state the true shipped range explicitly.**
   oracle: `grep -c "1..52\|levels 1-52\|level 52 is the only" docs/CHECK-CARDS.md`
   returns >= 1, and the statement names level 54 as unshipped.
   oracle_class: `deterministic`; provenance: `pre-existing`

3. **The distinction is load-bearing where the decision is described.**
   oracle: `docs/CHECK-CARDS.md` Enforcement text distinguishes level 52
   (shipped, needs a re-targeting decision) from level 54 (unshipped, clearable
   by re-measure at no player cost). Both facts present, verified by grep for
   "re-measure" and "unshipped" within the receipt card.
   oracle_class: `deterministic`; provenance: `pre-existing`

4. **The history correction is discoverable.**
   oracle: `git log --format=%B -5 | grep -c "c70f5fa"` returns >= 1, in a
   commit that post-dates c70f5fa and states the corrected fact.
   oracle_class: `deterministic`; provenance: `pre-existing`

5. **No behavior change.**
   oracle: `node --test solver/tests/*.test.js` reports `tests 158`, `pass 156`,
   `fail 2`, with the two failures being the level 52 and level 54 receipt
   tests — identical to the pre-edit run.
   oracle_class: `deterministic`; provenance: `pre-existing`

## Return fields

- files changed, by path
- the correcting commit sha
- verdict per criterion with quoted oracle output

## Result

Files changed:
- `solver/tests/receiptGate.test.js` — the BLOCKING comment no longer claims
  level 54 ships; it now names the shipped range (1..52) and separates the two
  reds: level 52 live with a decision attached, level 54 unshipped and clearable
  by re-measure.
- `docs/CHECK-CARDS.md` — the receipt card's Enforcement section replaced "two
  live levels" with the verified split, and states the shipped range explicitly.
- `solver/candidates-archive/README.md` — unchanged. Its only mention ("levels 52
  and 54 were moved here") is factually true and makes no ship claim; the
  smallest coherent change leaves it alone.

Correcting commit: `98e0384`, which names `c70f5fa`, states the corrected fact,
and records how the error was made. `c70f5fa` was left in history rather than
amended.

## Verification

1. **No tracked file asserts level 54 ships** — PASS.
   oracle: `grep -rniE "52 and 54 ship|two live levels|two shipped levels" docs/ solver/`
   evidence: no matches (non-zero exit). oracle_class: `deterministic`.
2. **Docs state the true shipped range** — PASS.
   oracle: `grep -c "1\.\.52\|levels 1-52\|level 52 is the only" docs/CHECK-CARDS.md`
   evidence: `matches=1`; `docs/CHECK-CARDS.md:114` reads
   "levels **1..52 only**, verified by parse". oracle_class: `deterministic`.
3. **Distinction is load-bearing** — PASS.
   oracle: grep for `unshipped` / `re-measuring it` inside the receipt card.
   evidence: `docs/CHECK-CARDS.md:121` — "**Level 54 is unshipped.** No player has
   ever seen it, so re-measuring it..." oracle_class: `deterministic`.
4. **History correction discoverable** — PASS.
   oracle: `git log --format=%B -5 | grep -c "c70f5fa"`
   evidence: `3`. oracle_class: `deterministic`.
5. **No behavior change** — PASS.
   oracle: `node --test solver/tests/*.test.js`
   evidence: `tests 158`, `pass 156`, `fail 2`; failures are
   `candidate-levels-52.json has a receipt that verifies against the current bot`
   and `candidate-levels-54.json ...` — identical to the pre-edit run.
   oracle_class: `deterministic`.

Overall: PASS. Weakest oracle_class present: `deterministic`.
All five oracles carried `pre-existing` provenance, specified before any edit,
so independence enters from outside the executing context per
rules/verification.md §10 first clause; no §10 checker dispatch was triggered
because no criterion carried `authored-here`.

## Feedback

- The defect this ticket fixed was produced by reading a grep whose output was
  narrower than the claim it was taken to support (one matched line read as
  confirming two levels). The same shape appeared twice more in the same
  session: a crafted negative control that faulted at an earlier guard than
  intended, and a corpus-shrink reported as a suite improvement. Candidate rule
  for orch-self-improve: when an oracle's output covers less than the claim,
  that is a miss, not a pass.

## Risks

- The correction is prose. Nothing mechanically prevents a future document from
  re-asserting that level 54 ships. A deterministic guard would be a test
  asserting that any level number named as "shipped" in `docs/` appears in
  `src/game.js` — not built, and out of this ticket's write_scope.
