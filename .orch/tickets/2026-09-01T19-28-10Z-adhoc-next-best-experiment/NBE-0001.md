---
id: NBE-0001
run: 2026-09-01T19-28-10Z-adhoc-next-best-experiment
status: limited
executor: orch-investigate
depends_on: []
write_scope: []
excluded_actions:
  - modify project evidence, code, protocols, experiments, backlog, or current-state records
  - run an experiment or claim that an unexecuted design is validated
  - infer owner priorities not explicitly recorded
  - generalize beyond the frozen repository evidence
bound: one recommendation, at most three serious alternatives, and twelve targeted primary-source reads
claimed_by: root
claimed_at: 2026-09-01T19:28:10Z
---

# NBE-0001 — Determine the next best experiment

## Objective

At frozen `main` commit `4dd93219f69d5288654dd2aee395f6e6388bda4a`, identify the one experiment that best reduces the current load-bearing decision uncertainty, explain why it outranks at most three serious alternatives, and state the prerequisites and interpretation boundary without executing it.

## Fixed inputs

- Question: Given where the project is now, what is the next best experiment?
- Source policy: tracked local repository evidence at frozen commit `4dd93219f69d5288654dd2aee395f6e6388bda4a`; `EVIDENCE_LEDGER.md` controls evidence standing, followed by `CURRENT.md` and their cited primary artifacts. Untracked work may be reported as current workspace state but cannot change proof standing.
- Bound: twelve targeted primary-source reads after the required ledger/current orientation; one recommendation and at most three alternatives; no experiment execution or project mutation.
- Required comparison fields: decision unlocked, current evidence, unresolved uncertainty, prerequisites, approximate execution burden if recorded, interpretation rule, and reason for rank.

## Completion test

1. The answer identifies the active direction and load-bearing unresolved decision from the frozen ledger/current records, with exact local citations.
   - Oracle: cited lines in `EVIDENCE_LEDGER.md`, `CURRENT.md`, and their primary linked records at the frozen commit.
   - oracle_class: evidence
   - provenance: pre-existing
2. The recommended experiment is compared against no more than three serious alternatives using every required comparison field, and contradictions are preserved rather than reconciled by assertion.
   - Oracle: direct comparison against the frozen protocol, report, ticket, run, and backlog sources cited for each candidate.
   - oracle_class: evidence
   - provenance: pre-existing
3. The return distinguishes evidence-backed facts from the recommendation, names dead ends and gaps, and does not claim the recommendation itself is experimentally verified.
   - Oracle: the `orch-investigate` return contract and this frozen ticket.
   - oracle_class: judged
   - provenance: pre-existing

## Return fields

- status
- result identity
- verification
- recommendation and decision unlocked
- ranked alternatives
- cited findings and confidence
- contradictions
- dead ends
- gaps and bound left uncovered

## Result

### Status

Completed within the source and read bounds. Formal acceptance remains limited because criterion 3 names a fresh judged oracle, while this run was explicitly kept inline with no independent judge.

### Recommendation

Do not launch another generated-level batch from current `main`. After two deterministic preconditions are repaired, run a preregistered **generated-level human acceptance calibration pilot**:

1. Freeze one already accepted level as an anchor and two structurally different, independently verifier-accepted generated candidates selected by a declared rule rather than by human outcome.
2. Have the owner play each candidate on two fresh, blinded seeds in randomized order. Add a third seed only where the first two conflict.
3. Before play, freeze the owner's small acceptance rubric and the interpretation rules.
4. Decide whether seed-to-seed variation overwhelms candidate differences and whether either generated candidate clears the anchor-defined acceptance threshold.

This is the smallest experiment that directly answers the active decision: whether generated levels deserve scaled execution and what human acceptance bar should govern the shortlist.

### Deterministic preconditions

1. `solver/calibration.js` declares `calib-1` as the frozen authoring ruler, but `solver/level-author.js` imports only live `chooseMove` and invokes it without calibration parameters. A read-only stub probe observed only `lookaheadRngFactory`; no calibration parameters reached the real measurement call. The 15 focused calibration/generator tests passed because none checks this call path.
2. `solver/generate-levels.js` applies `verifyCandidate` after constructing `passed`, but does not remove verification disagreements from that array. It prints and returns them while also saying the set “passed every gate.” The committed batch-04 artifact contains 15 gate-passing entries, five with `verifierConfirmed: false`, matching `HANDOFF.md`'s recorded mid-run code drift.

These are implementation defects or explicit instrument-choice decisions, not empirical uncertainties. The experiment should not begin until the authoring ruler is actually bound and verifier-rejected candidates cannot enter the shortlist.

### Ranked alternatives

1. **Fresh-seed Level 54 playtest.** Cheapest, and it resolves the narrow question of the current 85%-demand candidate. It ranks second because the current Level 54 identity has no bound human recording and the result would not establish the generator's general acceptance bar.
2. **Another 60-shape / 12-full generator batch.** Mechanically cheap after repair, but ranks third. Four exploratory committed batches already requested 510 shapes, and another batch would create more candidates without deciding what deserves human time.
3. **Another MAP-Elites policy round.** Ranks fourth because level authoring is the active milestone, while accepted MAP-Elites evidence found behavior diversity but no holdout winner and the later Universe artifact is not ledger-admitted.

### Cited findings

- High confidence: `CURRENT.md:7-13` names level authoring as active and the open choice as generator scale plus acceptance bar.
- High confidence: `EVIDENCE_LEDGER.md:341-352` and `.orch/runs/level-authoring-tracer-2026-08-12/worklog.md:96-102` show that Level 51 shipping depended on replayed human strategies and an owner judgment that bot win rate alone did not supply.
- High confidence: `solver/generate-levels.js:171-184` explicitly labels bot-win-rate ranking a thin proxy that ranks but does not judge.
- High confidence: `solver/generate-levels.js:193-318` defines a 60-shape / 12-full default, a 450-game pipeline, verifier confirmation, and the defective unchanged `passed` set.
- High confidence: `solver/level-author.js:14,121-138,181-232` uses the live bot for fitting and holdout while `solver/calibration.js:1-28,33-54` declares a frozen ruler intended to prevent exactly that drift.
- High confidence as direct source, not admitted result: `solver/generated-batch-01.json` through `-04.json` contain 120, 120, 150, and 120 requested shapes respectively. No batch has a ledger entry or experiment protocol on current main.
- High confidence: `experiments/README.md:7-17,51-60` requires a committed protocol before a generalizing heuristic experiment.

### Contradictions

- `calibration.js` says the ruler is separated from the live player; the current authoring implementation does not use it.
- The generator says verifier-rejected candidates should not be shown, then retains them in the printed and returned passed shortlist.
- `CURRENT.md` frames the choice as whether to run the generator “at scale,” but four non-admitted exploratory batches already exist. The unresolved choice is more precisely whether to repeat under a trustworthy instrument and human acceptance protocol.
- `HANDOFF.md:217-219` reports cross-seed structural ranking reliability of `r = 0.98`, but no ledger record or underlying recomputation artifact was located inside the bound. It remains a lead, not accepted project evidence.

### Dead ends

- `UNIVERSE.md`'s current frontier concerns MAP-Elites evidence admission and generalization, not the active level-authoring decision.
- `BL-0004` still points to executing the already-completed tracer, so its `Next action` is stale for this question.
- A current-state ticket pointed to a generated-corpus preregistration on another branch. The frozen source policy is current `main`, so that branch was not used as authority.

### Gaps and bound

- The owner has not recorded a durable acceptance rubric for generated levels. The pilot must elicit and freeze it; this result does not invent it.
- Exact candidate identities are intentionally not selected until the instrument and independent-verifier filter are trustworthy.
- No generator batch, receipt refresh, human play, or full-suite run was performed.
- The recommendation is a decision-value judgment, not an experimental result.

## Verification

1. **PASS — active direction and decision.** Exact citations resolve to frozen tracked sources at `4dd93219f69d5288654dd2aee395f6e6388bda4a`. Oracle class: evidence.
2. **PASS — bounded comparison.** One recommendation and three alternatives are compared by decision unlocked, evidence, prerequisites, burden, and interpretation. Contradictions are retained. Oracle class: evidence.
3. **UNVERIFIED — return-shape judgment.** The result explicitly separates evidence-backed facts from recommendation, dead ends, and gaps, but no fresh independent judged context was permitted or run. Oracle class: judged.

Overall: `UNVERIFIED` at the formal ticket level; the evidence findings and focused 15-test output are directly reproducible. Focused command: `node --test solver/tests/generateLevels.test.js solver/tests/calibration.test.js` -> 15 pass, 0 fail. The green result does not cover the two real call-path defects above.

## Feedback

- This case required reconstructing active intent, ledger standing, human decision evidence, generator behavior, historical batch artifacts, receipt drift, calibration intent, and experiment-policy enforcement before an experiment could be ranked.
- The highest-value missing capability exposed here is a decision-scoped impact view: for “next best experiment,” resolve the active decision, current instrument identity, admissible prior evidence, known invalidations, and execution prerequisites together.

## Risks

- Candidate selection and rubric design remain owner-sensitive; hard-coding either from old prose would convert a recommendation into an invented decision.
