---
id: EOL-001
run: 2026-09-23-u1-l1-corrections
status: complete
executor: orch-tdd
independence: gate
depends_on: []
write_scope:
  - .gitattributes
  - prototypes/bank-and-break/serve.js
excluded_actions:
  - modify tests, hashed runtime files, receipts, generated views, evidence records, experiments, or any path outside write_scope
  - weaken, skip, delete, or reclassify a failing test
  - merge branches, push, open a pull request, merge a pull request, or contact the user
  - begin U2 through U7, run a reportable experiment, or redesign Progressive Rigor
bound: 60 minutes or 45 tool calls, whichever binds first
claimed_by: eol_001_gpt_5_6_sol_high
claimed_at: 2026-09-23T18:04:53.8520946Z
profile: orch-worker
---

## Objective

Restore the Windows checkout to the repository's protected four-failure baseline by declaring LF materialization for identity-bearing text and making the bank-and-break recordings path separator-stable, without rewriting any receipt or weakening any check.

## Fixed inputs

- Isolated workspace: `C:\OOO\Map-Elites-2248-u1-eol`.
- Branch/base: `codex/u1-eol-path@5048f88245907f1178706f4b65c8895ca08313c8`.
- Failure partition and exact consumers: `C:\OOO\Map-Elites-2248\.orch\tickets\2026-09-23-u1-baseline-reconciliation\INV-001.md`.
- Expected protected red controls after correction: stale `candidate-levels-52.json`, stale `candidate-levels-54.json`, Universe generated-view freshness, and Universe date drift. These four must remain visible.
- Craft reference: existing repository `.gitattributes` conventions if present, Git attributes documentation already embodied by the repository's LF Git blobs, and existing Node path handling patterns.

## Completion test

1. Repository text files with identity-bearing raw-byte checks materialize as LF in a fresh Windows checkout, without changing their Git blobs or receipts. Oracle: `git check-attr text eol` for representative `*.js`, `*.json`, `*.md`, and `*.html`; raw SHA-256 in a fresh detached worktree equals the commit-qualified Git/LF identities cited by the failure partition. Oracle class: deterministic. Provenance: pre-existing.
2. The bank-and-break launcher exposes stable `/`-separated logical recording paths while retaining valid host filesystem paths. Oracle: the pre-existing `solver/tests/bankAndBreakValidation.test.js` passes unchanged. Oracle class: deterministic. Provenance: pre-existing.
3. The implicated Windows identity suites have no failures beyond the four protected controls. Oracle: unchanged targeted tests `humanPilot.test.js`, `humanPilot0002.test.js`, `receiptGate.test.js`, and `universeMap.test.js` run in a fresh Windows worktree at the correction commit; exact failure-name comparison to the protected set in Fixed inputs. Oracle class: deterministic. Provenance: pre-existing.
4. The correction changes only `.gitattributes` and `prototypes/bank-and-break/serve.js`, preserves U1 result ancestry, and leaves the working branch clean and committed. Oracle: Git ancestry, path-scoped diff, `git diff --check`, and `git status --short --branch`. Oracle class: deterministic. Provenance: pre-existing.

## Return fields

- status
- result_identity
- verification
- changed_artifacts
- commits
- red_green_evidence
- failures_found
- cost_added
- risks

## Result

### Progress

- Claimed in isolated workspace `C:\OOO\Map-Elites-2248-u1-eol` on branch `codex/u1-eol-path` at clean baseline `5048f88245907f1178706f4b65c8895ca08313c8`.
- Read the fixed failure partition and confirmed the permitted implementation scope is exactly `.gitattributes` plus `prototypes/bank-and-break/serve.js`; no terminal ticket status will be set by this executor.
- EOL red: all four representative paths reported `text: unspecified`, `eol: unspecified`; their working-tree SHA-256 identities differed from `git show HEAD:<path>` under Windows `core.autocrlf=true` (including `solver/generated-batch-04.json` working `31649856...` versus Git/LF `5b0dd3bf...`).
- EOL local green: `.gitattributes` now reports `text: set`, `eol: lf` for itself and representative `*.js`, `*.json`, `*.md`, and `*.html`; `git diff --check` passed. Slice committed as `1ef6f4d6e21b40af39409bdadf223dddc3a035ed`.
- Path red: unchanged `node --test solver/tests/bankAndBreakValidation.test.js` produced 8 tests / 6 pass / 1 fail / 1 skip; the sole failure was `launcher keeps validation roles separate in ports and recording paths`, with each Windows path remaining one backslash-delimited segment.
- Path green: the unchanged targeted test produced 8 tests / 7 pass / 0 fail / 1 skip. Every exposed recording path uses `/`, retains the expected two logical tail segments, and remains host-absolute on Windows. Slice committed as `68b6cdba4e0175fa98c25bfad899ba7fb9d2475b`.
- Fixed result selected for completion verification: `68b6cdba4e0175fa98c25bfad899ba7fb9d2475b`. A temporary detached worktree at `C:\OOO\Map-Elites-2248-u1-eol-verify-68b6cdb` will be used only for fresh-checkout oracles and removed after evidence capture.
- Fresh-checkout probe at `68b6cdb...` found all cited identity-bearing files byte-equal to their Git blobs and the four targeted suites at exactly 44 pass / 4 protected fail. It also exposed two pre-existing CRLF Git blobs (`solver/candidate-levels.json` and its receipt) as dirty under the broad JSON rule; their working bytes still equaled Git, but the checkout-cleanliness oracle failed. Evidence at this identity is invalidated pending a two-path-scope `.gitattributes` exception and a full rerun.
- The first temporary worktree was removed. Exact `-text -eol` exceptions now preserve only those two legacy CRLF blobs; all identity-bearing representatives remain `text eol=lf`. Corrective slice committed as `ba3137c9a732cb64977ea06d44ba447f24e542f3`; this is the new fixed identity. All completion oracles passed in clean detached worktree `C:\OOO\Map-Elites-2248-u1-eol-verify-ba3137c`, which was then cleanly removed.

### Contracted return

- **status:** `complete` for this executor result; ticket frontmatter remains `claimed` for the join.
- **result_identity:** clean `codex/u1-eol-path@ba3137c9a732cb64977ea06d44ba447f24e542f3`, descendant of U1 result `5048f88245907f1178706f4b65c8895ca08313c8`.
- **verification:** overall **PASS** at weakest oracle class `deterministic`; criteria 1-4 are evidenced below. The four named protected red controls remain visible by contract and are not excess failures.
- **changed_artifacts:** `.gitattributes`; `prototypes/bank-and-break/serve.js`. No tests, receipts, hashed runtime files, generated views, evidence records, or experiments changed.
- **commits:** `1ef6f4d6e21b40af39409bdadf223dddc3a035ed` (`fix: enforce LF for identity-bearing text`); `68b6cdba4e0175fa98c25bfad899ba7fb9d2475b` (`fix: normalize bank-and-break recording paths`); `ba3137c9a732cb64977ea06d44ba447f24e542f3` (`fix: preserve legacy candidate JSON bytes`).
- **red_green_evidence:** EOL red was four representative file classes with unspecified attributes and Windows working SHA-256 unequal to Git/LF; green is `text eol=lf` plus byte-for-byte equality in a fresh checkout. Path red was the unchanged bank-and-break suite at 6 pass / 1 fail / 1 skip; green is 7 pass / 0 fail / 1 skip. Fresh-checkout cleanliness initially exposed two legacy CRLF blobs as dirty; exact exceptions made the rerun clean without changing either blob.
- **failures_found:** final identity suites produced exactly the protected set: stale `candidate-levels-52.json`, stale `candidate-levels-54.json`, Universe generated-view freshness, and Universe date drift. Excess failure count: `0`. One implementation edge was found and resolved: the repository has two intentionally CRLF JSON Git blobs that must not be normalized implicitly.
- **cost_added:** three small commits across two authorized files; two temporary detached verification worktrees created and removed; no dependency, runtime, test, receipt, generated artifact, or reportable-experiment cost.
- **risks:** the two exact legacy CRLF exceptions must be revisited if those blobs are intentionally renormalized in a separately authorized change; the four protected controls remain deliberately red and must continue to be interpreted as the expected baseline.


## Verification

### Criterion 1 — PASS

- **verdict:** `PASS`
- **oracle:** `git check-attr text eol` plus raw SHA-256 comparison between files materialized in detached worktree `C:\OOO\Map-Elites-2248-u1-eol-verify-ba3137c` and `git show ba3137c9a732cb64977ea06d44ba447f24e542f3:<path>`.
- **oracle_class:** `deterministic`
- **evidence:** `.gitattributes`, representative/identity-bearing JS, JSON, Markdown, and HTML reported `text: set`, `eol: lf`. Working bytes equaled commit bytes with no CRLF for `solver/engine.js` (`0ed4b310...`), `pilots/HUMAN-PILOT-0002/qualify.js` (`1254c948...`), `solver/generated-batch-04.json` (`5b0dd3bf...`), the Universe archive (`ab8ed417...`), `EVIDENCE_LEDGER.md` (`af9aa407...`), `CURRENT.md` (`841bab39...`), and `src/index.html` (`e0098178...`). The two explicit legacy exceptions also equaled their unchanged CRLF Git blobs, and the fresh worktree was clean.
- **covers:** base `5048f88245907f1178706f4b65c8895ca08313c8`; result `ba3137c9a732cb64977ea06d44ba447f24e542f3`; dependencies `[]`.

### Criterion 2 — PASS

- **verdict:** `PASS`
- **oracle:** unchanged `node --test solver/tests/bankAndBreakValidation.test.js`, supplemented by `path.isAbsolute` and separator/tail assertions over `createEntries()` in the fresh detached worktree.
- **oracle_class:** `deterministic`
- **evidence:** 8 tests / 7 pass / 0 fail / 1 intentional skip. All three recording paths were Windows-absolute, contained no backslash, and exposed `validation-2026-09-12/<role>-seed-<seed>` as their final two `/`-separated segments.
- **covers:** base `5048f88245907f1178706f4b65c8895ca08313c8`; result `ba3137c9a732cb64977ea06d44ba447f24e542f3`; dependencies `[]`.

### Criterion 3 — PASS

- **verdict:** `PASS`
- **oracle:** unchanged targeted suites `humanPilot.test.js`, `humanPilot0002.test.js`, `receiptGate.test.js`, and `universeMap.test.js` in the fresh detached worktree, followed by a TAP failure-name set comparison to the four frozen controls.
- **oracle_class:** `deterministic`
- **evidence:** 48 tests / 44 pass / 4 fail / 0 skip. The set comparator reported `EXPECTED_FAILURE_COUNT=4`, `ACTUAL_FAILURE_COUNT=4`, `SET_DELTA_COUNT=0`. Exact failures: `candidate-levels-52.json has a receipt that verifies against the current bot`; `candidate-levels-54.json has a receipt that verifies against the current bot`; `the builder is byte-stable and the committed generated views are current`; `verification observations derive from rebound evidence instead of copied metrics in code`. Fresh worktree remained clean after both runs.
- **covers:** base `5048f88245907f1178706f4b65c8895ca08313c8`; result `ba3137c9a732cb64977ea06d44ba447f24e542f3`; dependencies `[]`.

### Criterion 4 — PASS

- **verdict:** `PASS`
- **oracle:** `git merge-base --is-ancestor`, path-scoped `git diff --name-only`, `git diff --check`, commit log, and `git status --short --branch`.
- **oracle_class:** `deterministic`
- **evidence:** ancestry exit `0`; the baseline-to-result diff names only `.gitattributes` and `prototypes/bank-and-break/serve.js`; `git diff --check` exit `0`; three commits are recorded above; branch status is clean at `ba3137c9a732cb64977ea06d44ba447f24e542f3`.
- **covers:** base `5048f88245907f1178706f4b65c8895ca08313c8`; result `ba3137c9a732cb64977ea06d44ba447f24e542f3`; dependencies `[]`.

### Overall verdict

**PASS** at weakest oracle class `deterministic`.


## Feedback

- Before applying an extension-wide EOL rule, inventory tracked index EOLs with `git ls-files --eol`; exact legacy CRLF exceptions may be required to preserve both blob identity and clean fresh-checkout status.

## Risks

- Two exact candidate JSON paths intentionally remain byte-preserved CRLF because their Git blobs are CRLF; a future authorized normalization must change the blobs and remove the exceptions together.
- Four protected controls remain deliberately red; downstream verification must compare their exact names rather than require a zero test-process exit.
