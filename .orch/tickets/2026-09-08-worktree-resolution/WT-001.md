---
id: WT-001
run: 2026-09-08-worktree-resolution
status: claimed
executor: orch-repair
profile: orch-worker
depends_on: []
write_scope:
  - C:/OOO/Map-Elites-2248/.orch/tickets/
  - local branch refs archive/worktree-ticket-records-20260908, main, work/next-2026-09-05, work/next-2026-09-08
  - remote branch refs/heads/archive/worktree-ticket-records-20260908
  - C:/OOO/Map-Elites-2248 checkout branch and index
  - C:/OOO/map-elites-2248-next checkout branch and index
  - C:/Users/eluck/AppData/Local/Temp/me2248-eval-fault-live
  - C:/Users/eluck/AppData/Local/Temp/me2248-eval-fault-negative
  - Git worktree administrative entries corresponding exactly to those two Temp paths
excluded_actions:
  - force-push
  - modify or push main
  - merge the archive branch into main
  - publish any source, gameplay, ledger, experiment, or unrelated working-tree change
  - delete any ticket record before its exact blob is reachable from the pushed archive branch
  - delete or modify any path outside the two named Temp directories
bound: 45 minutes
claimed_by: worktree_resolution_gpt_5_6_sol_high
claimed_at: 2026-09-08T14:48:50-05:00
---

## Objective

Leave the repository with two valid, clean worktrees based on current remote main, preserve every presently untracked ticket byte in a pushed archive branch, and remove the two proven-disposable broken Temp worktree remnants and registrations.

## Fixed inputs

- Main repository: `C:/OOO/Map-Elites-2248`; initial branch `feat/evaluator-foundation` at `5b6f92de7fc9b364c204d807fbb7b4108d38911e`.
- Current remote main: `9d125e8c94f41282388a90402b1fba0b22ae83a8` at cut time. Recheck immediately before mutations; suspend if it changed.
- Clean linked worktree: `C:/OOO/map-elites-2248-next`, branch `work/next-2026-09-05`, HEAD `bb9d39ce2f8a474ff93443be18ced882f87ab6fb`, zero branch-only commits against remote main.
- Untracked preservation set: every file returned at claim time by `git -C C:/OOO/Map-Elites-2248 ls-files --others --exclude-standard -- .orch/tickets/**`, including this ticket. Earlier inspection found 20 files across 15 directories; recompute rather than trust the count.
- Every pre-existing untracked ticket checked before cut had no exact blob at the same path on any live local branch, remote branch, or tag.
- Disposable paths: `C:/Users/eluck/AppData/Local/Temp/me2248-eval-fault-live` and `C:/Users/eluck/AppData/Local/Temp/me2248-eval-fault-negative`. Each contains 10 files / 27,631,331 bytes, with no modified or untracked file relative to commit `ac5a3f2badefdbf2081695d6cc89794869702b79`; each lacks 183 tracked files and has no `.git` entry. Git marks both registrations `prunable gitdir file points to non-existent location`.
- The user authorized resolving the worktree issues after receiving the exact proposed cleanup.
- Workspace identity: shared Git repository rooted at `C:/OOO/Map-Elites-2248`; second linked workspace `C:/OOO/map-elites-2248-next`; no pack binding.
- reply_to: main.

## Completion test

1. Every file in the claim-time preservation set has the same Git blob identity at the same path on pushed `origin/archive/worktree-ticket-records-20260908`; an exact manifest of path and blob identity is included in Result. Oracle: `git hash-object --path=<path> <file>` captured before staging versus `git rev-parse origin/archive/worktree-ticket-records-20260908:<path>` after push. oracle_class: deterministic. provenance: pre-existing.
2. The archive branch derives from the original `feat/evaluator-foundation` HEAD, contains only the preservation-set additions, and is pushed without force. Oracle: `git merge-base --is-ancestor 5b6f92d archive/worktree-ticket-records-20260908`, `git diff --name-status 5b6f92d..archive/worktree-ticket-records-20260908`, `git ls-remote origin refs/heads/archive/worktree-ticket-records-20260908`. oracle_class: deterministic. provenance: pre-existing.
3. `C:/OOO/map-elites-2248-next` is clean on non-default branch `work/next-2026-09-08`, its HEAD equals the rechecked `origin/main`, and obsolete local `work/next-2026-09-05` is absent only after proving it has no branch-only commits. Oracle: `git status --short --branch`, `git rev-parse HEAD`, `git rev-parse origin/main`, pre-deletion `git rev-list --right-only --count origin/main...work/next-2026-09-05`, and `git show-ref --verify`. oracle_class: deterministic. provenance: pre-existing.
4. Both named Temp paths are absent, neither appears in `git worktree list --porcelain`, and the two remaining registered paths resolve as Git worktrees. Before deletion, resolved absolute targets must be proven below `C:/Users/eluck/AppData/Local/Temp` and their file inventories must reproduce the fixed input. Oracle: PowerShell `Resolve-Path`, `Test-Path`, recomputed tracked/untracked/blob comparison, `git worktree prune`, `git worktree list --porcelain`, and `git -C <path> rev-parse --is-inside-work-tree`. oracle_class: deterministic. provenance: pre-existing.
5. No repository source or evidence content changed, remote main stayed at its rechecked identity, and the primary checkout is clean on the archive branch pending caller integration. Oracle: `git status --short --branch`, `git diff --name-status 5b6f92d..HEAD`, and closing `git ls-remote origin refs/heads/main`. oracle_class: deterministic. provenance: pre-existing.

## Return fields

- status
- changed_artifacts
- preservation manifest and archive identities
- per-criterion PASS/FAIL with exact oracle output
- final worktree inventory
- feedback
- risks
- queued work, if any

## Result

Pending.

## Verification

Pending.

## Feedback

[]

## Risks

[]
