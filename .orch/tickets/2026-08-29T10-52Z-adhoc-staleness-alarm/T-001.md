---
id: T-001
run: 2026-08-29T10-52Z-adhoc-staleness-alarm
status: complete
executor: orch-tdd
pack: code
independence: checker
depends_on: []
write_scope:
  - CURRENT.md
  - UNIVERSE.md
  - universe/resolved.json
  - universe/map.html
bound: 30 minutes
claimed_by: orchestrator-inline
claimed_at: 2026-08-29T11:13:13+00:00
---

## Objective

`CURRENT.md` states the level generator exists and that Level 53 ships, and the
three generated Universe Map views agree with a fresh rebuild.

## Fixed inputs

- Workspace: `.orch/runs/2026-08-29T10-29-19Z-adhoc-session-workspace/worktree`,
  branch `codex/2026-08-29T10-29-19Z-adhoc-session-workspace`, base `9016690`.
- Before-identities: `.orch/runs/2026-08-29T10-52Z-adhoc-staleness-alarm/evidence/before-hashes.txt`
- The two rotted statements, `CURRENT.md:11`, written 2026-08-20 in `32b90b3`
  and true at that revision.
- Disconfirming evidence, by identity:
  - `solver/generate-levels.js` — added 2026-08-20 in `355dc5a`.
  - `src/game.js` contains 53 levels; `solver/tests/gameLevels.test.js:9`
    asserts `LEVELS.length === 53`; level 53 entered in `530deb3` (2026-08-28).
- Rebuild command: `node tools/build-universe-map.js`.

## Completion test

Each oracle was executed against the pre-work state and returned the FAIL
reading shown, so none can pass vacuously.

1. `grep -c 'which does not exist' CURRENT.md` == 0.
   oracle: shell; oracle_class: deterministic; provenance: pre-existing.
   Pre-work reading: 1 (FAIL).
2. `grep -c 'correctly rejected' CURRENT.md` == 0.
   oracle: shell; oracle_class: deterministic; provenance: pre-existing.
   Pre-work reading: 1 (FAIL).
3. `grep -c 'solver/generate-levels.js' CURRENT.md` >= 1.
   oracle: shell; oracle_class: deterministic; provenance: pre-existing.
   Pre-work reading: 0 (FAIL).
4. `grep -Ec 'Level 53 ships|53 ships' CURRENT.md` >= 1.
   oracle: shell; oracle_class: deterministic; provenance: pre-existing.
   Pre-work reading: 0 (FAIL).
5. `node tools/verify-universe-map.js` exits 0 — committed views agree with a
   fresh rebuild. oracle: repo tool; oracle_class: deterministic;
   provenance: pre-existing. Pre-work reading: 0 (PASS). This criterion is a
   regression guard, not a flip: editing `CURRENT.md` without rebuilding
   changes `currentNavigationSha256` and drives it to FAIL.
6. `node --test solver/tests/*.test.js` — the set of failing test names is
   exactly {candidate-levels.json, candidate-levels-52.json,
   candidate-levels-54.json receipt verification}, unchanged from
   `.orch/runs/2026-08-29T10-29-19Z-adhoc-session-workspace/baseline-tests.txt`.
   oracle: node test runner; oracle_class: deterministic;
   provenance: pre-existing.

## Return fields

- `changed` — files changed, by path and after-sha256.
- `verdicts` — one per criterion.

## Result

Corrected two statements in `CURRENT.md` that were true when written on
2026-08-20 (`32b90b3`) and had since rotted, then rebuilt the three generated
Universe Map views with `node tools/build-universe-map.js`.

What changed, and why each is now true:

- The generator claim. Was "a level generator (invents shapes on its own),
  which does not exist". `solver/generate-levels.js` was added later the same
  day in `355dc5a`. The text now names the file and describes the two-stage
  screen. `HANDOFF.md:206`, the file this paragraph tells the reader to open,
  already said the generator was preserved — the navigation page contradicted
  its own pointer.
- The level 53 claim. Was "one correctly rejected (53, real lockouts)".
  `src/game.js` carries 53 levels and `solver/tests/gameLevels.test.js:9`
  asserts `LEVELS.length === 53`; level 53 entered in `530deb3` on 2026-08-28.
  The text now states it ships.
- Level 54 standing left intact and stated explicitly: still unshipped, playtest
  still void, receipt still one of the three the suite reports stale. It is NOT
  retired on this branch — `solver/candidate-levels-54.json` is present.
- Recorded, not resolved: level 53's move from rejected to shipped has no
  `RESULT-` or `DECISION-` record in `EVIDENCE_LEDGER.md`. Queued to the run
  worklog as owner work.
- Review date set to 2026-08-29, the date this review actually happened.

Identities (sha256), before -> after:

| file | before | after |
| --- | --- | --- |
| `CURRENT.md` | `3a0d11ad…0794f` | `2dffde19…ab645` |
| `UNIVERSE.md` | `96b1414e…4e3b53` | `036fe7d9…debdd9f` |
| `universe/resolved.json` | `a1092068…a7558` | `2cedac28…26d721` |
| `universe/map.html` | `d7332a03…55705d` | `d7332a03…55705d` (unchanged — the page renders the standing, not the date) |

Nothing outside `write_scope` was touched.

## Verification

| # | criterion | oracle | class | verdict | evidence |
| --- | --- | --- | --- | --- | --- |
| 1 | no "which does not exist" | `grep -c` | deterministic | PASS | 1 -> 0; `evidence/t001-c1-c4.txt` |
| 2 | no "correctly rejected" | `grep -c` | deterministic | PASS | 1 -> 0; `evidence/t001-c1-c4.txt` |
| 3 | names `solver/generate-levels.js` | `grep -c` | deterministic | PASS | 0 -> 1; `evidence/t001-c1-c4.txt` |
| 4 | states level 53 ships | `grep -Ec` | deterministic | PASS | 0 -> 1; `evidence/t001-c1-c4.txt` |
| 5 | committed views agree with rebuild | `node tools/verify-universe-map.js` | deterministic | PASS | exit 0; `evidence/t001-c5-verify-universe.txt` |
| 6 | failing test set unchanged | `node --test solver/tests/*.test.js` | deterministic | PASS | 233 pass; failing set byte-identical to baseline; `evidence/t001-c6-suite.clean.txt` |

Overall: PASS. Weakest oracle_class present: deterministic.

Independence per rules/verification.md §10 source 1: every criterion's oracle
carries `pre-existing` provenance, specified and exercised against the
known-bad state before any edit.

## Feedback

- Criterion 6's first measurement read `pass = 0` with no failures listed. The
  suite had in fact passed 233. The count was `grep -c '^OK-MARK'` over output
  that carried ANSI colour codes on this run but not on the identical earlier
  runs, so the anchor never matched. A count oracle whose zero is
  indistinguishable from "matched nothing" is the same failure class this run
  exists to remove. Logged to friction; both frictions this run are that shape.

## Risks

- `universe/contract.json` sets `maximumAgeDays: 7`. The review date now reads
  2026-08-29, so the alarm re-arms on 2026-09-05. T-002 is what makes that
  alarm testable; without it the date is the only thing holding.
