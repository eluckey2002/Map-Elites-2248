---
id: T-002
run: 2026-08-29T10-52Z-adhoc-staleness-alarm
status: complete
executor: orch-tdd
pack: code
independence: checker
depends_on: []
write_scope:
  - solver/tests/universeMap.test.js
bound: 30 minutes
claimed_by: orchestrator-inline
claimed_at: 2026-08-29T11:15:20+00:00
---

## Objective

The stale-navigation path is covered by a test that forces the stale condition
in a fixture, and the comment claiming coverage that does not exist is gone.

## Fixed inputs

- Same workspace as T-001.
- The false comment: `solver/tests/universeMap.test.js:90` — "the stale path is
  covered by the fails-closed tests below". Verified false: no test in the
  repository exercises `navigationAgeDays > maximumAgeDays`.
- Production code under test: `tools/universe-map-core.js:208` (the
  `current-navigation-stale` warning) and `:301` (the `currentNavigation`
  standing field). Limit: `universe/contract.json` `maximumAgeDays: 7`.
- Available fixture pattern: `makeFixture(t)`, `solver/tests/universeMap.test.js:29`,
  which copies `CURRENT.md` into a temp tree — used by 8 sibling tests.

## Completion test

1. `grep -c 'the stale path is covered by the' solver/tests/universeMap.test.js` == 0.
   oracle: shell; oracle_class: deterministic; provenance: pre-existing.
   Pre-work reading: 1 (FAIL).
   NOTE: the pattern is a single-line fragment on purpose. The full sentence
   wraps across two comment lines and a whole-sentence grep silently returns 0,
   which would read PASS while the defect stood.
2. A test named `the stale-navigation warning fires when CURRENT.md ages past
   the contract limit` exists and passes under
   `node --test solver/tests/universeMap.test.js`.
   oracle: node test runner; oracle_class: deterministic; provenance: pre-existing.
   Pre-work reading: absent (FAIL).
3. Non-vacuity by mutation: with `tools/universe-map-core.js` mutated to delete
   the `current-navigation-stale` warning push, the test from criterion 2 FAILS;
   with the mutation reverted it passes, and
   `shasum -a 256 tools/universe-map-core.js` returns
   `2975fe4d7fa97b2a3399216bf5348bd05d19faaaf356e4eebbb7cfda8b9c14e4`.
   oracle: mutation run + hash; oracle_class: deterministic; provenance: pre-existing.
   This is the criterion that distinguishes a real test from the one being replaced.
4. `node --test solver/tests/*.test.js` — failing test names unchanged from
   baseline (the same three receipt-gate failures), and total test count is
   baseline + the tests added here.
   oracle: node test runner; oracle_class: deterministic; provenance: pre-existing.

## Return fields

- `changed` — files changed, by path and after-sha256.
- `verdicts` — one per criterion.
- `mutation_evidence` — the failing output under mutation.

## Result

Added one test, `the stale-navigation warning fires when CURRENT.md ages past
the contract limit`, to `solver/tests/universeMap.test.js`, and corrected the
comment that claimed coverage which did not exist.

The test uses the file's existing `makeFixture(t)` helper, which copies
`CURRENT.md` into a temp tree, then rewrites the fixture's `Last reviewed` line
and calls `resolveUniverse` twice:

- at `maximumAgeDays + 1` days before `contract.asOf` — asserts the standing
  reads `stale`, the `current-navigation-stale` warning is raised, and its
  message carries the computed age;
- at exactly `maximumAgeDays` — asserts the standing reads `current` and the
  warning is absent.

Both directions are asserted from a real state change, so the test cannot pass
by never computing either branch — which is precisely how the assertion it
replaces was passing.

The limit and `asOf` are read from `universe/contract.json` rather than
hard-coded, so changing the contract's freshness policy does not silently
neuter the test.

The corrected comment now names the covering test instead of pointing at the
fails-closed tests, and says plainly that the previous claim was wrong.

Identity (sha256): `solver/tests/universeMap.test.js`
`c4e7b9db…fc7c14` -> `e77d9ae1…5d3ffa`.

Production code was mutated twice during verification and restored; its hash is
unchanged at `2975fe4d…c14e4`.

## Verification

| # | criterion | oracle | class | verdict | evidence |
| --- | --- | --- | --- | --- | --- |
| 1 | false comment gone | `grep -c 'the stale path is covered by the'` | deterministic | PASS | 1 -> 0 |
| 2 | named test exists and passes | `node --test solver/tests/universeMap.test.js` | deterministic | PASS | 16 pass / 0 fail; `evidence/t002-c2.clean.txt` |
| 3 | non-vacuity by mutation | mutation run + hash | deterministic | PASS | see mutation table below |
| 4 | failing set unchanged, count +1 | `node --test solver/tests/*.test.js` | deterministic | PASS | 234 pass (baseline 233); failing set byte-identical; `evidence/t002-c4-suite.clean.txt` |

Mutation evidence for criterion 3 — the new test must FAIL when the behaviour
it claims to cover is removed:

| mutation to `tools/universe-map-core.js` | new test | pre-existing tests |
| --- | --- | --- |
| A: `current-navigation-stale` warning never pushed (:208) | FAIL (caught) | all three PASS (blind) |
| B: `currentNavigation` hard-coded to `'current'` (:301) | FAIL (caught) | — |
| restored | PASS | PASS |

The middle column of row A is the finding that justified this ticket. With the
staleness warning deleted from production code, `one resolved model keeps
admitted evidence separate from the later verified artifact`, `the builder is
byte-stable and the committed generated views are current`, and `generated
Markdown exposes the load-bearing distinctions in one screen` all reported
green. The suite could not see the defect at all. Evidence:
`evidence/t002-blindness.txt`.

Overall: PASS. Weakest oracle_class present: deterministic.

Independence per rules/verification.md §10 source 1: every criterion's oracle
carries `pre-existing` provenance, specified and exercised against the
known-bad state before the test was written. No `authored-here` criterion
exists on this ticket, so the `independence: checker` field gates nothing and
no `checked_by` pass is required.

## Feedback

- The replaced assertion was not merely weak, it was unfalsifiable in the
  repository's normal state: warning-absent and standing-`current` made it read
  `false === false`. The invariant it encodes is worth keeping, and is kept —
  what it needed was a second test that forces the other branch.
- Retaining the invariant assertion AND adding the branch test means neither
  pins the repository to being stale, which was the original complaint, and the
  behaviour is still covered.

## Risks

- The test pins the message format `"<n> days before <asOf>"`. A wording change
  in the warning breaks it. Judged worth it: asserting the computed age is what
  stops a warning that fires with a wrong or empty age from passing.
