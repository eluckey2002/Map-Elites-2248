---
id: WS-01
run: 2026-09-05-main-worktree
status: limited
executor: orch-workspace
pack: orch-code-pack
depends_on: []
write_scope:
  - C:/ooo/map-elites-2248/.git remote-tracking refs and local main ref
  - C:/ooo/map-elites-2248/.git worktree registration and work/next-2026-09-05 ref
  - C:/ooo/map-elites-2248-next
bound: 15 minutes; one fetch and one baseline gate, with safe repair only if needed
claimed_by: /root
claimed_at: 2026-09-05
excluded_actions:
  - No push, commit, merge, branch deletion, force update, or modification of existing checkout files
---

## Objective

Bring local main up to the remote main tip and create a clean worktree from it, preserving the existing feature checkout.

## Fixed inputs

- User request: pull latest remote Git to update main, then create a worktree from that.
- Repository: C:/ooo/map-elites-2248; origin https://github.com/eluckey2002/Map-Elites-2248.git.
- Existing checkout: feat/evaluator-foundation at 5b6f92de7fc9b364c204d807fbb7b4108d38911e; tracked files clean, twelve existing untracked ticket directories preserved.
- Starting main: 7f697d30d6e85b05a80ece9ce65912707314038e; main is not checked out in any registered worktree.
- Target workspace: C:/ooo/map-elites-2248-next, absent at cut time; branch work/next-2026-09-05, absent at cut time.
- Workspace cell: orch-code-pack git worktree isolation; provenance is latest fetched main revision.
- Use core.autocrlf=false for initial materialization to preserve committed source bytes; no persistent Git configuration change.

## Completion test

1. Local main equals fetched origin/main and live remote main, and starting main is an ancestor. Oracle: git rev-parse, git ls-remote, git merge-base --is-ancestor. oracle_class: deterministic; provenance: pre-existing Git revision operations specified before mutation.
2. New worktree HEAD equals main, is on work/next-2026-09-05, and has empty porcelain status. Oracle: git rev-parse HEAD, git branch --show-current, git status --porcelain=v1 and git worktree list --porcelain. oracle_class: deterministic; provenance: pre-existing Git operations specified before mutation.
3. Original checkout remains on its original feature HEAD and has the identical pre-existing status after excluding this run's bookkeeping. Oracle: before/after git status --porcelain=v1, git branch --show-current and git rev-parse HEAD. oracle_class: deterministic; provenance: pre-existing Git operations specified before mutation.
4. New worktree passes the existing experiment gate. Oracle: node tools/verify-experiments.js exits 0; oracle_class: deterministic; provenance: pre-existing repository gate documented by remote AGENTS.md.

## Return fields

Workspace identity, provenance, baseline evidence, changed artifacts, disposition and gaps.

## Result

Workspace created at C:/ooo/map-elites-2248-next on work/next-2026-09-05, HEAD bb9d39ce2f8a474ff93443be18ced882f87ab6fb (Merge pull request #15 from eluckey2002/docs/record-rollout-fix-impact). Local main advanced by 27 commits from 7f697d30d6e85b05a80ece9ce65912707314038e to this same commit. It matches fetched origin/main and the live remote main returned by git ls-remote after creation.

Changed artifacts: fetched remote-tracking refs, fast-forwarded refs/heads/main, new refs/heads/work/next-2026-09-05, its worktree registration and checkout directory, and this ticket. Original feature checkout HEAD and pre-existing status are unchanged. No persistent Git configuration change or publication occurred.

Earlier branch explanation: feat/evaluator-foundation isolates the calib-1 authoring evaluator; docs/goals/evaluator-foundation/INTENT_BRIEF.md names its purpose. GitHub PR 1, Integrate frozen evaluator foundation, merged 2026-09-03 at 7f697d3 via integrate/evaluator-foundation-20260903. No PR uses the original feature branch as head.

## Verification

1. PASS, deterministic, pre-existing: main, origin/main and live remote main all bb9d39ce2f8a474ff93443be18ced882f87ab6fb; git merge-base --is-ancestor from original main exits 0.
2. PASS, deterministic, pre-existing: git worktree list registers the new directory at the exact revision and branch; git status --porcelain=v1 is empty before and after the baseline gate and diagnostic probe.
3. PASS, deterministic, pre-existing: original checkout status plus HEAD output is byte-for-byte equal to the captured pre-fetch baseline (including this run's untracked ticket directory); original HEAD remains 5b6f92de7fc9b364c204d807fbb7b4108d38911e.
4. FAIL, deterministic, pre-existing: node tools/verify-experiments.js exits 1, reporting unreadable registration protocols for RESULT-0020, RESULT-0021, RESULT-0024 and RESULT-0026. This is baseline behavior on the unmodified fetched revision, not a setup mutation. In tools/verify-experiments.js:520, path.join creates a Windows backslash path subsequently passed to git show at lines 160-166. A direct two-arm read-only probe of b09737b5:experiments/RESULT-0020/protocol.md showed backslashes exit 128 with zero bytes while forward slashes exit 0 with 13832 bytes. No fix was made: the user authorized updating main and creating a worktree, and both are done.

Overall completion test: FAIL due to criterion 4; workspace setup criteria 1-3 pass. The workspace is Git-clean but has a known failing Windows baseline check. Full application health is not asserted.

Join (orch-integrate, /root): limited. All changed artifacts are in the authorized scope. The read-only Git oracles independently establish the requested update and worktree creation, with no invalidated evidence. Criterion 4 cannot be admitted as passing; the existing gate failure is returned explicitly to the owner rather than repaired outside the requested setup. No child was dispatched; inline execution used the pre-existing-oracle independence route.

## Feedback

Original origin/main was stale relative to live GitHub main; fetch established current identity. Prior branch inspection also found stale CURRENT.md navigation. The fresh-worktree experiment gate failed on Windows historical Git paths. Both observations were logged through friction.py.

## Risks

The existing feature checkout retains its untracked ticket directories. Full solver and gameplay tests are outside this workspace setup. The experiment gate's Windows path handling remains a separate code issue.
