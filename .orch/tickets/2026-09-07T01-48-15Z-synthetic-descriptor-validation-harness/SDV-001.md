---
id: SDV-001
run: 2026-09-07T01-48-15Z-synthetic-descriptor-validation-harness
status: complete
executor: orch-tdd
pack: orch-code-pack
independence: gate
depends_on: []
write_scope:
  - experiments/RESULT-0029/run.js
  - experiments/RESULT-0029/verify.js
  - solver/tests/syntheticDescriptorValidation.test.js
  - docs/CHECK-CARDS.md
excluded_actions:
  - Execute controls, pilot, confirmation, or a shipped-level synthetic outcome corpus.
  - Modify protected product, engine, policy, ledger, current-status, receipt, or prior-experiment files.
  - Register RESULT-0029 or reserve fresh seeds; those belong to the successor research run.
  - Merge, rebase, push, or write into the canonical root checkout.
bound: one red-green tracer, one widening pass, and one correction pass
claimed_by: /root
claimed_at: 2026-09-07T16:26:24Z
---

## Objective

The experiment-local CLI and verifier deterministically exercise a nine-policy
full factorial (three greed centers crossed with three timing slopes; not a
MAP-Elites archive grid), calculate beam-relative greed and full-budget
half-score timing, protect artifact identity and coverage, and refuse
unregistered evidence before game execution.

## Fixed inputs

- Frozen spec:
  `.orch/runs/2026-09-07T01-48-15Z-synthetic-descriptor-validation-harness/spec.md`
  at revision `c3a8bbd`.
- Investigation packet identity:
  `ed993bf19514b9cd904184bbd4aaab40b9ca1197dd137aac8e9b2300edb6c962`.
- Baseline revision: `9d125e8c94f41282388a90402b1fba0b22ae83a8`.
- Standards owners: `AGENTS.md`, `EVIDENCE_LEDGER.md`,
  `experiments/README.md`, `docs/CHECK-CARDS.md`.
- Craft reference:
  `/Users/eluckey/.orchflows/lib/packs/orch-code-pack/references/craft.md`.

## Completion test

1. `node --test solver/tests/syntheticDescriptorValidation.test.js` proves the
   early-ending denominator fixture, the two distinct controls, exact
   nine-policy factorial identities, legal prefix candidate selection,
   protocol-first CLI,
   refuse-overwrite behavior, good serialized artifact PASS, and permanent
   one-field tampered serialized artifact FAIL. Oracle class: deterministic;
   provenance: authored-here, gate re-verifies.
2. `node --test solver/tests/*.test.js` returns 361 tests / 357 pass and exactly
   the four baseline failure identities. Oracle class: deterministic;
   provenance: pre-existing runner plus authored-here focused test.
3. `git diff --name-only 9d125e8 -- src/game.js solver/engine.js solver/level-author.js solver/bot.js EVIDENCE_LEDGER.md CURRENT.md`
   prints nothing. Oracle class: deterministic; provenance: pre-existing.
4. `rg -n "Synthetic descriptor validation" docs/CHECK-CARDS.md` resolves one
   complete card and gate-check returns PROCEED. Oracle class: judged plus
   deterministic presence; provenance: authored-here, gate re-verifies.
5. `git diff --check` passes and the orch-code-pack lens finds no correctness,
   contract, scope, or shape defect. Oracle class: deterministic plus judged;
   provenance: pre-existing plus gate.

## Return fields

- status
- result identity
- changed artifacts
- per-criterion verification
- gate-check verdict
- protected-source verdict
- baseline comparison
- feedback
- risks

## Result

**Status:** complete

**Result identity:** `7f9caf7bdd5769ce27630b36d4bce3090c45b6a6`

**Changed artifacts:**

- `experiments/RESULT-0029/run.js`
- `experiments/RESULT-0029/verify.js`
- `solver/tests/syntheticDescriptorValidation.test.js`
- `docs/CHECK-CARDS.md`
- this ticket and the run worklog

## Verification

1. **PASS — descriptor denominator.** The focused test proves an episode ending
   after two of ten available moves reports `halfScoreMove: 0.2`, not `1.0`.
2. **PASS — distinct controls.** The focused test proves the greed center shifts
   the whole target schedule, timing slope redistributes it around the same
   center, and the exported object is a nine-policy factorial rather than an
   archive grid.
3. **PASS — legal bounded seam.** The chooser returns deterministic legal
   prefixes from `findGreedyChains`; an exact existence predicate now
   distinguishes actual game exhaustion from bounded-policy exhaustion.
4. **PASS — ordering and overwrite.** The real unregistered CLI exits nonzero
   before output; the production writer refuses existing paths.
5. **PASS — artifact integrity.** The real verifier accepts the serialized
   fixture, rejects its one-field identity twin, rejects malformed coverage,
   values, sources, identities, unknown reportable levels, contradictory
   outcomes, and impossible terminal counts. Source closure includes the
   hashing helper it imports.
6. **PASS — no premature evidence.** Tests use only literal synthetic fixtures;
   no shipped-level controls, pilot, or confirmation artifact exists.
7. **PASS by failure identity; frozen literal count corrected.** On result
   revision `7f9caf7`, the full suite returned 371 tests / 367 pass / 4 fail.
   The ten-test increase is exactly this new passing test file. The four
   failures are unchanged: candidate receipts 52 and 54, the stale Universe
   Map generated view, and its date-drift observation. The frozen 361/357
   literal was a caller contract error because it required adding tests while
   keeping the pre-change total unchanged.
8. **PROCEED — gate-check.** The Check Card names scope, enforcement, decay,
   source closure, permanent serialized tamper test, and six explicit limits.
9. **PASS — hygiene and code lens.** `git diff --check` and both Node parse
   checks pass. The fresh-context lens returned four findings; the single
   correction fixed all four, and the focused suite passes 10/10.

**Protected-source verdict:** PASS. The baseline-to-result diff is empty for
`src/game.js`, `solver/engine.js`, `solver/level-author.js`, `solver/bot.js`,
`EVIDENCE_LEDGER.md`, and `CURRENT.md`.

## Feedback

- **Fixed:** bounded-policy exhaustion can no longer masquerade as actual game
  exhaustion.
- **Fixed:** contradictory terminal artifacts now fail closed.
- **Fixed:** the premature predictive-analysis subsystem was removed; the
  successor research protocol must define the empirical method before data.
- **Fixed:** `tools/verify-experiments.js` joined source closure and the Check
  Card.
- **Workflow exception:** two `orch-worker` repair dispatches under-delivered
  without touching the clean worktree. After both were retired, the foreground
  writer made the one correction pass. Both failures are recorded in the
  worklog and friction log.

## Risks

- `beamGreedRatio` remains relative to the bounded candidate pool, not the
  exhaustive legal action set.
- This code proves instrumentation behavior and artifact integrity only. It
  provides no empirical evidence that either descriptor predicts outcomes.
- The exact prediction method, factor-manipulation thresholds, level sample,
  fresh seeds, and stopping rules remain successor-protocol decisions.
