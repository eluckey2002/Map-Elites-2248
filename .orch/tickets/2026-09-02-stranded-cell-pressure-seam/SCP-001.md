---
id: SCP-001
run: 2026-09-02-stranded-cell-pressure-seam
status: complete
executor: tdd
depends_on: []
write_scope:
  - solver/engine.js
  - solver/behavior-descriptors.js
  - solver/policy-eval.js
  - solver/tests/behaviorDescriptors.test.js
  - solver/tests/policy-eval.test.js
  - docs/CHECK-CARDS.md
  - CURRENT.md
  - UNIVERSE.md
  - universe/map.html
  - universe/resolved.json
excluded_actions:
  - change game rules or shipped levels
  - run or register a sampled experiment
  - select or modify a MAP-Elites axis
  - add an evidence-ledger result
  - push to a remote
bound: 90 minutes; one post-move seam, one descriptor, one permanent negative twin
claimed_by: Codex /root
claimed_at: 2026-09-02T07:29:13-05:00
---

## Objective

The existing deterministic policy-play path exposes a reusable post-transition
measurement trace and computes `strandedCellPressure` as the mean per-move share
of the non-stone board footprint occupied by off-lattice tiles, without treating
that measurement as a validated MAP-Elites axis or as evidence of useful range.

## Fixed inputs

- Repository `main` at `6d24d6ac3861e710b5e30193b646125782e6aa1b`.
- `CURRENT.md` definition selected on 2026-09-02.
- `EVIDENCE_LEDGER.md`, `FACT-0006` mergeable-sum lattice.
- `solver/engine.js` transition order and `isMergeableSum` implementation at the
  fixed input revision.
- `solver/policy-eval.js`, public `playToBudget` and `evaluatePolicy` seams at the
  fixed input revision.
- Immediate owner-approved seam: one reusable post-move trace, one descriptor
  now, and a plug-in interface for later descriptors.

## Completion test

1. `playToBudget` records exactly one observation after the full transition for
   a one-move 2x2 deterministic game. On the open subject (four initial 2 tiles,
   `minChain: 3`, `tileScale: 1`, constant RNG 0), the observation has move 1,
   four occupied non-stone cells after refill, and
   `strandedCellPressure === 0`. On the controlled twin differing only by a
   stone at `(1,1)`, the same seam reports three occupied non-stone cells and
   `strandedCellPressure === 1/3`. Oracle: the named literal assertions in
   `solver/tests/behaviorDescriptors.test.js`, run with
   `node --test solver/tests/behaviorDescriptors.test.js`; oracle_class:
   deterministic executable; oracle_provenance: pre-existing (this exact
   subject, twin, transition, and expected literals were frozen before code).
2. The lattice calculation respects non-power-of-two scale: on a two-cell
   state with `tileScale: 3` and values 6 and 9, exactly one cell is stranded
   and pressure is `1/2`. Oracle: named literal test in
   `solver/tests/behaviorDescriptors.test.js`; oracle_class: deterministic
   executable; oracle_provenance: pre-existing.
3. The controlled twin is observed failing before the production implementation
   exists, and the permanent test would fail an always-zero implementation.
   Oracle: initial red `node --test solver/tests/behaviorDescriptors.test.js`
   exit 1 plus the final literal `1/3` assertion; oracle_class: deterministic
   executable; oracle_provenance: pre-existing.
4. A single-game `evaluatePolicy` aggregate reports the same descriptor value as
   `playToBudget` on the identical shipped level and seed, while existing score,
   chain-length, and late-score behavior remains unchanged. Oracle:
   `node --test solver/tests/policy-eval.test.js`; oracle_class: deterministic
   executable; oracle_provenance: pre-existing.
5. Existing engine, MAP-Elites, experiment-admission, and Universe Map checks
   retain their prior behavior. Oracle:
   `node --test solver/tests/engine.test.js solver/tests/policy-eval.test.js solver/tests/mapElites.test.js solver/tests/experiments.test.js solver/tests/universeMap.test.js`;
   oracle_class: deterministic regression suite; oracle_provenance: pre-existing
   (98/98 PASS at the fixed input revision).
6. `docs/CHECK-CARDS.md` contains one card for the new measurement check naming
   its exact scope, controlled twin, blocking rung, decay command, and the blind
   spots that it does not establish useful range, policy separation, fun, or
   MAP-Elites fitness. Oracle: card template field inventory plus
   `rg -n 'stranded-cell-pressure-real-state-seam|Does NOT catch|Crafted-bypass test|Enforcement|Decay' docs/CHECK-CARDS.md`;
   oracle_class: structural inspection; oracle_provenance: pre-existing.
7. `CURRENT.md` says the descriptor is mechanized but not range-qualified, and
   generated Universe views are byte-current. Oracle:
   `node tools/build-universe-map.js && node tools/verify-universe-map.js` plus
   `rg -n 'stranded-cell pressure|mechanized|range' CURRENT.md`;
   oracle_class: deterministic generated-view gate plus structural inspection;
   oracle_provenance: pre-existing.

## Return fields

- changed_artifacts
- red_evidence
- green_evidence
- negative_control
- check_card_verdict
- granted_entitlement
- withheld_claims
- residual_risks

## Result

- **changed_artifacts:** `solver/behavior-descriptors.js` adds the post-move
  registry, trace observation, aggregation, and `strandedCellPressure`;
  `solver/engine.js` exports its existing lattice predicate;
  `solver/policy-eval.js` records after the full transition and aggregates
  without retaining all game traces; focused tests, the SCP Check Card,
  `CURRENT.md`, and `universe/resolved.json` were updated.
- **red_evidence:** the real-seam test first failed 0/1 because
  `postMoveTrace` was absent; the aggregation test then failed 9/10 because its
  summarizer was absent; the blocker-timing slice failed until the trace exposed
  the post-tick blocker count.
- **green_evidence:** descriptor and policy-evaluation tests 13/13 PASS;
  completion suite 103/103 PASS; experiment and Universe gates PASS.
- **negative_control:** same one-move path reports open `0` and its one-stone
  twin `1/3`; duration-1 ice reports zero temporarily blocked cells only after
  ticking; scale-3 values 6/9 report `1/2`.
- **check_card_verdict:** PROCEED at HARD implementation-test rung only;
  `docs/CHECK-CARDS.md#stranded-cell-pressure-real-state-seam`.
- **granted_entitlement:** agents may compute and inspect this candidate
  descriptor through `playToBudget`/`evaluatePolicy` on identified inputs.
- **withheld_claims:** useful range, policy/style separation, human-experience
  prediction, fitness value, MAP-Elites-axis status, or candidate selection.
- **residual_risks:** runners outside `policy-eval.js` remain uninstrumented;
  large-run overhead has not been benchmarked; no range experiment was run.

## Verification

1. PASS — real open subject is `0` with four refilled cells; one-stone twin is
   `1/3` with three refilled non-stone cells. The duration-1 ice subject also
   confirms observation after blocker ticking.
2. PASS — scale-3 values 6 and 9 produce exactly `1/2`.
3. PASS — initial real-seam run exited 1 with `postMoveTrace` absent; permanent
   twin literal makes an always-zero implementation fail.
4. PASS — one-cell `evaluatePolicy` aggregate equals its `playToBudget` trace
   summary; policy-evaluation tests pass.
5. PASS — fixed focused regression suite 103/103; experiment gate PASS;
   Universe Map PASS. Repository-wide suite is 292/297 with the same five
   pre-existing failure classes: three stale candidate receipts and two
   uncommitted durable-state checks.
6. PASS — SCP Check Card contains exact scope, negative subjects, HARD rung,
   decay command, and six explicit blind spots.
7. PASS — `CURRENT.md` says mechanized but not range-qualified; generated
   Universe view verifies byte-current.

## Feedback

[]

## Risks

- The descriptor's denominator is now precisely the non-stone grid footprint;
  temporary ice remains part of that footprint even while its tile cannot be
  selected.
- No empirical standing changed and no ledger result was added.

## Handoff

- **Reason:** final criterion 5 exposed a pre-existing lifecycle defect outside
  this ticket's write scope. Accepted runs RESULT-0021 and RESULT-0024 both
  have reports and ledger standing, but their protocol frontmatter still says
  `status: registered`; changes to their frozen `solver/engine.js` and
  `solver/policy-eval.js` inputs therefore make the live experiment gate fail as
  though the runs were unfinished.
- **Accepted progress:** red/green real-subject, stone-twin, ice-timing, scale-3,
  and one-cell aggregation checks pass; implementation and Check Card are
  present but uncommitted.
- **Remaining:** route the lifecycle failure through the `fix` composition with
  a regression guard, then resume at criterion 5 and the `orch-integrate` join.
- **Budget:** within bound; this is the ticket's first and only suspension.
- **Resumed:** prerequisite fix run
  `2026-09-02-experiment-lifecycle-completion-fix` completed; experiment gate
  PASS and its lifecycle regression 20/20 PASS. Resume at criterion 5.
