---
id: INV-001
run: 2026-09-23-case002-u1-oracle-adoption
status: complete
executor: orch-investigate
independence: gate
depends_on: []
write_scope:
  - .orch/tickets/2026-09-23-case002-u1-oracle-adoption/INV-001.md
excluded_actions:
  - modify repository source, documentation, experiments, branches, commits, remotes, or Git refs
  - reserve or rename any RESULT identity
  - redesign the Progressive Rigor framework
  - contact the user
bound: 45 minutes or 30 tool calls, whichever binds first
claimed_by: inv_001_gpt_5_6_sol_high
claimed_at: 2026-09-23T16:37:10.5771569Z
profile: orch-worker
---

## Objective

Produce one citation-complete Case 002 execution decision that identifies the exact U1 implementation slice from the Case 001 `PASS` state and returns `STOP — SUFFICIENT`, `ESCALATE TO L2 — EVIDENCE PROVIDED`, or `STOP — UNRESOLVED` before any implementation begins.

## Fixed inputs

- Repository: `C:\OOO\Map-Elites-2248`
- Current checkout identity at issue: `main@8404bf7d095c0b20c23a253f25fca6fd377de42b`, with local main 2 ahead / 8 behind `origin/main`.
- Canonical base selected by Case 001: `origin/main@a98d7f42c10d4b832c98f0f81fbf31bc14421277`.
- Planned branch selected by Case 001: `codex/level-map-elites-authoring` (not yet created).
- Oracle source cut: `c88668d1499a449b2638153231a435649594bca4`.
- Case 001 evidence: `.orch/tickets/2026-09-23-case001-map-elites-readiness/INV-002.md` and `INV-003.md`.
- Governing project artifacts: `START-HERE-LEVEL-MAP-ELITES.md`, `docs/plans/2026-09-16-2225-feat-level-map-elites-authoring-plan.md`, `CURRENT.md`, and `EVIDENCE_LEDGER.md`.
- Verified uncommitted repair: `tools/verify-experiments.js` and `solver/tests/experiments.test.js`; preserve it exactly unless primary evidence proves it conflicts with canonical base.
- Progressive Rigor rule: use the cheapest sufficient level; escalate only on evidenced material failure. L1 owns readiness and executable controls. L2 owns Challenge → Repair only after an L1 artifact works as designed yet remains materially vulnerable.
- Case boundary: framework observations are reported separately and never applied as framework redesign during the case.

## Completion test

1. The packet identifies the exact U1 behaviors, source objects, target paths, authority/ledger record, and focused plus baseline verification required before U1 can be called complete. Oracle: commit-qualified citations from the fixed project artifacts and `git show`/`git ls-tree` against the fixed commits. Oracle class: evidence. Provenance: pre-existing.
2. The packet reconciles the verified Windows repair and the current dirty checkout against the selected canonical base, distinguishing safe carry-forward from unrelated user state without changing either. Oracle: `git status --short --branch`, `git diff -- tools/verify-experiments.js solver/tests/experiments.test.js`, `git diff --check`, and commit-qualified comparison to `origin/main@a98d7f42c10d4b832c98f0f81fbf31bc14421277`. Oracle class: deterministic. Provenance: pre-existing.
3. The packet returns exactly one stopping decision and, if escalating, supplies the current artifact, evidenced material failure, why L1 cannot resolve it, and why L2 fits. Oracle: the Case 001 boundary recorded in `INV-003.md` plus the Progressive Rigor rule in Fixed inputs, checked against the cited findings. Oracle class: evidence. Provenance: pre-existing.
4. The packet gives a minimal, path-scoped implementation ticket proposal with no whole-lineage merge, no RESULT reservation, no historical identity rewrite, and no framework redesign. Oracle: Case 001 reuse/conflict controls in `INV-002.md` and `INV-003.md`, checked against every proposed changed path and action. Oracle class: evidence. Provenance: pre-existing.

## Return fields

- status
- result_identity
- verification
- stopping_level
- stopping_decision
- exact_u1_slice
- proposed_implementation_ticket
- escalation_evidence
- failures_found
- cost_added
- framework_observations
- contradictions
- dead_ends
- gaps
- changed_artifacts

## Result

### Contracted return

- **status:** `ready_for_join` (the executor has not changed frontmatter status).
- **result_identity:** this evidence packet, `C:\OOO\Map-Elites-2248\.orch\tickets\2026-09-23-case002-u1-oracle-adoption\INV-001.md`.
- **verification:** `PASS` for the decision packet against all four named completion-test oracles; this is not a claim that U1 has been implemented.
- **stopping_level:** `L1`.
- **stopping_decision:** **`STOP — SUFFICIENT`**.
- **escalation_evidence:** none. Case 001's L1 artifact now works as designed on the target Windows host and no material vulnerability remains that requires a Challenge → Repair loop (`.orch/tickets/2026-09-23-case001-map-elites-readiness/INV-003.md:18-37,44-46`).
- **failures_found:** no L1 execution-decision failure. The missing ranker/portfolio/continuation behaviors below are the expected U1 implementation delta, not a failure of the readiness artifact.
- **cost_added:** zero additional rigor levels, zero implementation changes, zero experiment runs, and zero reserved identities. The proposed U1 ticket is restricted to 24 named paths and existing base dependencies.
- **exact_u1_slice:** the fixed imports, required implementation behaviors, authority record, and verification commands in **Exact U1 slice** below.
- **proposed_implementation_ticket:** the bounded 24-path proposal in **Proposed implementation ticket** below.
- **framework_observations:** the two bounded L1 observations in **Framework observations** below; no redesign is proposed.
- **contradictions:** the three source conflicts in **Contradictions** below, each reconciled without blending identities.
- **dead_ends:** the four rejected routes in **Dead ends** below.
- **gaps:** the four uncovered items in **Gaps left by the bound** below.
- **changed_artifacts:** only `.orch/tickets/2026-09-23-case002-u1-oracle-adoption/INV-001.md`.

### Cited findings

1. **The only valid starting point is a new `codex/level-map-elites-authoring` branch at `a98d7f42c10d4b832c98f0f81fbf31bc14421277`.** Local `main` remains `8404bf7d095c0b20c23a253f25fca6fd377de42b`, two ahead/eight behind `origin/main`, with the two-file verified repair plus unrelated user/run state. Case 001 explicitly rejects local `main` as the base and requires the untracked start page to be materialized on the future branch (`INV-002.md:16-23`; live `git status --short --branch`). **Confidence: high.**

2. **U1 is exactly a repair/adoption slice, not an experiment or a MAP-Elites implementation.** The governing plan requires the bounded oracle and evolved harvesting ranker, named immediate and harvesting arms, the shortest independently replayed witness across arms, bounded misses that remain `UNKNOWN`, an identity-bound root-state continuation seam, mismatch rejection for board/draw-cursor/score/source-puzzle identity, independent illegal-chain rejection, and an append-only narrow owner decision (`a98d7f42c10d4b832c98f0f81fbf31bc14421277:docs/plans/2026-09-16-2225-feat-level-map-elites-authoring-plan.md:212-228`). Case 001 independently classifies U1 as `repair` with the same delta and keeps U4–U7 outside this slice (`INV-002.md:54-66`). **Confidence: high.**

3. **The source cut is reusable but does not already satisfy U1.** At `c88668d…`, `search` imports one harvesting `rankState`, accepts `{ level, seed, ... }`, constructs its own root, and exports only `candidates` and `search`; `solver/oracle/rankers.js` does not exist (`c88668d1499a449b2638153231a435649594bca4:solver/oracle/search.js:1-5,52-78,134-144`; negative `git cat-file -e c88668d…:solver/oracle/rankers.js`). Its verifier is usefully independent of the oracle transition/candidate generator, and its tests already reject planted illegal chains (`c88668d1499a449b2638153231a435649594bca4:solver/oracle/verify.js:1-57`; `c88668d1499a449b2638153231a435649594bca4:solver/tests/oracle.test.js:65-100`). **Confidence: high.**

4. **The oracle's protected dependencies and captured recordings need no copy or edit.** The base and source cut have identical blobs for `solver/engine.js`, `solver/bot.js`, `solver/benchmark-inputs.js`, `solver/benchmark-replay.js`, `solver/human-benchmark.js`, `solver/recording-replay.js`, `solver/board-trace.js`, and `src/game.js` (commit-qualified `git rev-parse <commit>:<path>` comparison). All 25 recording files named by `c88668d…:docs/oracle/corpus.json` are also blob-identical at the base. The corpus loader itself freezes manifest identity `59daa4e54dceef9b5da7eacb730d3cecb08f43fc389f9721adec6b4c3dc31308` and replays exactly those named files (`c88668d1499a449b2638153231a435649594bca4:solver/oracle/corpus.js:9-76`). **Confidence: high.**

5. **The Windows repair is a conflict-free carry-forward onto the selected base.** Before the patch, base and local HEAD share blobs `14497de5a638b7bf4deda6bcd465888cca81a89e` (`tools/verify-experiments.js`) and `ada083c6defa7d32db09c0bbe45c1206c63e6036` (`solver/tests/experiments.test.js`). The repaired working blobs are respectively `cd73f4d1ea5a3ddf689d85095a79ac28dac22c49` and `22a526f198b00511abd78fa352b18dec51cee6d6`; `git diff a98d7f4… --` is identical to the current two-path patch. Live controls pass: 32/32 experiment tests, `EXPERIMENT GATE PASS`, and `git diff --check` exit 0. Case 001 records the same repair and passing controls (`INV-003.md:9-26`). **Confidence: high.**

6. **The two histories assign incompatible meanings to bare `RESULT-0041`, so the adoption record must be commit-qualified.** Canonical base `RESULT-0041` is a greed-harness run closed `INVALID` (`a98d7f42c10d4b832c98f0f81fbf31bc14421277:EVIDENCE_LEDGER.md:849-862`). The source cut's different `RESULT-0041` is the harvesting-policy comparison closed `UNVERIFIED`, with two measured regressions and no entitled primary conclusion (`c88668d1499a449b2638153231a435649594bca4:EVIDENCE_LEDGER.md:893-906`). Case 001 therefore forbids importing the source ledger/experiment directories and requires capability evidence to remain commit-qualified (`INV-002.md:25-37,68-87`). **Confidence: high.**

### Exact U1 slice

#### Fixed imports from `c88668d1499a449b2638153231a435649594bca4`

Import these source/test blobs by path, not by merging the branch:

| Target path | Source blob |
| --- | --- |
| `solver/oracle/cli.js` | `972b3e386be95023cae4749cda2a09d790efe48e` |
| `solver/oracle/corpus.js` | `aec8acd0fb240fdba8a3240af0f0e6d4d020a220` |
| `solver/oracle/harvest-policy.js` | `f627cac396e165e21808b1e80fa0342ea77ba2f4` |
| `solver/oracle/search.js` | `311d4e43d7cfdf1156c0b053cd93c8a79e0bf21a` |
| `solver/oracle/simulation.js` | `e83604c87c02469de450967f8c352e330edaee0f` |
| `solver/oracle/verify.js` | `36315551500cf975175736cd3c4216e0cc7b1d19` |
| `solver/oracle/worker.js` | `1cc9070e9a8684eb2549cbe47b3de139b58db5af` |
| `solver/tests/oracle.test.js` | `d142e77e4cf04e1b59f349882402115fa1783d63` |
| `solver/tests/harvestPolicy.test.js` | `56ab44e5fce2645f8256de98ce2287f81186c5db` |

Import the isolated direct-source provenance packet under `docs/oracle/`: `CONTRACT.md` (`58a4d35d…`), `QUALIFICATION.md` (`cb6ed918…`), `README.md` (`82c4c11a…`), `RESULT.md` (`f4b06a20…`), `corpus.json` (`abd17b0a…`), and five `runs/*.json` blobs (`2ae6d4bd…`, `e1484e11…`, `ec3f7282…`, `b5358157…`, `93e6b1ea…`). These are the complete objects returned by `git ls-tree -r c88668d… -- docs/oracle`. Do not import any source-cut `CURRENT.md`, `EVIDENCE_LEDGER.md`, `experiments/RESULT-0040/**`, `experiments/RESULT-0041/**`, or `experiments/SEEDS.md` (`INV-002.md:25-37,68-87`).

#### Required implementation behavior and target paths

- Add `solver/oracle/rankers.js` with stable named immediate-score and harvesting arms. Both must consume the same legal-action set, seeded draw stream, terminal objective, and deterministic state cap.
- Repair `solver/oracle/search.js` and, only as needed for the root identity record, `solver/oracle/simulation.js` / `solver/oracle/verify.js`: preserve the level-and-seed wrapper; add a continuation seam rooted in an independently verified successor; reject mismatched board, draw cursor, score, or source-puzzle identity; retain the shorter independently replay-valid witness; and preserve another arm's valid witness when one arm is a bounded miss/`UNKNOWN`.
- Keep `solver/oracle/harvest-policy.js` authoring-only. Do not route it into the shipped bot or frozen `calib-1` evaluator.
- Extend only `solver/tests/oracle.test.js` and `solver/tests/harvestPolicy.test.js` for every positive and planted-negative scenario at plan lines 220-227, including the four mismatch fields and initial-state equivalence.
- Materialize the fixed working-tree `START-HERE-LEVEL-MAP-ELITES.md` blob `c9ae3ed44b9b3136c07a81b265c429167f581156` on the implementation branch, as Case 001 requires (`INV-002.md:21-23`).
- Carry forward exactly the verified blobs `cd73f4d1…` at `tools/verify-experiments.js` and `22a526f1…` at `solver/tests/experiments.test.js`; do not scoop any other current-checkout state.
- Append `DECISION-0007` to `EVIDENCE_LEDGER.md` (the base has decisions only through `DECISION-0006`; `git grep '^### DECISION-0007' a98d7f4…` has no match). Its scope is authoring-only use of the `c88668d…` evolved harvesting ranker as one portfolio arm. Its evidence must cite the full source commit and its `UNVERIFIED` result at `c88668d…:EVIDENCE_LEDGER.md:893-906`, retain both regressions, make no superiority/generalization claim, preserve canonical base `RESULT-0041` unchanged, and state that neither the shipped bot nor `calib-1` changes. This is an owner decision, not a new `RESULT` and not an experiment reservation.

#### Required focused and baseline verification before U1 can be called complete

1. Focused oracle tests: `node --test solver/tests/oracle.test.js solver/tests/harvestPolicy.test.js`; every plan scenario must have a positive control and an observed planted negative before final green.
2. Frozen corpus replay: `node solver/oracle/cli.js --verify docs/oracle/runs/attempt-05-full-corpus.json`; expect `valid:true`, `pass:true`, 20 puzzles, and 20 wins, as specified by the source result (`c88668d…:EVIDENCE_LEDGER.md:863-876`).
3. Carried repair: `node --test solver/tests/experiments.test.js` (32/32) and `node tools/verify-experiments.js` (`EXPERIMENT GATE PASS`).
4. Historical boundaries: `git diff --exit-code a98d7f4… -- CURRENT.md experiments/RESULT-0040 experiments/RESULT-0041 experiments/SEEDS.md`; inspect the `EVIDENCE_LEDGER.md` diff and require one append-only `DECISION-0007` with no edit to the canonical `RESULT-0041` record. Source-cut experiment artifacts remain referenced at their original commit, not copied.
5. Receipt/baseline protection: `solver/engine.js` and `solver/level-author.js` remain unchanged, satisfying the receipt-preservation condition at `a98d7f4…:docs/plans/2026-09-16-2225-feat-level-map-elites-authoring-plan.md:345-354`.
6. Repository baseline: `node --test solver/tests/*.test.js`; require the documented baseline to remain green, then run `git diff --check` and a final scoped `git status --short --branch` review.

### Proposed implementation ticket

- **Objective:** On `codex/level-map-elites-authoring` created from exact base `a98d7f42c10d4b832c98f0f81fbf31bc14421277`, materialize the Case 001 PASS controls and implement only U1 so the bounded oracle is a verified, identity-bound authoring dependency with a commit-qualified owner decision.
- **Write scope (24 paths):** `START-HERE-LEVEL-MAP-ELITES.md`; `tools/verify-experiments.js`; `solver/tests/experiments.test.js`; the eight `solver/oracle/{cli,corpus,harvest-policy,rankers,search,simulation,verify,worker}.js` paths; `solver/tests/oracle.test.js`; `solver/tests/harvestPolicy.test.js`; the ten enumerated `docs/oracle/**` files; and `EVIDENCE_LEDGER.md`.
- **Excluded actions:** no whole-lineage merge; no edit/import of `CURRENT.md`, source experiment directories, or `experiments/SEEDS.md`; no `RESULT` reservation, renumbering, or historical identity rewrite; no shipped-bot or `calib-1` change; no U2–U7 work; no framework redesign; no reportable experiment.
- **Acceptance criteria:** (A) exact objects and repair blobs above are present; (B) all seven U1 behavior tests at plan lines 220-227 pass with planted negatives; (C) the append-only decision has the collision-safe semantics above; (D) all six verification steps pass; (E) final diff contains no path outside the 24-path scope.
- **Oracles:** commit-qualified `git ls-tree`/`git show` at the fixed base and source cut; focused Node tests; independent frozen-corpus replay; experiment gate; full solver suite; `git diff --check`; path-scoped Git diff/status review.

### Framework observations

- Case 001's two L1 findings remain sufficient: controls must bind an authoritative identity, and execution-governing controls must be demonstrated on the actual target host (`INV-003.md:39-46`).
- This case adds no framework redesign. It demonstrates why an identity-binding control must include both commit and path when stable labels collide across histories; the existing L1 boundary already accommodates that through commit-qualified evidence and path-scoped integration.

### Contradictions

- Canonical-base `RESULT-0041` is an `INVALID` greed-harness run, while source-cut `RESULT-0041` is an `UNVERIFIED` harvesting comparison. The plan's bare reference cannot safely name both; the implementation decision resolves this by citing `c88668d…:EVIDENCE_LEDGER.md:893-906` and leaving the canonical record untouched.
- `START-HERE-LEVEL-MAP-ELITES.md` is a governing fixed input but is absent from the selected base. Case 001 resolves this by requiring its identified working blob to be materialized on the future branch; it is not evidence that local `main` should be used.
- The plan's U1 file list names core modules but omits the oracle CLI/corpus/worker and `docs/oracle` provenance files required by the existing frozen replay. Case 001's explicit `solver/oracle/` plus documentation/corpus source policy and the source code's imports resolve the actual import set.

### Dead ends

- Whole-branch reuse is rejected because it imports known ledger, experiment, seed, and namespace conflicts (`INV-002.md:68-87`).
- Starting from local `main` is rejected because it is divergent and carries unrelated state (`INV-002.md:18-23`; live status).
- Directly copying `solver/oracle/rankers.js` from the source cut is impossible because that object does not exist there; it is new U1 implementation.
- Importing or paraphrasing the source cut's bare `RESULT-0041` as canonical evidence is rejected because the ID already has a different canonical meaning.

### Gaps left by the bound

- Primary evidence specifies continuation behavior and identity fields but not the exported symbol names or internal verified-root record schema; the implementer must choose the smallest API that makes the prescribed tests pass without widening scope.
- The fixed start page is identified by working-tree Git blob, not by a commit, because it is absent from the selected base; its first commit-qualified identity will be the U1 branch commit.
- Focused oracle and full baseline suites cannot validate the future behavior until implementation exists. This investigation validated only the pre-existing Windows repair and evidence boundaries.
- The branch remains uncreated, no oracle blobs have been imported, and U1 has not begun.

## Verification

1. **PASS — exact U1 slice.** Commit-qualified plan citations define the behaviors and tests; `git ls-tree` supplies every imported source/provenance object; negative object lookup proves `rankers.js` is new; the proposal names all 24 possible changed paths and the append-only decision semantics.
2. **PASS — checkout/repair reconciliation.** `git status --short --branch`, both required diffs, blob comparisons against `a98d7f4…`, `git diff --check` (exit 0), 32/32 experiment tests, and the live experiment gate (exit 0, `EXPERIMENT GATE PASS`) distinguish the exact safe carry-forward from unrelated state.
3. **PASS — stopping decision.** Case 001 says L1 is sufficient after the target-host repair and U1 may begin; the findings reveal no material L1 artifact failure, so no L2 evidence exists (`INV-003.md:28-46`).
4. **PASS — minimal path-scoped proposal.** Every path is either an exact oracle/provenance object, a required U1 behavior/test path, the identified readiness repair/start artifact, or the append-only ledger decision. Whole lineages, experiment identities, historical rewrites, later units, and framework changes are explicitly excluded.

## Feedback

[]

## Risks

- A bare `RESULT-0041` citation would silently bind the wrong experiment on canonical main; require full commit/path qualification in code comments, tests, and the ledger decision.
- Omitting `docs/oracle/corpus.json`, its retained run, or the CLI/corpus/worker modules would make the imported oracle look present while breaking its existing independent replay oracle.
- Building from the current checkout instead of the exact base could accidentally absorb unrelated user/run state. The implementation ticket must stage only its 24-path scope.
